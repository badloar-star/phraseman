# План интеграции системы энергии

## Быстрый старт

### 1. Основной модуль
Файл: `/app/energy_system.ts`

Функции:
- `getEnergyState()` - получить текущую энергию
- `checkAndRecover()` - проверить и восстановить энергию
- `spendEnergy()` - потратить энергию за ошибку
- `addEnergy()` - добавить энергию (для премиум)
- `resetEnergyToMax()` - сброс для тестирования

### 2. Проверка энергии в уроках
Файл: `/app/lesson1.tsx`

Что было добавлено:
```typescript
import { spendEnergy, checkAndRecover } from './energy_system';

// В loadData():
const energyState = await checkAndRecover();
if (energyState.current <= 0) {
  setShowEnergyModal(true);
  return; // Не загружаем урок
}

// После ошибки:
await spendEnergy(1);
```

Модальное окно показывается при недостатке энергии.

### 3. UI на главном экране
Файл: `/app/(tabs)/home.tsx`

Что было добавлено:
- Компонент энергии между хедером и цепочкой
- Прогресс-бар с цветным индикатором (синий/оранжевый/красный)
- Статус восстановления энергии
- Поддержка обоих стилей главного экрана (classic и new)

## Тестирование

### Unit тесты
Файл: `/tests/energy_system.test.ts`

Запуск:
```bash
npm test -- energy_system.test
```

Покрытие:
- getEnergyState() - инициализация и восстановление
- spendEnergy() - расход энергии
- addEnergy() - добавление энергии
- checkAndRecover() - восстановление по таймеру: +1 энергия каждые 10 минут; цепочка дней не ускоряет энергию
- formatTimeUntilRecovery() - форматирование времени

### Ручное тестирование

1. **Проверка недостатка энергии:**
   - Откройте приложение
   - Сделайте 5 ошибок подряд (расходуется вся энергия)
   - При следующей попытке ответа должно появиться модальное окно

2. **Проверка восстановления энергии:**
   - В консоли браузера разработчика (DevTools) выполните:
   ```javascript
   // Установить время последнего восстановления 10+ минут назад
   const newState = {
     current: 0,
     max: 5,
     lastRecoveryTime: Date.now() - 11 * 60 * 1000
   };
   AsyncStorage.setItem('energy_state', JSON.stringify(newState));
   ```
   - Обновите страницу
   - Энергия должна восстановиться до 1

3. **Сброс энергии для разработки:**
   ```javascript
   import { resetEnergyToMax } from './app/energy_system';
   await resetEnergyToMax();
   ```

## Соответствие требованиям

- ✓ Макс энергия: 5 единиц
- ✓ Восстановление: +1 каждые 10 минут; цепочка дней не ускоряет энергию
- ✓ Уроки и квизы: -1 энергия за ошибку
- ✓ Верный ответ: -0 энергии
- ✓ Хранилище: AsyncStorage 'energy_state'
- ✓ Арена и экзамен спендят энергию на старт режима

## Локализация

Система поддерживает русский и украинский языки:

**Русский:**
- "Энергия" - название компонента
- "Полная энергия" - статус при максимуме
- "Следующая энергия через" - время восстановления

**Украинский:**
- "Енергія" - название компонента
- "Повна енергія" - статус при максимуме
- "Наступна енергія за" - время восстановления

## Релевантные файлы

- `/app/energy_system.ts` - основная логика (160+ строк, типизирована)
- `/app/lesson1.tsx` - проверка энергии перед уроком
- `/app/(tabs)/home.tsx` - UI отображение энергии
- `/tests/energy_system.test.ts` - 11 unit тестов
- `/docs/ENERGY_SYSTEM.md` - детальная документация

## Возможные расширения

### 1. Премиум бонус (+2 энергии в день)
```typescript
if (isPremium) {
  const bonusState = await addEnergy(2);
}
```

### 2. Достижение (восстановить энергию)
```typescript
await addEnergy(1);
```

### 3. Специальные события (день рождения, праздник)
```typescript
await resetEnergyToMax();
```

### 4. Функция быстрого восстановления (премиум, стоит монеты)
```typescript
const newState = await addEnergy(1);
```

## Контроль качества

Все проверки пройдены:
- Синтаксис: ✓ (expo lint)
- Типизация: ✓ (TypeScript)
- Тесты: ✓ (Jest - 11/11)
- Интеграция: ✓ (импорты работают)

## Вопросы и ответы

**В: Почему энергия восстанавливается за 10 минут?**
А: Это текущий продуктовый канон. Цепочка дней повышает XP, но не ускоряет восстановление энергии.

**В: Может ли пользователь получить энергию за достижения?**
А: Да, функция `addEnergy()` позволяет добавить энергию. Это можно вызвать из системы достижений.

**В: Что происходит при ошибке?**
А: В уроках и квизах ошибка тратит 1 энергию. Верный ответ энергию не тратит.

**В: Как энергия хранится?**
А: В AsyncStorage под ключом `energy_state` с полной информацией о времени последнего восстановления.
