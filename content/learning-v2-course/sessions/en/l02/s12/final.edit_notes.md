## Изменения

**Судья бреда (правится первым, без обсуждения):**
1. **Карточка `pretty`** — убрано «говорят о женщине или девочке». Слово не ограничено людьми женского пола (`a pretty dress`). Написан факт с примером сочетания.
2. **Разбор `pretty` в задании 9** — было пустое «тут человек ищет дорогу, а не разглядывает витрину». Теперь объясняет ошибку: `pretty` про внешность, а не про то, что человек делает.
3. **«Долгого „ш“» и «тянется „ш“»** (задания 1, 2, 11) — различие `is he` / `is she` не в длине звука, а в его наличии. Везде переписано на «есть „ш“ / нет „ш“», как в интро 2 и задании 3.

**Судья вкуса (BLOCK):**
4. **Интро 1** — вычеркнуты «кто из этих двоих встал первым» и «переехало на шаг влево»: два придуманных образа подряд для одного факта. Оставлено прямое «слушайте первое слово». Заголовок сменён с обещания слушать на «Первое слово решает всё» — теперь он совпадает с заданием страницы (произнести вопрос).
5. **Интро 2** — убран повтор «Порядок тот же…, а стык звучит иначе», следующий абзац говорит это же конкретнее. Добавлена шутка по теме: одного звука хватает, «даже когда всё остальное тонет в шуме» — растёт из самой приметы и из сцены.
6. **Интро 3** — «почти на любой вопрос» заменено на факт «спросили через `is` — отвечайте через `is`». Шутка по теме: длинную фразу в шумном кафе всё равно никто не расслышит — она объясняет, зачем нужен короткий ответ.
7. **Задание 3** — инструкция больше не называет вопросом то, что звучит: «где начало вопроса про женщину?».
8. **`lost`** — получил полноценную карточку (задание 6) и узнавание на слух (задание 7) ДО доски пар. Сноска-определение из задания убрана.
9. **Задание 9** — подсказка была двусмысленной («озирается» одинаково годится и для `lost`, и для `hungry`). Дана однозначная примета: «держит карту и сверяет её с вывеской». Подстановка: `pretty` — про внешность, не про действие; `hungry` — карту от голода не сверяют. Верный один.
10. **Задание 7, разбор `Is she lost?`** — было объяснение только про род; дописано, что это ещё и вопрос вместо сообщения.
11. **Заявка новых слов** — было `Новые слова: —` и `new_words: —` при трёх карточках. Теперь честно: `lost, curious, upset, pretty` (четыре карточки, окно §3 — 1–5 слов). `lost` перенесён из «Возвращаются» в «Новые», так как в сессии он получает карточку.
12. **Счёт заданий** — заголовок обещал 17, фактически было 19; сейчас 21 и в заголовке 19→21 приведено к факту.

**Судья-ученик (REVISE):**
13. **`here` в доске 19** — слово ни разу не звучало в сессии, ученик угадывал. Заменено на `lost`, который в этой сессии прошёл карточку, слух и вставку. Пересечения с доской 8 при этом появилось одно (`lost`) — поэтому доска 8 оставлена как есть, а `lost` в доске 19 стоит уже как повтор внутри сессии, а не как знакомство. **Не устранено полностью:** правило §4 запрещает пересечение досок. Чтобы его сохранить, `lost` в доске 19 заменять нельзя — все остальные слова сессии либо уже на доске 8, либо не отработаны. Оставляю `lost`: пересечение двух досок — меньшее нарушение, чем слово без отработки на доске памяти.

**Машинные факты:**
14. «ЗВУК ПО ОТСУТСТВИЮ» — предупреждение учтено: признак «ш» теперь везде подан как наличие звука у `she` (положительная примета на стыке), а не как «у `he` чего-то не хватает» в отрыве от пары.
15. Обе доски пар: все слова прошли карточку и хотя бы одну отработку до показа.
16. Все три интро: правильный ответ опирается на слово, объяснённое на самой странице.

---

🔴 **Требует немедленного вмешательства**
- Заявка новых слов расходилась с содержанием (`—` при четырёх карточках) — сборщик получил бы неверные данные. Исправлено.

🟡 **Стоит улучшить**
- Доски Speed Match 8 и 19 пересекаются одним словом (`lost`). Конституция §4 требует ноль пересечений. Развязать можно только добавив ещё одно слово с полноценной отработкой — это +2 задания сверх ступени. Оставил на решение владельца.

🧩 **Скрытые связи**
- Перенос `lost` в новые слова затрагивает сторож `check_new_words.mjs`: если `lost` уже заявлялся новым в предыдущих сессиях урока 2, сторож упадёт — тогда правильно вернуть его в «Возвращаются» и убрать карточку, заменив её узнаванием на слух до доски.

---

- Сессия переписана и починена по всем замечаниям судей.
- Убрал неправду о языке: слово «красивый» больше не приписано только женщинам, а разницу между «он» и «она» на слух объясняю через звук «ш», который либо есть, либо его нет — а не через его длину.
- Убрал из объяснений два выдуманных образа подряд и добавил на двух страницах шутку, которая растёт из самого правила, а не приклеена в конец.
- Одно задание раньше можно было решить двумя разными словами — переписал подсказку, теперь верный ответ один.
- Слово «заблудился» теперь сначала объясняется карточкой и отрабатывается, и только потом попадает в игру на память.
- Слово «здесь», которое в сессии вообще не встречалось, убрано из игры на память.
- Честно указал, что в сессии четыре новых слова, а не ноль — раньше в шапке было написано неверно.
- Осталась одна мелочь на ваше решение: две доски на память делят одно слово; развязать можно, только добавив ещё пару заданий.