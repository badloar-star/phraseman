# AUDIT: Уроки 1–32 — словарь и неправильные глаголы
Сгенерировано: 2026-04-30T06:29:07.690Z

Источники данных:
- `app/lesson_words.tsx` — `WORDS_BY_LESSON` (раздел «Словарь» в уроке)
- `app/irregular_verbs_data.ts` — `IRREGULAR_VERBS_BY_LESSON` (раздел «Неправильные глаголы»)
- `app/lesson_data_*.ts` — `LESSON_*_PHRASES` (фразы для проверки покрытия)

## Сводка по урокам
| Урок | Слов в словаре | Неправ. глаголов |
|------|----------------|------------------|
| 1 | 57 | 2 |
| 2 | 23 | 1 |
| 3 | 91 | 16 |
| 4 | 49 | 7 |
| 5 | 33 | 5 |
| 6 | 37 | 5 |
| 7 | 49 | 1 |
| 8 | 63 | 1 |
| 9 | 75 | 0 |
| 10 | 67 | 3 |
| 11 | 76 | 0 |
| 12 | 76 | 4 |
| 13 | 34 | 2 |
| 14 | 67 | 5 |
| 15 | 65 | 5 |
| 16 | 70 | 5 |
| 17 | 115 | 4 |
| 18 | 63 | 6 |
| 19 | 69 | 7 |
| 20 | 50 | 0 |
| 21 | 40 | 1 |
| 22 | 104 | 2 |
| 23 | 48 | 0 |
| 24 | 56 | 2 |
| 25 | 44 | 0 |
| 26 | 91 | 4 |
| 27 | 31 | 0 |
| 28 | 51 | 1 |
| 29 | 67 | 3 |
| 30 | 73 | 2 |
| 31 | 53 | 3 |
| 32 | 74 | 0 |

================================================================
## 1) ДУБЛИКАТЫ в словаре между уроками
Слово в уроке N, которое уже встречалось в словаре урока M < N.
================================================================
OK: точных дубликатов в словаре между уроками нет.

================================================================
## 2) ДУБЛИКАТЫ в разделе «Неправильные глаголы»
Глагол в уроке N, базовая форма которого уже встречалась в неправ. глаголах урока M < N.
================================================================
OK: точных дубликатов в неправильных глаголах между уроками нет.

================================================================
## 3) МЯГКИЕ дубликаты словаря: словоформы базовой леммы из предыдущего урока
Например: «called» в уроке 11, когда «call» уже был в уроке 3.
Это технически НОВЫЕ строки, но базовая лемма уже преподавалась.
================================================================
Всего мягких дубликатов: **203**.

### Урок 7 — 2 словоформ
  - `booking` ← базовая «book» уже была раньше
  - `needed` ← базовая «need» уже была раньше
### Урок 8 — 6 словоформ
  - `checks` ← базовая «check» уже была раньше
  - `closes` ← базовая «close» уже была раньше
  - `finishes` ← базовая «finish» уже была раньше
  - `leaves` ← базовая «leave» уже была раньше
  - `pays` ← базовая «pay» уже была раньше
  - `works` ← базовая «work» уже была раньше
### Урок 9 — 6 словоформ
  - `cafes` ← базовая «cafe» уже была раньше
  - `cars` ← базовая «car» уже была раньше
  - `pens` ← базовая «pen» уже была раньше
  - `pharmacies` ← базовая «pharmacy» уже была раньше
  - `questions` ← базовая «question» уже была раньше
  - `windows` ← базовая «window» уже была раньше
### Урок 11 — 28 словоформ
  - `answered` ← базовая «answer» уже была раньше
  - `attended` ← базовая «attend» уже была раньше
  - `booked` ← базовая «book» уже была раньше
  - `called` ← базовая «call» уже была раньше
  - `changed` ← базовая «change» уже была раньше
  - `checked` ← базовая «check» уже была раньше
  - `cleaned` ← базовая «clean» уже была раньше
  - `closed` ← базовая «close» уже была раньше
  - `cooked` ← базовая «cook» уже была раньше
  - `days` ← базовая «day» уже была раньше
  - `discussed` ← базовая «discuss» уже была раньше
  - `finished` ← базовая «finish» уже была раньше
  - `fixed` ← базовая «fix» уже была раньше
  - `helped` ← базовая «help» уже была раньше
  - `mailed` ← базовая «mail» уже была раньше
  - `moved` ← базовая «move» уже была раньше
  - `opened` ← базовая «open» уже была раньше
  - `ordered` ← базовая «order» уже была раньше
  - `parked` ← базовая «park» уже была раньше
  - `printed` ← базовая «print» уже была раньше
  - `printer` ← базовая «print» уже была раньше
  - `rented` ← базовая «rent» уже была раньше
  - `returned` ← базовая «return» уже была раньше
  - `saved` ← базовая «save» уже была раньше
  - `suitcases` ← базовая «suitcase» уже была раньше
  - `washed` ← базовая «wash» уже была раньше
  - `watched` ← базовая «watch» уже была раньше
  - `watered` ← базовая «water» уже была раньше
### Урок 12 — 1 словоформ
  - `bags` ← базовая «bag» уже была раньше
### Урок 13 — 1 словоформ
  - `shopping` ← базовая «shop» уже была раньше
### Урок 14 — 21 словоформ
  - `cheaper` ← базовая «cheap» уже была раньше
  - `cheapest` ← базовая «cheap» уже была раньше
  - `coldest` ← базовая «cold» уже была раньше
  - `fastest` ← базовая «fast» уже была раньше
  - `freshest` ← базовая «fresh» уже была раньше
  - `happier` ← базовая «happy» уже была раньше
  - `heaviest` ← базовая «heavy» уже была раньше
  - `juicier` ← базовая «juicy» уже была раньше
  - `lightest` ← базовая «light» уже была раньше
  - `liked` ← базовая «like» уже была раньше
  - `looks` ← базовая «look» уже была раньше
  - `quieter` ← базовая «quiet» уже была раньше
  - `quietest` ← базовая «quiet» уже была раньше
  - `shorter` ← базовая «short» уже была раньше
  - `shortest` ← базовая «short» уже была раньше
  - `softer` ← базовая «soft» уже была раньше
  - `softest` ← базовая «soft» уже была раньше
  - `spicier` ← базовая «spicy» уже была раньше
  - `sweeter` ← базовая «sweet» уже была раньше
  - `tastiest` ← базовая «tasty» уже была раньше
  - `walked` ← базовая «walk» уже была раньше
### Урок 15 — 6 словоформ
  - `buildings` ← базовая «building» уже была раньше
  - `clocks` ← базовая «clock» уже была раньше
  - `driving` ← базовая «drive» уже была раньше
  - `licenses` ← базовая «license» уже была раньше
  - `maps` ← базовая «map» уже была раньше
  - `nuts` ← базовая «nut» уже была раньше
### Урок 16 — 11 словоформ
  - `addresses` ← базовая «address» уже была раньше
  - `brings` ← базовая «bring» уже была раньше
  - `finds` ← базовая «find» уже была раньше
  - `folders` ← базовая «folder» уже была раньше
  - `jackets` ← базовая «jacket» уже была раньше
  - `owner` ← базовая «own» уже была раньше
  - `parcels` ← базовая «parcel» уже была раньше
  - `plates` ← базовая «plate» уже была раньше
  - `problems` ← базовая «problem» уже была раньше
  - `puts` ← базовая «put» уже была раньше
  - `waiter` ← базовая «wait» уже была раньше
### Урок 17 — 30 словоформ
  - `bringing` ← базовая «bring» уже была раньше
  - `buying` ← базовая «buy» уже была раньше
  - `carrying` ← базовая «carry» уже была раньше
  - `checking` ← базовая «check» уже была раньше
  - `choosing` ← базовая «choose» уже была раньше
  - `cleaning` ← базовая «clean» уже была раньше
  - `cooking` ← базовая «cook» уже была раньше
  - `cutting` ← базовая «cut» уже была раньше
  - `discussing` ← базовая «discuss» уже была раньше
  - `explaining` ← базовая «explain» уже была раньше
  - `fixing` ← базовая «fix» уже была раньше
  - `gifts` ← базовая «gift» уже была раньше
  - `hanging` ← базовая «hang» уже была раньше
  - `listening` ← базовая «listen» уже была раньше
  - `looking` ← базовая «look» уже была раньше
  - `ordering` ← базовая «order» уже была раньше
  - `painting` ← базовая «paint» уже была раньше
  - `picking` ← базовая «pick» уже была раньше
  - `reading` ← базовая «read» уже была раньше
  - `repairing` ← базовая «repair» уже была раньше
  - `showing` ← базовая «show» уже была раньше
  - `signing` ← базовая «sign» уже была раньше
  - `throwing` ← базовая «throw» уже была раньше
  - `translating` ← базовая «translate» уже была раньше
  - `trying` ← базовая «try» уже была раньше
  - `washing` ← базовая «wash» уже была раньше
  - `watching` ← базовая «watch» уже была раньше
  - `watering` ← базовая «water» уже была раньше
  - `worker` ← базовая «work» уже была раньше
  - `writing` ← базовая «write» уже была раньше
### Урок 18 — 2 словоформ
  - `newspapers` ← базовая «newspaper» уже была раньше
  - `watches` ← базовая «watch» уже была раньше
### Урок 19 — 9 словоформ
  - `boxes` ← базовая «box» уже была раньше
  - `bushes` ← базовая «bush» уже была раньше
  - `forests` ← базовая «forest» уже была раньше
  - `hangs` ← базовая «hang» уже была раньше
  - `hides` ← базовая «hide» уже была раньше
  - `parks` ← базовая «park» уже была раньше
  - `sleeps` ← базовая «sleep» уже была раньше
  - `tables` ← базовая «table» уже была раньше
  - `waits` ← базовая «wait» уже была раньше
### Урок 21 — 4 словоформ
  - `believed` ← базовая «believe» уже была раньше
  - `laptops` ← базовая «laptop» уже была раньше
  - `singer` ← базовая «sing» уже была раньше
  - `wants` ← базовая «want» уже была раньше
### Урок 22 — 24 словоформ
  - `attending` ← базовая «attend» уже была раньше
  - `changing` ← базовая «change» уже была раньше
  - `gives` ← базовая «give» уже была раньше
  - `ignoring` ← базовая «ignore» уже была раньше
  - `keeps` ← базовая «keep» уже была раньше
  - `learning` ← базовая «learn» уже была раньше
  - `likes` ← базовая «like» уже была раньше
  - `lines` ← базовая «line» уже была раньше
  - `moving` ← базовая «move» уже была раньше
  - `opens` ← базовая «open» уже была раньше
  - `relaxes` ← базовая «relax» уже была раньше
  - `selling` ← базовая «sell» уже была раньше
  - `smoking` ← базовая «smoke» уже была раньше
  - `souvenirs` ← базовая «souvenir» уже была раньше
  - `spending` ← базовая «spend» уже была раньше
  - `studying` ← базовая «study» уже была раньше
  - `teaching` ← базовая «teach» уже была раньше
  - `toys` ← базовая «toy» уже была раньше
  - `traveling` ← базовая «travel» уже была раньше
  - `using` ← базовая «use» уже была раньше
  - `visiting` ← базовая «visit» уже была раньше
  - `waiting` ← базовая «wait» уже была раньше
  - `walking` ← базовая «walk» уже была раньше
  - `working` ← базовая «work» уже была раньше
### Урок 23 — 15 словоформ
  - `bills` ← базовая «bill» уже была раньше
  - `explained` ← базовая «explain» уже была раньше
  - `files` ← базовая «file» уже была раньше
  - `gardener` ← базовая «garden» уже была раньше
  - `ideas` ← базовая «idea» уже была раньше
  - `invited` ← базовая «invite» уже была раньше
  - `learned` ← базовая «learn» уже была раньше
  - `meetings` ← базовая «meeting» уже была раньше
  - `protected` ← базовая «protect» уже была раньше
  - `repaired` ← базовая «repair» уже была раньше
  - `reports` ← базовая «report» уже была раньше
  - `signed` ← базовая «sign» уже была раньше
  - `teacher` ← базовая «teach» уже была раньше
  - `tourists` ← базовая «tourist» уже была раньше
  - `used` ← базовая «use» уже была раньше
### Урок 24 — 1 словоформ
  - `sports` ← базовая «sport» уже была раньше
### Урок 25 — 10 словоформ
  - `answering` ← базовая «answer» уже была раньше
  - `contracts` ← базовая «contract» уже была раньше
  - `neighboring` ← базовая «neighbor» уже была раньше
  - `printing` ← базовая «print» уже была раньше
  - `sending` ← базовая «send» уже была раньше
  - `services` ← базовая «service» уже была раньше
  - `settings` ← базовая «setting» уже была раньше
  - `singing` ← базовая «sing» уже была раньше
  - `taking` ← базовая «take» уже была раньше
  - `telling` ← базовая «tell» уже была раньше
### Урок 26 — 13 словоформ
  - `attends` ← базовая «attend» уже была раньше
  - `changes` ← базовая «change» уже была раньше
  - `comes` ← базовая «come» уже была раньше
  - `designer` ← базовая «design» уже была раньше
  - `fixes` ← базовая «fix» уже была раньше
  - `gets` ← базовая «get» уже была раньше
  - `presses` ← базовая «press» уже была раньше
  - `reads` ← базовая «read» уже была раньше
  - `shows` ← базовая «show» уже была раньше
  - `signs` ← базовая «sign» уже была раньше
  - `speaking` ← базовая «speak» уже была раньше
  - `throws` ← базовая «throw» уже была раньше
  - `turns` ← базовая «turn» уже была раньше
### Урок 27 — 4 словоформ
  - `noticed` ← базовая «notice» уже была раньше
  - `promised` ← базовая «promise» уже была раньше
  - `reported` ← базовая «report» уже была раньше
  - `stated` ← базовая «state» уже была раньше
### Урок 28 — 1 словоформ
  - `studies` ← базовая «study» уже была раньше
### Урок 29 — 1 словоформ
  - `loving` ← базовая «love» уже была раньше
### Урок 30 — 1 словоформ
  - `companies` ← базовая «company» уже была раньше
### Урок 31 — 1 словоформ
  - `layer` ← базовая «lay» уже была раньше
### Урок 32 — 5 словоформ
  - `earlier` ← базовая «early» уже была раньше
  - `giving` ← базовая «give» уже была раньше
  - `goods` ← базовая «good» уже была раньше
  - `making` ← базовая «make» уже была раньше
  - `parties` ← базовая «party» уже была раньше

================================================================
## 4) Мусор в словаре (предлоги / артикли / союзы)
Эти слова не должны быть в разделе «Словарь» (это служебные слова).
================================================================
OK: служебных слов в словаре нет.

================================================================
## 5) Слова из фраз урока, отсутствующие в словарях (накопительно)
«Содержательные» слова (без местоимений, to-be, артиклей, предлогов, демонстративов, please)
которые употреблены в фразах урока, но не встречаются ни в одном словаре до этого урока включительно.
================================================================
OK: всё покрыто.

================================================================
## 6) Уроки, у которых нет данных в `IRREGULAR_VERBS_BY_LESSON`
================================================================
OK: все 32 урока имеют запись (даже если пустую).

Урок(и) с пустым списком неправильных глаголов: **9, 11, 20, 23, 25, 27, 32**

================================================================
## 7) В словаре стоит pos=irregular_verbs, но глагола нет в данных неправильных глаголов до этого урока включительно
Здесь словарь маркирует слово как неправильный глагол, но в `IRREGULAR_VERBS_BY_LESSON` его нет.
================================================================
OK: все неправильные глаголы покрыты.
