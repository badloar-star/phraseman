# Аудит раздела «Теория» (все 32 урока) — 2026-06-21

Экран: `app/lesson_help.tsx` (20 022 строки) — единый движок рендера теории
(`Section/Body/Example/Warn/Tip/Table`, цвета `theoryColors`, скроллы) + контент всех 32 уроков
в `const THEORY: Record<number, TheoryContent>` (строки 662–19730). Оболочка экрана и логика XP —
`LessonHelp()` (19732–20022).

Метод: многоагентный воркфлоу (1 агент по движку/оболочке + 8 агентов по контенту 32 уроков
батчами + adversarial-верификация каждой находки против реального кода). Из 98 сырых находок:
**49 подтверждено**, **5 опровергнуто** (ложные), **44 не успели формально проверить** (упёрлись
в лимит сессии — НЕ опровергнуты; ключевые из них я перепроверил вручную). Все находки с
точными `file:line` и цитатами кода.

---

## 1. Главный вывод

Раздел «Теория» **содержательно сильный** и единообразный по тону: у каждого урока чёткая
структура (правило → таблица-формула → примеры → частые ошибки → итог), консистентные карточки
`Warn (❌→✅)` / `Tip`, аккуратная локализация RU/UK и +25 XP в конце. **Главная системная
проблема — табличная вёрстка на узком телефоне**: фиксированная ширина колонки 132px означает, что
любая таблица на 3+ колонки шире экрана и включает горизонтальный скролл, а 4–5-колоночные
таблицы-формулы (самое важное в уроке) физически не помещаются и прячут готовую фразу за правым
краем. Вторая системная проблема — **«простыни»**: длинные уроки (6, 13, 17, 20, 21, 22, 27, 29)
идут сплошным вертикальным скроллом из 15–23 секций без сворачивания, а финальные блоки «частые
ошибки» дословно повторяют ранее показанные `Warn`. Плюс точечные **смысловые/контентные баги** и
**мёртвый код тем** в движке. Визуальных «поломок» рендера почти нет — есть рассинхрон формата
(где-то `Example`, где-то 2-колоночная таблица ради той же пары).

---

## 2. Подтверждённые проблемы

### СКРОЛЛЫ И ТАБЛИЦЫ (корень — `TABLE_COL_MIN_W = 132` фикс., `lesson_help.tsx:46,175,202,211`)

На экране ~360dp полезная ширина контента ~328–340dp → помещается ~2.5 колонки. Любая таблица
шире контейнера → `canScroll=true` (`:176`) → горизонтальный скролл **внутри** вертикального
урока (плохой паттерн: правые колонки не видны без свайпа).

- **[high] 5-колоночные таблицы-формулы → 660px, ~2× ширины экрана.** Уроки 5 (`t2` :2122), 6
  (`t2` :2558), 13 (`t2` :5964, `t7` :6210), 17 (`t2` :8350, `t9` :8646), 25/26/27 (5-кол.).
  Главная формула урока (`Do+you+drink+coffee`, `кто+will+действие`) уезжает за край — ученик
  видит обрезанную формулу. Колонка `will`/`am/is/are` тратит 132px ради 2–4 букв.
- **[high] 4-колоночные таблицы → 528px.** Урок 1 (`t2` :722, `t3`, `t7`), урок 3 (`t2` :1320),
  уроки 11/12 (`t2` :4613/:5290), 21/23/24 (формулы пассива/Present Perfect), 29 (`t2` :16555,
  used to). Часто 4-я колонка `Фраза` = механическая склейка первых трёх (избыточна).
- **[medium] 3-колоночные таблицы → 396px** (уроки 19 :9450/:9523/:9537, 21, 26, 30, 31, 32) —
  лёгкий, но повторяющийся горизонтальный скролл; прячется колонка-пример.
- **[low] Кастомный индикатор скролла таблицы декоративен** (`:191-226`): нативный индикатор
  выключен (`showsHorizontalScrollIndicator={false}`), самодельный thumb 4px без семантики и
  `accessibilityHint` — скринридер не сигналит, что таблица скроллится вбок.
- **Фикс:** для таблиц-формул ввести компонент-диаграмму (цветные «фишки» `[кто][will][verb]` с
  переносом, без горизонтального скролла) ИЛИ убирать избыточную колонку `Фраза` ИЛИ сделать
  последнюю колонку гибкой (`flex`) с переносом вместо фикс-132px.

### ВЁРСТКА И «ПРОСТЫНИ»

- **[medium] Перегруженные ячейки** — по 4–9 примеров через ` / ` в одной 132px-колонке
  переносятся на 6–15 строк, строка таблицы «прыгает», соседняя колонка пустая. Урок 1 (`t3`
  :766, до 9 фраз в ячейке), уроки 8, 11 (`t6` фонетика -ed), 13, 15, 17, 21 (`t5` :10627), 25,
  27, 31. **Фикс:** 1–2 примера на ячейку, остальное в `Body`/`Example`/«облако слов».
- **[medium] Длинные уроки без передышек** (15–23 секции сплошным скроллом): 6 (14 секций), 13
  (16), 17 (20), 20 (19), 21 (17), 22 (22 секции/20 таблиц), 27 (23 секции, хвост из 12 `Warn`),
  29 (21 секция). **Фикс:** аккордеоны/спойлеры на уровне `Section`, объединение однотипных
  блоков (8 почти одинаковых «глагол + -ing» → одна таблица).
- **[low/medium] Финальные «частые ошибки» дублируют ранее показанные `Warn`** почти дословно
  (уроки 9, 21 :10993, 26 :14583, 27, 29). **Фикс:** в итоге — короткий чеклист `→` без повтора
  полных `Warn`.
- **[medium] Пустые ячейки `''` рисуются как «дыра»-колонка с рамками** (выглядит как сломанная
  таблица). Урок 9 `t9` (:4021 — колонка `an` заполнена 1 строкой из 8 ✓ проверено лично),
  урок 7 `t8` (:3240), урок 13 `t7` (:6215, строка Who), урок 15, уроки 25/26 (строка Who в
  5-кол.). **Фикс:** `—` вместо `''` или раздельные блоки `a` / `an`.

### КОНТЕНТНЫЕ БАГИ (точечные, но в учебном материале)

- **[high] Урок 25 `t5` :13411 — `['come', 'coming', 'I came in']`** ✓ ПРОВЕРЕНО ЛИЧНО. Таблица
  учит форме `-ing` (`coming`), а пример `I came in` — это Past Simple, прямо ПРОТИВОПОЛОЖНАЯ
  форма. Дезинформация. **Фикс:** заменить на `while she was coming home` или убрать строку.
- **[high] Урок 20 `t3` :9795–9796** ✓ ПРОВЕРЕНО ЛИЧНО. Дубль строки `an idea` подряд; в первой
  пример `We chose an option` принадлежит строке `an option` (она ниже на :9799). Copy-paste в
  ключевом уроке про артикли. **Фикс:** удалить дубль, оставить `['an idea', 'He has an idea']`.
- **[high] Урок 20 `t8` :10014 / `t5` :9898 — `'один новый umbrella'`** — английское слово в
  RU-тексте + неверный род (муж. при женском «зонт»). **Фикс:** «один новый зонт».
- **[high] Урок 21 :11013 — реальная коллизия `key="tip2"`** с `Tip` из раздела 2 в одном
  render-массиве. React оставит один — финальная памятка может не показаться/мигать. **Фикс:**
  уникальный ключ `tipFinal`.
- **[medium] Урок 27/26 — `Body` ссылается на «фразы 9-10», «фразы 31-38»** (:14946 и др.) —
  нумерация тренажёра, которой в экране Теории НЕТ. Ссылка повисает. **Фикс:** переформулировать
  по смыслу («в отрицательных фразах урока»).
- **[low] Мёртвый тернарник `isUK ? X : X`** — урок 30 `t6` :18486 (колонка «Логика» по-английски
  в обеих ветках), урок 21 `t9` :10790 (UK-ветка содержит русское «без»). **Фикс:** перевести или
  убрать тернарник.

### ТЕМЫ И КОНТРАСТ (движок `theoryColors`, `:48-68`)

- **[medium] Мёртвая ветка `isLight`** (`:50`) ✓ ПРОВЕРЕНО ЛИЧНО: `isLight = themeMode === 'ocean'
  || themeMode === 'sakura'`, но тип `ThemeMode` (`constants/theme.ts:309`) этих значений НЕ
  содержит, а `ThemeContext.tsx:239` мигрирует их в `dark`. Значит `isLight` **всегда false** —
  вся «светлая» ветка (формула `#6D28D9`, светлые строки таблиц, плотные фоны плашек) никогда не
  выполняется. **Фикс:** удалить проверку или определять светлость по реальной яркости фона.
- **[medium] Цвета захардкожены, не зависят от темы** (`:60-66`): формула всегда лавандовая
  `#C4B5FD`, фоны плашек ~10% альфы. На «cinema»-темах (ember/volt/aurora — `ScreenGradient`
  рисует яркий блум снизу + частицы) фон плашки 0.10 почти не отделяет её от фона, фиолетовая
  формула на янтарном фоне даёт слабый контраст. **Фикс:** брать формулу/фон из палитры активной
  темы (`t.accent`), поднять альфу плашек.
- **[medium] Текст теории лежит прямо на анимированном градиенте** без подложки (`Body`/`Example`
  без `backgroundColor`, только `Warn`/`Tip` имеют фон). Серый перевод примера (`c.muted`) на
  светлых участках блума теряет контраст. **Фикс:** лёгкий scrim или карточка `bgSurface` под
  текстовый контент.
- **[low] Кнопка XP игнорирует тему** (`:19973` — `#F5A623`, белый текст): на gold-теме сливается,
  белый на оранжевом — пограничный контраст, disabled = `t.border` может быть невидим.

### ТИПОГРАФИКА

- **[high] Заголовок ячейки таблицы `maxFontSizeMultiplier={1}`** (`:203`) — игнорирует системное
  укрупнение шрифта (Dynamic Type). Размеры `11/12` захардкожены, не берутся из `f`. *(Уточнение
  верификатора: тело ячейки через `TheoryInlineText` всё же масштабируется до 1.2× — заморожен
  именно заголовок.)* **Фикс:** `maxFontSizeMultiplier={1.2}`, брать размер из `f.caption`.
- **[low] Нет размерной иерархии `body`/`sub`** — `BASE_FONTS.body = 14 = sub` (`ThemeContext.tsx:78,81`),
  поэтому перевод примера (`f.sub`) не мельче английского, иерархия держится только на цвете+весе.
  **Фикс:** сделать `sub` реально на 1px меньше.
- **[nit] Смешанные кавычки** `“ ”` vs `"` между уроками 21/22/23; англо-метки в кириллических
  заголовках секций (урок 25 `s3`/`s10`, урок 27 `s15` — «Other reporting verbs: …»).

### КОНСИСТЕНТНОСТЬ ПОДАЧИ

- **[medium] `Example` используется непоследовательно**: уроки 3/4/28/29/30 показывают пару
  eng–rus компактным `Example`, а уроки 1/2/25/26/27/31/32 — той же парой, но в 2-колоночной
  таблице (264px, тяжелее). **Фикс:** единое правило — пара eng–rus всегда `Example`, `Table`
  только для матрицы 3+ осмысленных колонок.
- **[medium] Подсветка `am/is/are` глобальна для ВСЕХ уроков** (`THEORY_ACCENT_WORDS` :45):
  в уроках про артикли/прошедшее/предлоги слова `am/is/are/do/will/it/you` всё равно красятся
  фиолетовым жирным, хотя «формула to be» там не тема — визуальный шум. **Фикс:** флаг подсветки
  на уровне урока.
- **[high/maint] Хрупкая логика подсветки**: `/g`-regex используется и для `split`, и для
  повторного `.test()` с ручным `lastIndex = 0` (:45,89-94) — двойная логика, забытый сброс даст
  пропуск/лишнюю подсветку. **Фикс:** отдельный не-global regex для проверки куска или `Set`.
- **[high] Урок 1 имеет испанскую теорию (`renderES`), уроки 2–4 — нет** (`titleES`/`renderES`
  только у урока 1 во всём файле). Испаноязычный юзер видит полную теорию в уроке 1 и
  английский фолбэк в остальных. **Фикс:** добавить `renderES` или явно `spanishStatus:'missing'`.

### ДОСТУПНОСТЬ

- **[medium] Кнопка «назад» без `accessibilityRole`/`Label`** (`:19869`) — для VoiceOver/TalkBack
  безымянный элемент; тач-таргет ~32px < 44px. Кнопка XP без `accessibilityState={{disabled}}`.
  **Фикс:** добавить роли/лейблы, `hitSlop` до 44px.

### PERF

- **[nit] `new Animated.Value(0)` на каждый рендер** в неактивной ветке `Table` (`:181-183`) —
  не мемоизировано. **Фикс:** `useRef` или статический `transform`.

---

## 3. Опровергнутые находки (для прозрачности)

5 находок верификатор отклонил как НЕ-проблемы: «дубль `key="tip2"` в уроке 22» (там ключи
разные), «урок 1 — простыня» (длинный справочник теории — норма, а не баг), «смешение языков в
ячейках» (англ. слова в колонке примеров — намеренно), 2× «эмодзи ❌/✅ непоследовательны» (на деле
консистентны). Это сильные стороны, ломать их не нужно.

---

## 4. Что бы я сделал лучше (продуктовые улучшения)

| # | Идея | Почему | Эффект |
|---|------|--------|--------|
| 1 | **Компонент `Formula`** — цветные «фишки» ролей (`[кто]` зелёный · `[will]` синий · `[verb]` оранжевый) в строку с переносом вместо широких таблиц-формул | Убирает горизонтальный скролл у самого важного блока + даёт цветовое кодирование грамматики | Высокий |
| 2 | **Аккордеоны/спойлеры на уровне `Section`** для длинных уроков (6,13,17,20,21,22,27,29) | Простыня из 20+ секций → сворачиваемый каркас; важное не тонет | Высокий |
| 3 | **`Table` с гибкой последней колонкой** (или `numCols≥3` → меньше `TABLE_COL_MIN_W`) | Системно лечит горизонтальный скролл всех 3-колоночных таблиц одной правкой движка | Высокий |
| 4 | **«Облако слов» / чип-сетка** вместо мега-ячеек с 9 примерами через `/` | Перечни читаются как теги на всю ширину, а не как 15-строчная башня в 112px | Средний |
| 5 | **Sticky-заголовок текущей секции** + тонкий индикатор прогресса чтения сверху | В уроке на 4 экрана скролла пользователь всегда видит, где он | Средний |
| 6 | **scrim/карточка `bgSurface` под весь текст теории** | Текст перестаёт конкурировать с анимированным блумом фона | Средний |
| 7 | **Диаграмма-трансформация** для вопрос/отрицание (стрелка переноса `To Be` в начало, вставка `not`) | Механика урока 2/4/5/6 — идеальна для анимированной схемы, а не текста в ячейке | Средний |
| 8 | **Единый словарь компонентов**: пара eng–rus → всегда `Example`; `Warn` → всегда `❌→✅`; нейтральные уточнения → `Tip` | Убирает рассинхрон подачи между уроками | Низкий |

---

## 5. Приоритизированный план действий (польза / усилие)

| Приоритет | Что | Файл:строка | Эффект | Сложность |
|-----------|-----|-------------|--------|-----------|
| **P0** | Фикс смыслового бага `come→coming / I came in` | `:13411` | Убрать дезинформацию | Тривиально |
| **P0** | Фикс дубля строки `an idea` + чужой пример | `:9795-9796` | Доверие к уроку артиклей | Тривиально |
| **P0** | Фикс коллизии `key="tip2"` (урок 21) | `:11013` | Финальная памятка показывается | Тривиально |
| **P0** | Род/язык `umbrella` → «зонт» | `:9898,:10014` | Чистота RU-текста | Тривиально |
| **P1** | Компонент `Formula` + замена 4–5-колоночных таблиц-формул | движок + уроки 1,3,5,6,11,12,13,17,23,24,29 | Снимает худший горизонтальный скролл | Средняя |
| **P1** | Гибкая последняя колонка / меньше `132px` для `numCols≥3` | `:46,202,211` | Системно лечит 3-кол. скролл | Средняя |
| **P1** | Пустые `''` → `—` или раздельные блоки `a`/`an` | уроки 7,9,13,15 | Убирает «дыры»-колонки | Низкая |
| **P2** | Аккордеоны для длинных уроков + ужать дубль-«частые ошибки» | уроки 6,13,17,20,21,22,27,29 | Короче скролл, выше удержание | Средняя |
| **P2** | Убрать мёртвую ветку `isLight` + темо-зависимые цвета плашек | `:50-68` | Контраст на cinema-темах, чистый код | Низкая |
| **P2** | scrim/карточка под текст теории | `:19908` | Читаемость на градиенте | Низкая |
| **P3** | `accessibilityRole/Label/State` + тач-таргеты | `:19869,:19966` | Доступность (VoiceOver/TalkBack) | Низкая |
| **P3** | `Example` вместо 2-кол. таблиц; флаг подсветки на урок; кавычки/заголовки | по урокам | Консистентность | Низкая |
| **P3** | `renderES` для уроков 2–4 или явный `spanishStatus` | `:967,:1265,:1641` | Равная локализация ES | Средняя |

> Примечание: 44 находки из фазы верификации не были формально подтверждены из-за лимита сессии
> (НЕ опровергнуты). Самые важные из них (смысловой баг урока 25, пустые колонки `a/an`, мёртвые
> тернарники) перепроверены вручную и включены выше как реальные. Остальные — преимущественно
> «простыни» и длинные ячейки уроков 25–32 той же природы, что и подтверждённые.
