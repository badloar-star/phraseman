# imperative_basic

Jesse rework pass for imperative training.

## Scope

- One microdiagnosis only: `imperative_basic`.
- Keep the MVP contract: category `syntax`, priority `49`, locales `ru/uk`, 6+ examples, 12+ steps, guided recovery, mastery, Smart Trainer, and distractor-specific feedback.
- Preserve contract anchors:
  - `imperative_easy_001`: wrong `You open` feedback mentions `Open the door`.
  - `imperative_contrast_001`: wrong `No` feedback mentions `Don't touch it`.
  - `imperative_contrast_006`: wrong `to be` feedback mentions `Don't be`.
  - `imperative_mixed_003`: wrong `to start` feedback mentions `Let's start`.

## Teaching Point

The learner should hear imperatives as action-first phrases:

1. command: `Open the door`
2. negative command: `Don't touch it`
3. polite request: `Please wait`
4. together suggestion: `Let's start`

## Copy Rules

- Keep tested English anchors exactly.
- Avoid learner-facing raw labels like `base verb` and `subject`.
- Contract contrast arrays keep required labels.

## Structure

- Easy: direct commands.
- Contrast: `Don't`, `Don't be`, no `to`.
- Mixed: `please`, `let's`, pair repair, full sentence repair.
