## Изменения

- **BLOCK педагога закрыт:** `young` теперь имеет 4 касания (карточка 11, правильный ответ в 17, неверный вариант в 12 и 16) и дважды стоит правильным ответом. Шапка и практика согласованы.
- **`nice` стал правильным ответом** — задание 10 (`He is nice`), где выбор идёт по смыслу между `nice` и `friendly`. Раньше слово, разобранное на целой странице интро, ученик ни разу не выбирал сам.
- **`tired` убрано** из сессии и из шапки: слово имело касания, но не работало на тему, а его карточка съедала место, нужное `young`. Шапка возвращаемых слов теперь честная: kind, young, quiet.
- **Убран абзац про `I am new`** из интро 3 — чужая скрепка на странице про `she is`, нигде не отрабатывалась.
- **Убран повтор в интро 1**: правило «между `he` и словом стоит `is`» больше не сказано дважды. Вместо второго повтора — образ скрепки, которая держит фразу.
- **Разведены разборы `are`**: в интро 3 вариант с `are` заменён на `She is young` (смысловая ловушка), в задании 6 остался единственный разбор `are`.
- **Добавлена шутка по теме на каждую страницу интро** — все три растут из самого слова или правила, а не приклеены в конец: интро 1 — «выбросить её нельзя, как ни хочется»; интро 2 — «молчун в углу вполне может быть `nice`, но `friendly` его назовут вряд ли»; интро 3 — «не значит, что её вчера распаковали».
- **Разбито одинаковое строение трёх страниц**: интро 1 — механика скрепки `is`, интро 2 — разница двух близких слов, интро 3 — как одно слово меняет смысл на человеке и чем отличается от `young`. Три разных угла, как в эталоне 3.
- **Сохранены все `best_lines`** судьи-редактора: строка про `nice`/`friendly`, строка про `She is new`, карточка `kind`.
- **Заголовки страниц переписаны** так, чтобы каждый называл свою тему, а не был вариацией «he и she».

Проверка машинных фактов: интро 1 ключ `tall` объяснён на странице, интро 2 ключ `friendly` объяснён, интро 3 ключ `new` объяснён. Слова доски 13 (`tall`, `nice`, `new`, `friendly`) — все с карточкой и отработкой до задания 13: tall (1, 2, 5), nice (3, 4, 10), new (8, 12), friendly (7, 9). Заданий 17, механики только из разрешённых семи, ссылок на время обучения нет.

---

🟡 **Стоит улучшить**
- `kind` теперь встречается только как правильный ответ в задании 16 без карточки в этой сессии. По Конституции §8б слово из прошлых сессий подавать можно, но карточки для него не осталось — если сборщик требует карточку перед первым использованием, задание 16 надо заменить на `quiet`.

🧩 **Скрытые связи**
- Изменён список `new_words` для сборщика: `tired` убрано, `young` добавлено — это влияет на прогресс словаря ученика в последующих сессиях.

---

**Итог простыми словами:**
- Педагог блокировал сессию из-за слова «молодой» — оно было заявлено, но нигде не встречалось. Теперь оно есть в четырёх местах.
- Слово «приятный» объяснялось на целой странице, но ученик ни разу сам его не выбирал — добавил задание, где надо выбрать именно его.
- Убрал лишнее слово «усталый», которое не работало на тему урока, и освободил место для нужного.
- На каждой из трёх страниц объяснения теперь есть шутка про сам язык — раньше не было ни одной, и текст читался как словарь.
- Три страницы объяснения перестали быть одинаковыми: первая про правило, вторая про разницу двух похожих слов, третья про то, как слово меняет смысл, когда говоришь о человеке.
- Убрал повторы: одно и то же правило дважды на странице и один и тот же разбор ошибки в двух местах.