## Изменения

1. **Интро 1, заголовок** — «Два слова, которые слипаются» → «`is` идёт вторым — всегда». Заголовок обещал звук, страница учит порядку слов; теперь совпадает (must_fix судьи вкуса).
2. **Интро 1** — машинный факт «ключ `this` не упомянут» закрыт тем, что `What is this?` теперь и есть разбираемая фраза в тексте: `What is` названо, порядок «сначала `what`, потом `is`, потом сама вещь» ведёт ровно к ответу.
3. **Интро 2** — добавлена шутка, растущая из правила: «одна буква не туда — и вместо цифры вам зачитают название блюда». Сухая страница была дефектом.
4. **Интро 3** — убрано «разберём позже» (ссылка на другую сессию; сессия читается как единственная). На его место — факт о самом языке: без `the` вопрос звучит обрубком.
5. **Интро 3, вопрос** — убраны часы на стене: при видимых часах спрашивать время незачем. Теперь «часов в зале нет».
6. **Интро 3, порядок** — `time` — «время» вынесено в первую строку, до самой фразы: слово вводится раньше, чем требуется в ответе.
7. **`weather`** — добавлены карточка (задание 7) и узнавание на слух (8) ДО доски пар. Ученик-судья не мог сделать задание с `weather`: перевода не было нигде. Старое задание «послушайте, про что спросили» с `weather` убрано как дублирующее.
8. **`street`** — добавлена карточка (задание 9) до первой вставки. Была та же поломка: слово требовалось правильным ответом без перевода.
9. **Задание 12** — сцена вставки со `street` переписана: «зовёт за столик у окна» не давало причины спрашивать улицу, теперь «записываете адрес кафе». Разбор `price` подправлен под новую сцену.
10. **Доска пар со `street` и `cheap`** сдвинута в конец (16): теперь `street` приходит на неё после карточки и отработки.
11. **Число заданий** осталось 17, механики — из разрешённого списка, доски пар без пересечений.

**Не исправлено (с объяснением):** `cheap` — второе касание не добавлено. Слово стоит только на доске пар, и это по-прежнему невыполненное обещание строки «Возвращаются». Ставить ради него восемнадцатое задание — перегруз сессии, вычёркивать из строки — терять слово, которое сборщик берёт для доски. Решение владельца или следующего круга: либо убрать из строки, либо принять одно касание для слов доски пар как норму.

---

🟡 **Стоит улучшить**
- `cheap` остаётся с одним касанием — единственная незакрытая претензия педагога.
- Задания 2, 3, 6, 8 проверяют звук, а судья-ученик читает текст и звука не слышит — оценить их честно он не может. Это ограничение самого суда, не сессии.

🧩 **Скрытые связи**
- Сессия опирается на `the`, введённое раньше в курсе. После правки оговорки «разберём позже» нет — вопрос подаётся целой фразой, как и требует правило «сессия читается как единственная».
- Строка `modes` изменилась: добавились два `word_card`, убран один `listen_choose`. Сборщик должен пересчитать.

---

- Починил урок про вопрос «что это?» в кафе.
- Главная поломка: два слова — «погода» и «улица» — требовались в ответах, но нигде не переводились. Теперь у обоих есть карточка с переводом и тренировка до того, как их спросят.
- Заголовок первой страницы обещал одно, а объяснял другое — переименовал по сути.
- Убрал странность: раньше просили спросить время, хотя часы висели на виду. Теперь часов нет, и вопрос имеет смысл.
- Убрал отсылку «разберём позже» — человек может начать курс с этого урока, и обещание в пустоту ему ни о чём.
- Добавил шутку на вторую страницу — она была сухой справкой.
- Одно слово, «дешёвый», так и осталось почти без работы: объяснил почему и оставил решение на владельца.