## Изменения

**BLOCK судьи бреда (правится первым):**
1. Задание 2, разбор `nine` — убрана выдумка про длину гласной. Теперь разбор идёт по смыслу («счёт, а не место») плюс верный факт про звук «ай».
2. Задание 4, разбор `find` — было «одно слово против двух слогов» (сравнение разных вещей). Теперь честно: слог против слогов, различитель — начало.
3. Задание 4, разбор `bell` — убрана «длина», различие по началу слова.
4. Задание 9, разбор `here` — снят дубль с заданием 2: добавлено, что слова отличаются ТОЛЬКО первым звуком.
5. Задание 8, разбор `street` — исправлено противоречие с интро 3 (там стояло `behind the street`). Теперь объясняется реальная причина: место в пропуск не встанет, `the station` уже есть.
6. Интро 1 — вычеркнуто «рука тянется обрубить фразу» вместе со всем противопоставлением с русским. Осталось только правило про `is`.
7. Интро 2 — вычеркнуто «раздражает» и «англичанин запнётся». Оставлен проверяемый факт: в русском такого слова нет, поэтому его теряют.
8. Интро 3 — вычеркнута выдуманная «половина всех ответов».

**BLOCK педагога и ученика (`are` в целях):**
9. Задание 15 переписано: было `The shops are behind the station` (форма `are` в правильном ответе, нигде не объяснена). Стало `It is behind the shops` — та же операция сессии, а `are` перенесено в лишнюю плитку-ловушку. Это разрешено §3 Конституции: незнакомая форма в ловушке — приём, а не дефект.
10. Задание 17, третий ответ: было `The shops are near the street`, стало `It is near the street`. Финал теперь целиком на операции сессии.

**BLOCK судьи вкуса:**
11. Интро 3 — убрана несуществующая фраза `It is behind the street` («за улицей» — не место). Заменена на `It is behind the shops`.
12. Все три интро сокращены: было 360/364/340 знаков, стало в пределах нормы. Резалось только повторение и разгон, шутка карточек и образы не тронуты.
13. Заголовок интро 3 «Ответ на „где?“ — три слова» обещал три слова при фразах из пяти. Заменён на «Начало не трогают» — по сути страницы.
14. Разбита одинаковая структура трёх страниц: интро 2 и 3 больше не начинаются с «слово — перевод» в заголовке, заголовки теперь по смыслу страницы.
15. Из интро 3 убран повтор правила про `is` (оно уже полностью объяснено в интро 1).

**Сохранено без изменений (`best_lines`):** карточка `near` про «идти пару минут», строка «Одно слово, а объясняет всё» (перенесена в интро 2 первым абзацем), обманка в задании 14.

**Не исправлено:** нет — все `must_fix` закрыты.

---

🟡 **Стоит улучшить**
- Задание 15 теперь содержит лишнюю плитку `are` как ловушку. Если судья-ученик сочтёт, что она провоцирует, а не проверяет, — её можно убрать, оставив ровно нужные пять плиток.

🧩 **Скрытые связи**
- `near` и `behind` заняты: в следующих сессиях они идут строкой «Возвращаются».
- Форма `are` с множественным числом в этой сессии больше не преподаётся — если она была нужна плану урока 2, её место в отдельной сессии со своим объяснением.

---

- Переписал сессию про «рядом» и «позади» по замечаниям всех четырёх проверяющих.
- Убрал два задания, которые требовали от ученика слово, которому его никто не учил — теперь вся сессия держится на одной конструкции.
- Выбросил три неверных объяснения про звучание слов: там было написано, что слова отличаются длиной гласной, хотя они отличаются совсем другим.
- Вычеркнул придуманные красивости: «рука тянется», «раздражает», «половина всех ответов на где» — вместо них простые факты.
- Убрал фразу «это за улицей» — так по-английски не говорят, заменил на «за магазинами».
- Сократил три страницы объяснения до нормальной длины, срезав повторы, а не смысл и шутки.