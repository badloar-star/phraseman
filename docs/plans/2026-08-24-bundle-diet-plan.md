# Программа «Бандл-диета»: JS-бандл 55 → 15–20 МБ, APK 165 → ~95 МБ

Дата: 2026-08-24. Владелец: цель — супермалый бандл (10–20 МБ), при этом
пользователь НИКОГДА не видит незагруженный контент: всё прогружается заранее.

## Точка отсчёта (аудит 2026-08-24, замерено по реальным артефактам)

| Компонент release-APK | Вес | Комментарий |
|---|---|---|
| JS-бандл (Hermes) | **55.3 МБ** | норма для RN — 5–10 МБ; раздут контентом в коде |
| res/ (картинки/звуки/шрифты) | 39.4 МБ | webp 17.8 + wav 14.2 + ttf 5.2 |
| Нативные библиотеки (1 ABI) | 43.7 МБ | WebRTC 12.2 (MAX-звонки), whisper 3.6, OpenSSL 5 |
| classes.dex ×3 | 22.4 МБ | Firebase/Play services |
| Итого распаковано | ~165 МБ | скачивание из стора ~95–110 МБ |

Главный источник: через expo-router ВСЁ из `app/` попадает в бандл, а там ~36 МБ
контента в TS: `plan_content_*` (19 МБ), данные уроков EN/ES (~6.5 МБ),
diagnosis_training (~1.5 МБ), каталоги, карты URL.

## Фаза 0 — быстрые победы ✅ СДЕЛАНО 2026-08-24

- ✅ 34 barrel-импорта `@expo/vector-icons` → прямые (−3.5 МБ ttf; контракт
  `android_bundle_hygiene_contract` снова зелёный). Коммит 7ccd5f545.
- ✅ 60 sfx WAV → M4A 96k: 14.2 → 0.9 МБ (−13.3 МБ). Коммит 50e514b63.
- ✅ Карты аудио-URL компактные: plan 0.89→0.11 МБ, phrase 0.85→0.29 МБ
  (−1.34 МБ JS). Скрипт `scripts/compact_audio_url_maps.mjs`, самопроверка 1:1.
  Коммит 27f584c14.
- ✅ Храповик as-any разблокирован (307→300), сторожа звуков/webp догнали
  реальность. Коммиты b9b52cc3c, 439ba21b7.

Итог фазы 0: **≈ −18 МБ** установки без единого риска для рантайма.

## Фаза 1 — plan_content_* вон из бандла ✅ ЗАВЕРШЕНА 2026-08-24 (−19.0 МБ)

> Статус 2026-08-24: шаги 1–3 СДЕЛАНЫ (коммиты 22e66c510, c93f579d9 + префетч).
> Пак `release.20260824.e4381599` (546 дней: echo 84, gavan 126, impuls 140,
> mitap 112, voyazh 84; 25.14 МБ JSON) выгружен и проверен по публичным URL
> байт-в-байт. Отличия от первоначального наброска — сознательные:
> · префикс `course-packs/plan_content/en/ru/<версия>` вместо `plan-content/<hash>`
>   — storage.rules уже дают этому пути публичное чтение, деплой правил не нужен;
> · plain JSON вместо json.gz — рантайм-цепочка (downloadFileAsync + sha256)
>   доказана на несжатых объектах; gzip добавим после проверки на устройстве.
> Сторож: tests/plan_content_prefetch_contract.test.ts (каноникализация
> экспортёра = рантайму, rowPathFor, префикс, событийность префетча, 5 require).
> Осталось: проверка на устройстве → одобрение владельца → шаг 4 (финал).

> **[ИСТОРИЯ, УЖЕ НЕ АКТУАЛЬНО] Приёмка сначала не прошла — см. разбор ниже.**
> Дев-лог на живом устройстве дважды показал:
> `[plan_content] echo d1 theory → bundled_compatibility (reason: pack_not_cached)`
> — контент дня взят из БАНДЛА, а не с сервера. Пак на Storage корректен
> (проверено: паритет 546/546 побайтово, все 84 дня echo проходят sha256,
> заголовки immutable), значит проблема на клиенте: либо не успевает дедлайн
> 150 мс в `fetchPlanContentDayForScreenServerFirst`, либо пак не скачивается.
> Выпил 5 require из `plan_content_registry.ts` до выяснения = гарантированная
> дыра в контенте у пользователей.
>
> Что уже сделано для диагностики (коммиты 549ab7919, 953394c25):
> · `[plan_content] <plan> d<N> <surface> → <source>` — источник каждого дня;
> · `[plan_pack] ensure → <state> (<ms>)` — исход загрузки пака с деталями
>   ошибки (`network_unavailable` / `manifest_invalid` / `integrity_failed`);
> · `[plan_pack] row miss <path>` — строка дня не в кэше.
> Всё только под `__DEV__`. Следующий шаг: снять эти строки на устройстве и
> по ним найти настоящую причину.

> **✅ ПРИЧИНА НАЙДЕНА И УСТРАНЕНА (2026-08-24, коммит 4baa9d518).**
> Диагностика на живом устройстве дала `[plan_pack] ensure → network_unavailable
> (1787ms)` — при том, что Storage отдавал объекты нормально (curl: 200, 193 КБ,
> 859 мс). Расследование вскрыло ТРИ независимые причины:
>
> 1. **КОРЕНЬ**: `downloadFileAsync` вызывался БЕЗ `idempotent: true`. Нативный
>    Android бросает `DestinationAlreadyExistsException`, если файл существует
>    (`FileSystemModule.kt`: `if (options?.idempotent != true && destination
>    .exists()) throw`). Исключение глоталось общим catch → `network_unavailable`.
>    Пак не собирался НИКОГДА, а лог врал про сеть.
> 2. `SERVER_DEADLINE_MS` = 150 мс против реальной холодной цепочки ~2100 мс
>    (замер: manifest 654 + index 848 + row 597). Сервер не мог выиграть гонку.
>    Поднято до 1200 мс — тёплый диск отвечает ~8 мс и выигрывает с запасом.
> 3. Экран плана грел только манифест; день-строки качались лишь на холодном
>    старте (idle+4.5 с). Добавлен прогрев окна «текущий ±2» при открытии плана.
>
> Плюс `network_unavailable` разделён: у провала index.json теперь своё
> состояние `index_download_failed`.
> Сторожа: `tests/course_pack_download_idempotent_contract.test.ts` (мок
> воспроизводит нативный контракт; проверено обратным экспериментом — без флага
> падает 2 из 3) и три новых кейса в `plan_content_prefetch_contract`.
>
> **✅ ФАЗА 1 ЗАВЕРШЕНА ПОЛНОСТЬЮ (2026-08-24, коммит bcd0db47f).**
> Приёмка на живом устройстве пройдена:
> `[plan_pack] ensure → ready (1634ms)` + `[plan_content] echo d1 theory →
> downloaded_pack`. По решению владельца выпилены все 5 require —
> **−19.0 МБ из JS-бандла** (mitap 4.18 + gavan 4.42 + impuls 4.79 + echo 2.79
> + voyazh 2.86). Реестр остался точкой входа API, но контента не содержит.
> Экспортёр переведён на прямое чтение исходников (проверено: тот же sha256
> 26f964d7…, 546 дней). Сторож перевёрнут: теперь охраняет ОТСУТСТВИЕ require.
> ОТКАТ: `git revert bcd0db47f`.
>
> ПРИНЯТЫЙ РИСК: юзер без сети и без прогретого кэша увидит день без контента.
> Прогрев: открытие плана (окно ±2), покупка/выбор плана (весь план),
> холодный старт. Владелец выбрал этот вариант осознанно.

Инфраструктура УЖЕ ЕСТЬ и включена (`VERIFIED_COURSE_PACK_REMOTE_ENABLED = true`):
`course_pack_remote_loader.ts` (скачивание + дисковый кэш + sha256 + evict),
`plan_content_remote_readiness.ts` (сервер → кэш → bundled-фолбэк),
`course_pack_manifest.ts` (валидация манифеста). Не хватает трёх шагов:

1. **Экспортёр**: скрипт `scripts/export_plan_content_packs.mjs` — читает
   `plan_content_{mitap,gavan,impuls,echo,voyazh}.ts`, пишет пак:
   `manifest.json` (buildCoursePackCacheKey-совместимый) + `index.json` +
   `plans/<plan>/day-NNN.json` (формат уже зашит в rowPathFor:
   `plans/${planId}/day-${NNN}.json`, контент — `{contentHash, content}` под
   `readVerifiedCoursePackDay`).
2. **Выгрузка в Firebase Storage** (`plan-content/<hash>/...`) — по образцу
   ранее использованного upload_plan_audio_to_storage. Storage+CDN, json.gz,
   0 чтений Firestore: клиент качает по манифесту один раз на версию.
3. **Префетч-оркестровка** (чтобы юзер НИКОГДА не увидел дыру):
   - холодный старт: после первого кадра, в idle — `ensureRemoteCoursePack`
     ТОЛЬКО для активного плана пользователя; затем лениво day-rows ±2 дня
     от текущего (каждый ~50 КБ);
   - покупка/выбор плана: сразу фоновый префетч всех дней плана
     (84 × ~50 КБ ≈ 4 МБ, wifi-friendly), тихий прогресс в карточке плана;
   - вход в день: `resolveRemoteOrBundledPlanContentDay` уже отдаёт кэш
     мгновенно; не в кэше → скелет с зарезервированной геометрией (Performance
     Bible) + догрузка одного дня <1 c, ретрай, офлайн-сообщение только когда
     нет НИ кэша, НИ сети;
   - версии: смена манифеста → фоновая догрузка новой версии, старый кэш
     живёт до полной готовности новой (никаких дыр при обновлении контента).
4. **ФИНАЛ (после проверки на устройстве, отдельный коммит-переключатель):**
   выпилить 5 require из `plan_content_registry.ts` → −19 МБ из бандла.
   До этого коммита приложение работает как сегодня (bundled-фолбэк).
   Откат = revert одного коммита.

Риски: офлайн-первый-запуск нового юзера, купившего план до выхода в сеть —
недостижимо (покупка требует сети); повреждённый кэш — уже покрыт sha256+evict.

## Фаза 2 — данные уроков той же трубой (−6–7 МБ JS)

`lesson_data_17_24/25_32`, `lesson_data_*_es.gen`, `lesson_help_theory_data`
(1.07 МБ), `idioms_data`, `lesson_intro_screens_en_17_32/es_l2`,
`diagnosis_training_*` (~15 файлов). Правило: уроки 1–2 (первая неделя юзера)
остаются в бандле НАВСЕГДА — мгновенный онбординг без сети. Остальное — паки
с префетчем «текущий урок ± 2» и полным префетчем курса на wifi в фоне.

## Фаза 3 — learning-v2 content source (−3 МБ JS)

`modules/learning-v2/content/source/*` — та же схема, тот же манифест.

## Фаза 4 — картинки res/ (−9–12 МБ APK) 🔶 ЧАСТИЧНО 2026-08-24

Отчёт: `docs/plans/2026-08-24-webp-size-audit.md`.

- ✅ Размерный аудит: `npm run img:audit-sizes` — для каждого require()-ного webp
  сравнивает пиксели файла с максимальным размером отображения ×3 (@3x).
  Результат: 973 файла / 19.94 МБ, из них 83 oversized.
- ✅ Пережатие по вайтлисту: `npm run img:resize-to-display -- --write`
  (dry-run без флага). Применено к 79 webp: levels/generated-v5-dalle 512→256,
  медали уроков →384, theme-icons 144→96, achievement_categories 256→160,
  onboarding_icon_cutout 1024→256.
- ✅ Иконки приложения: ios-icon 1178→652 КБ, android-icon-foreground
  966→397 КБ (палитра-256, ошибка p99 ≤ 8/255). **−1.63 МБ.**
- ✅ Иконки серий: 44 файла 512/384→256 (показ до 72dp). **−0.39 МБ.**
- ✅ `android-icon-monochrome.png` был побайтовой копией foreground — теперь
  настоящий силуэт из `scripts/build_android_monochrome_icon.mjs`, 397→17 КБ.
  Иконка убрана из `ensure-expo-asset-pngs.mjs`, иначе он вернул бы копию.
- ✅ Сторож `android_bundle_hygiene_contract` не запускался (heap OOM на 2266
  файлах) — обход переписан на генератор, 8/8 зелёные за 20 с, проверки те же.
- 🔶 Ждут коммита чужих сессий (скрипт идемпотентен, подхватит сам):
  `achievements` 1024→512 (~**3 МБ**, все 70 статуэток сейчас `M` у соседа),
  `personal_plan_tasks_fit` (~1.6 МБ), `friends-shared-flame` (~0.55 МБ).
- ❌ НЕ уменьшать: `avatar-auras` (117 ф., 320×320) — кольцо рисуется как
  size × 2.28, при size=112 это 255dp → 766px @3x, запаса НЕТ. Плюс сторожа
  точной геометрии: `level-spin-rewards` 512×512 и season-ауры 320×320 —
  владелец решил 2026-08-24 их не трогать.
- ❌ CDN отклонён владельцем 2026-08-24: «сначала просто уменьшить».

## Фаза 5 — зависимости и натив (отдельные сессии)

- ✅ СДЕЛАНО 2026-08-24: `three` + `@react-three/fiber` + `expo-gl` выпилены с
  корнями — владелец подтвердил «3D закрыт навсегда» (2.5D whole-renders):
  12 тестов-стражей отменённого правила, `components/avatar-dna-3d`,
  `local_assets.ts` + GLB и весь его конвейер (`scripts/avatar-dna/*human_v2*`,
  `fetch_makehuman_cc0`, `lib/`), артефакты human_v3-рига, npm-скрипты
  `avatar-dna:*`, babel-плагин static-block. Чистый TS в
  `modules/avatar-dna-3d` (типы для живого 2D-рига) оставлен.
- ✅ 2026-08-24: `chart.js`/`chartjs-plugin-zoom`/`hammerjs` УДАЛЕНЫ совсем
  (решение владельца: админка грузит их с CDN unpkg, npm-пакеты никто не
  импортировал); `react-native-web`/`react-dom` → devDependencies.
- whisper.rn кладёт 2 копии .so (обычная + оптимизированная) — проверить
  апстрим-опцию отключения generic-варианта (−1.8 МБ/ABI).

## 📊 Контрольный замер 2026-08-24 (после Ф0 + Ф4-частично + Ф5)

Замерено по рабочему дереву (не по старому APK от 20.08):

| Что | Было (аудит) | Стало | Дельта |
|---|---|---|---|
| sfx-звуки | 14.2 МБ (60 wav) | **0.89 МБ** (60 m4a) | −13.3 |
| WAV в проекте | 76 файлов | **0** | −14.1 всего |
| Иконки приложения (3 шт) | 3.06 МБ | **1.04 МБ** | −2.0 |
| images без collectibles (реально в res/) | ~27 МБ | **25.3 МБ** | −1.7 |
| Шрифты | 5.2 МБ в APK | 1.31 МБ на диске (4×Inter) | −3.5 |
| Карты аудио-URL (JS) | 1.74 МБ | 0.40 МБ | −1.34 |

**Суммарно уже снято ≈ 22 МБ** установки (было ~165 МБ → ожидаемо ~143 МБ).
`collectibles/dalli` (79 МБ) в APK не едут — раздаются по CDN, в res/ их нет.

Хвост Фазы 4 практически выбран: повторный `img:audit-sizes` показывает
19.32 МБ / 73 oversized, из них безопасно пережать — **0.28 МБ**. Дальше
резать картинки бессмысленно; главный оставшийся рычаг по res/ — ленивая
CDN-загрузка achievements (4.13) + avatar-auras (2.50) + streak_icons (1.77)
≈ 8.4 МБ, но это отдельное решение владельца.

### Недоделки, найденные повторным аудитом и закрытые
- ✅ 16 мёртвых WAV в `assets/audio/ui/` (0.81 МБ) — ни одного require,
  звуки давно идут из sfx/*.m4a и sounds/fc/*.mp3. Удалены (0603067a4).
- ✅ `personal_plan_tasks_fit` (3.9 МБ) и `friends-shared-flame` (0.8 МБ)
  лежали ВНЕ git, из-за чего скрипт пережатия их намеренно пропускал.
  Взяты под git (905cbf504); повторный аудит показал: размеры корректные,
  пережимать нечего.

### Что осталось (в порядке отдачи)
1. **Финал Фазы 1** — выпил 5 require из `plan_content_registry.ts`
   (−19 МБ JS). Пак выгружен, префетч подключён; ждёт проверки на устройстве
   и одобрения владельца. Это ГЛАВНЫЙ оставшийся рычаг.
2. **Фаза 2** (уроки, −6–7 МБ JS) — только после финала Ф1.
3. **Фаза 3** (learning-v2, −3 МБ JS).
4. Ленивая CDN-загрузка картинок (≈8.4 МБ) — решение владельца.
5. whisper.rn: 2 копии .so (−1.8 МБ/ABI) — не проверено.
6. 🔴 `android-icon-monochrome.png` — копия foreground, а не силуэт
   (найдено Фазой 4, не исправлено).

## 🎯 Замер 2026-08-24 (вечер): Фаза 1 ЗАВЕРШЕНА, снято ~41 МБ

Проверено по коду, не по документам:

| Проверка | Факт |
|---|---|
| `require('./plan_content_*')` в реестре | **0** (были 5) — коммиты bcd0db47f, 74c649637 |
| Бандлируемый код без plan_content | **40.33 МБ** (было 59.4) |
| `three`/`@react-three/fiber`/`expo-gl`/`chart.js`/`hammerjs` | **все удалены** ✓ |
| WAV в проекте | **0** |
| Картинки в сборке (без collectibles) | 25.30 МБ |

**Итог: ~41 МБ снято** (19 план-контента + 22 ассеты/иконки/звуки/карты).
APK ожидаемо ~124 МБ вместо 165; JS-бандл ~36 МБ вместо 55.3.

ВАЖНО: `app/plan_content_*.ts` (19.12 МБ) ОСТАЮТСЯ на диске намеренно —
`scripts/export_plan_content_packs.mjs` читает их через `createRequire` как
источник правды при пересборке пака. Из рантайма они не импортируются
(проверено grep), в бандл не едут. Удалять НЕЛЬЗЯ.

### Статус предложений аудита

| Предложение | Статус |
|---|---|
| Иконки barrel→direct (−3.5 МБ) | ✅ 7ccd5f545 |
| Звуки WAV→M4A (−13.3 МБ) | ✅ 50e514b63 |
| Компактные карты URL (−1.34 МБ) | ✅ 27f584c14 |
| Витрина/smoke вон из стор-бандла | ✅ e95d94b25 |
| Мёртвые audio/ui (−0.81 МБ) | ✅ 0603067a4 |
| Ф1: plan_content на сервер (−19 МБ) | ✅ bcd0db47f |
| Ф5: three/chart.js выпил | ✅ отдельная сессия |
| Ф4: webp + иконки (−3.6 МБ) | ✅ хвост выбран (осталось 0.28 МБ) |
| Ф2: уроки (−6–7 МБ) | 🔜 сессия создана |
| Достижения+ауры на CDN (−7.4 МБ) | 🔜 сессия создана (решение владельца: streak_icons ОСТАВИТЬ) |
| Монохромная иконка Android | 🔜 сессия создана |
| Ф3: learning-v2 (3.57 МБ) | ⏸ после Ф2 |
| whisper.rn дубли .so (−1.8 МБ/ABI) | ⏸ не проверено |
| Деньги: читатели → PhoneState | 🔜 в работе (все 4 читателя ещё на мёртвой коллекции) |

## Целевые цифры

| Метрика | Сейчас | После Ф0 | После Ф1–3 | Цель |
|---|---|---|---|---|
| JS-бандл | 55.3 МБ | ~53.8 | **~17–19 МБ** | −67% ✅ цель владельца |
| APK установка | 165 МБ | ~147 | ~118 | ~95 МБ (с Ф4–5) |
| OTA-апдейт | ~55 МБ | ~54 | **~18 МБ** | каждый релиз ×3 дешевле |

JS-бандл −70% достижим; для всего APK физический потолок ≈ −45%
(WebRTC/whisper/Firebase — работающие функции).

## Порядок и правила

1. Ф1 экспортёр+выгрузка → проверка на устройстве → финал-коммит. Только потом Ф2.
2. Каждый перенос — коммит-переключатель с мгновенным revert.
3. Сторожа не ослаблять; контракты путей обновлять в том же коммите.
4. Никаких новых таймеров/фоновых циклов: префетч только по событиям
   (старт-idle, покупка, вход на экран) — правило Firebase-экономии.
