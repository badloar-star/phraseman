## Изменения

**По судье бреда (BLOCK) — первым делом:**
1. **Интро 1, ложное противопоставление.** Убрано «„я волнуюсь“ — два слова… рука тянется сделать так же коротко». Русский «волнуюсь» — глагол, параллели с прилагательным `nervous` нет. Заменено на настоящее различие: «я взволнован» без связки против обязательного `am`.
2. **Задание 2, неверный факт о звуке.** Дистрактор `never` убран: разводить его с `nervous` русской транскрипцией («хвост -вэс») запрещено — оба слова двусложные, ученик различия не услышит. Поставлены `nurse` и `number`, разбор — по числу слогов и смыслу.
3. **Задание 4, различие «ком-/кон-», которого нет на слух.** Убрано полностью. `comfort` и `content` теперь разводятся счётом слогов (два против трёх) и смыслом.
4. **Интро 3, `am` описано через место в строке** («слово между `I` и тем, какой вы»). Убрано — это правило сломается на `Am I late?`. Во всех интро и разборах одна формулировка: «`am` — форма связки для `I`» / «про себя — `am`».
5. **Интро 1, приписка «признаться в этом вслух — почти всегда выигрыш».** Вычеркнута целиком, ничего на её место не дописано.
6. **Интро 2, «ловушка не в грамматике, а в смелости».** Вычеркнута. На её месте — проверенный факт о слове, который нужен в задании 5: `ready` про момент, `confident` про вас вообще.
7. **Интро 3, «врать в этом месте проверяют сразу».** Вычеркнута; вместо неё — факт по подсказке судьи вкуса: `fluent` не про число выученных слов.

**По судье-педагогу (длина интро):**
8. Все три страницы ужаты до нормы: 401 → 268, 294 → 271, 344 → 281 знака. Резалась вода и повторы («Три слова, и ни одно не выбрасывается» — третье повторение той же мысли), не шутки и не факты.

**По судье вкуса:**
9. **Интро 2 получила свой угол** — разграничение `confident` / `ready` вместо пересказа правила со страницы 1.
10. **Доска пар перенесена в конец** (было задание 9, стало 16). Теперь `ready` до неё отработано как правильный ответ в задании 15, а не только разбором ловушки. Остальные задания сдвинулись на одну позицию, строка `modes` пересчитана.

**Сохранено без изменений** (из `best_lines`): карточка `nervous` про холодные руки, карточка `fluent` про речь, которая идёт сама.

**Не исправлено:** ничего из `must_fix` не отклонено.

---

🟡 **Стоит улучшить**
- Задание 2: дистрактор `number` разводится в основном по смыслу, а не по звуку. Это сознательно — правило языкового файла велит предпочитать смысловой разбор, когда звуковой рискует оказаться неточным.

🧩 **Скрытые связи**
- Перенос доски пар сдвинул нумерацию заданий 9–16 — строка `modes` для сборщика пересчитана целиком, иначе механики съедут на одну позицию.
- Слова `honest`, `calm`, `ready` остаются в строке «Возвращаются»; в «Новые слова» они не попадают — иначе сработает сторож повторов.

---

- Переписал три страницы объяснения: убрал выдуманное сравнение с русским языком, три приклеенные шутки «для красоты» и описание маленького слова `am` через его место в строке.
- Вместо выброшенного дописал только проверенные факты: чем «уверен в себе» отличается от «готов» и что «говорю свободно» — не про число выученных слов.
- Починил два задания на слух: убрал слова, которые на самом деле звучат почти одинаково с нужным, и объяснения через русскую запись звуков.
- Сократил все три страницы примерно на треть — они выходили за норму по длине.
- Перенёс доску с парами слов в конец, чтобы слово «готов» успело появиться в задании как правильный ответ, а не только в разборе ошибки.
- Число заданий осталось прежним — семнадцать, порядок механик пересчитан.