## Изменения

1. **Интро 1, третий абзац** — вычеркнут образ с очередью (метафора-расшифровка, must_fix судьи вкуса). На его место — простое предложение, которое заодно закрывает машинный факт «ключ `lazy` не упомянут в тексте страницы»: теперь `lazy` объяснено прямо там, где стоит правильным ответом.
2. **Задание 14 (было) с `You are right`** — переделано. Слово `right` не проходило в этой сессии, ученик не мог ответить честно (жалоба ученика + must_fix судьи вкуса + границы §3). Теперь это задание 15: та же обманка на внимательность (лишняя плитка `not`), но правильный ответ `You are kind` — слово с карточкой и отработкой в этой же сессии.
3. **`sure` получило правильный ответ** — новое задание 12 («вы сомневались, а она знает точно» → `You are sure`). Закрывает машинный факт «sure: правильным ответом 0» и вердикт педагога о правиле ≥2 касаний.
4. **Задание 16 (речевое) убрано, вместо него сборка `You are not busy`** — чтобы число заданий осталось 17 и `busy` сохранил касание правильным ответом. Финал сцены (17) остался единственным голосовым выходом без подсказок — поддержка по-прежнему убывает.
5. **`alone` получил касание раньше диктанта** — задание 12 ставит его отвлекающим вариантом с разбором, до этого он шёл сразу в сборку. Правильным ответом `alone` идёт в задании 13 и в финале.
6. **Строка `modes` пересобрана** под новую нумерацию; механики те же семь разрешённых, новых не появилось.
7. **Не тронуто:** все три `best_lines` судьи вкуса («извинилась за него же», «бросаются — обычно в свою же сторону», «`wrong` не ругает человека») стоят на местах. Speed Match не менялся: все четыре слова к моменту доски имеют карточку и отработку.

🟡 **Стоит улучшить**
- В интро 3 два новых слова на одной странице (`wrong` и `kind`). Читается нормально, но если владелец захочет строже — `kind` можно вынести целиком в практику, оставив на странице только `wrong`.

🧩 **Скрытые связи**
- Сессия по-прежнему опирается на `You are` (сессии 9–10) и на `not` (сессия 3). Слово `right`, которое было в старом задании, теперь в сессии не встречается вообще — если оно стояло в плане курса как «возвращаемое», план надо сверить.

---

- Убрал из первой страницы запутанное сравнение с очередью — вместо него простое объяснение, где заодно проговаривается слово «ленивая», которое дальше нужно в ответе.
- Выбросил задание, где правильным ответом было слово, которого человек в этой сессии не видел ни разу — теперь на его месте похвала «ты добрая», и слово это в сессии полностью объяснено.
- Слово «уверена» раньше стояло только в неправильных вариантах — добавил задание, где оно правильный ответ.
- Заменил одно повторение вслух на сборку фразы, чтобы слово «занята» тоже отработалось, а заданий осталось те же семнадцать.
- Все удачные строки, которые понравились проверяющим, остались нетронутыми.