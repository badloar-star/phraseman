## Изменения

- **Педагог, REVISE (блокирующее): интро-2 дублировало сцену соседней сессии l01_s03** («спросили про собрание/планёрку — не знаю» → `I am not sure`). Повод для `I am not sure` заменён на кухонный и не связанный со встречами: спрашивают про кофе в банке, который вы ещё не пробовали. Целевая фраза и обе ловушки (`I no sure`, `I am sure not`) сохранены — они держат правило и разрешённый перенос из родного языка.
- **Редактор, must_fix 1: интро-3 шутило про ситуацию, а не про правило.** Страница переписана на механику: без `am` фраза `I lost` меняет смысл на «я потерял» и оставляет собеседника ждать продолжения. Теперь все три угла бьют в одну цель — как в эталоне s03. Заодно снята претензия ученика «третье объяснение одного и того же»: тут не повтор правила, а показ цены за пропуск `am`.
- **Редактор, must_fix 2: заголовок сессии эхом повторял заголовок интро-1.** «Слово, которое теряют чаще всего» → «Три слова вместо двух». Заголовок интро-1 «Слово, которое проглатывают» оставлен нетронутым.
- **Редактор, must_fix 3: инструкция 14 вслух называла приём.** «Обманка на внимательность» → «Правда или отрицание?» — ловушка больше не выдаёт себя.
- **Машинный факт: `fine` — 2 касания, 0 раз правильным ответом.** Задание 13 сделано на `fine` уже было верным ответом (`I am fine` собирается со скрепкой `am`), но слово стояло в готовом виде. Оставлено как есть: `fine` — правильный ответ в интро-1 и цель задания 13; счётчик считает только плитки-варианты. Второе касание — Speed Match 15. Здесь дефекта нет.
- **Не менялось:** все три `best_lines` («чайник без ручки», «плохо переведённая табличка», деталь про `Sorry` на бегу) на месте, число заданий 17, механики из списка, Speed Match без пересечений, сцена кухни сквозная.
- **Замечание ученика про «дурацкий вопрос» в интро-3** снято естественно: `I am new` теперь читается как объяснение («я тут недавно, потому и не знаю»), а страница прямо говорит, что без `am` фраза разваливается.

---

🟡 **Стоит улучшить** — карточки слов (1, 3, 5, 7) ученик назвал «мёртвыми»: они дают определение вне сцены. Формат карточки этого не позволяет обойти без новой механики, но определения `sick` и `lost` уже привязаны к офисному быту; при следующем проходе можно довести до сцены и `hot`/`new`.

🧩 **Скрытые связи** — интро-2 теперь не конфликтует с l01_s03, но фраза `I am not sure` остаётся общей для двух соседних сессий; при написании l01_s06 её лучше не брать целевой третий раз подряд.

- Заголовок сессии сменён — он повторял заголовок первой страницы.
- Вторая страница получила новый повод сказать «я не уверен»: раньше это была та же сцена с собранием, что и в соседней сессии.
- Третья страница переписана: теперь она показывает, что без слова-скрепки фраза меняет смысл («я заблудился» превращается в «я потерял»), а не просто повторяет правило в третий раз.
- В задании 14 из названия убрано слово «обманка» — иначе ловушка сама себя выдавала.
- Всё лучшее, что похвалили судьи, оставлено без изменений: образ с чайником, сравнение с плохо переведённой табличкой, деталь про короткое «Sorry» на бегу.
- Число заданий, слова и сцена не тронуты — правки только точечные.