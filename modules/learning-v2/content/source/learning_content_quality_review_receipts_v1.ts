// Намеренно пусто: автоматическая генерация или автор материала не имеют права
// сами себе выписывать APPROVED. Запись появляется только после независимой
// проверки интро, подбора фраз и каждой из восьми локалей. Fingerprint делает
// решение недействительным после любого изменения текста.
import type { LearningV2ContentQualityReviewReceipt } from './learning_content_quality_gate_v1';

export const LEARNING_V2_CONTENT_QUALITY_REVIEW_RECEIPTS_V1: Readonly<
  Partial<Record<number, LearningV2ContentQualityReviewReceipt>>
> = Object.freeze({});
