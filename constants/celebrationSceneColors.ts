/**
 * Декоративные цвета сцен празднования (v6 «Золотая палата»).
 *
 * зачем: контракт палитр запрещает hex-литералы внутри сцен — иначе цвета
 * расползаются по компонентам и перестают быть системой. Здесь только то, что
 * НЕ зависит от тира (общее для золотого Plus, синего Pro, зелёного VIP и
 * синего MAX): подложки, «мёртвые» треки, красный проблемы.
 *
 * Цвета самих тиров живут в components/premium_celebration/celebrationContent.ts
 * (CELEBRATION_PALETTES) и сюда не дублируются.
 */
export const CELEBRATION_SCENE_COLORS = {
  /** Незаполненный трек шкалы, погашенная плитка, неактивная точка плана. */
  trackDim: 'rgba(255,255,255,0.09)',
  /** Иконка закрытого замка до разблокировки. */
  lockIcon: 'rgba(255,255,255,0.34)',
  /** Подпись лимита «5 / 5» до разрыва. */
  capBg: 'rgba(0,0,0,0.62)',
  capText: '#FFFFFF',

  /** Низ карточки в веере — уходит в тень, чтобы стопка читалась объёмной. */
  deckCardBottom: '#14161C',

  /** Реплика собеседника в диалоге (своя реплика красится в цвет тира). */
  bubbleAiBg: 'rgba(255,255,255,0.11)',
  bubbleAiText: '#FFFFFF',

  /**
   * Красный проблемы: слабый узел графа и неверная фраза.
   * зачем: единственный цвет вне палитры тира — он несёт смысл «здесь ошибка»,
   * и заменить его цветом тира нельзя, иначе исчезнет контраст «проблема → решение».
   */
  nodeWeak: '#E45B5B',
  nodeIdle: 'rgba(255,255,255,0.17)',
  linkIdle: 'rgba(255,255,255,0.12)',

  /** Неактивный номер точки маршрута и цифра закрытого дня серии. */
  planIdleText: 'rgba(255,255,255,0.42)',

  /** Низ столбца аналитики. */
  statBarBottom: '#1B2430',

  /** Аура: белый пик перелива и тёмная подложка лица. */
  auraHighlight: '#FFFFFF',
  auraFaceBg: '#0D0A05',
  /** Образцы тем — реальные акценты тем приложения. */
  auraSwatches: ['#FFD700', '#7C5CFF', '#34D399', '#38BDF8', '#F472B6'] as const,

  /** Сфера MAX: те же тона, что у орба на главной. */
  orbGradient: ['#5E9BE8', '#1E4A85', '#0A1E3C'] as [string, string, string],
  orbGlint: 'rgba(255,255,255,0.34)',
} as const;

/* expo-router route shim: keeps utility module from warning when discovered as route */
export default function __RouteShim() { return null; }
