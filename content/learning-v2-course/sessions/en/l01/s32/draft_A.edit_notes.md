## Изменения

**По судье бреда (BLOCK — первыми):**
1. Интро 1: убран неверный факт «`is` остаётся без работы» (в `It not small` слова `is` вообще нет). Заменено на факт о самом английском: `not` держится за `is`.
2. Интро 2: убрана неверная разбивка «икс-ПЕН-сив» — `x` даёт `[ks]`, где `[k]` закрывает первый слог. Стоит «ик-СПЕН-сив».
3. Интро 3: убрана придуманная реакция слушателя («англичанин ждёт, кто там дождливый»). На её месте — факт: английская фраза не строится без подлежащего, у погоды это место занимает `it`.
4. Задание 11 старой версии (с приписками «погода не бывает маленькой» и «мёрзнете вы не от этого») удалено целиком вместе с обеими приписками; на его месте теперь узнавание `warm` на слух, а подстановка `warm` (задание 12) идёт в утверждении с короткими разборами по делу.

**По судье-ученику (REVISE):**
5. `cold` больше не сваливается на ученика без подготовки: добавлено задание 13 — узнавание на слух с разбором.
6. `clean` тоже получил узнавание на слух — задание 15, до доски пар.
7. `quiet` остаётся на доске 7 (пришёл из прошлых сессий), но теперь у него есть касание раньше доски — ловушка с разбором в задании 2. Отдельного узнавания ему не добавлено: сессия и так на 17 заданий, а Конституция требует для доски либо прошлых сессий, либо отработки — оба условия выполнены.

**По редактору вкуса (REVISE):**
8. Интро 2: четырёхкратный пересказ произношения свёрнут в одну строку, добавлена шутка про язык («рука тянется стукнуть по первому, как в „экспорт“»), и в первый абзац вернулась сама операция сессии — `It is not expensive`.
9. Из интро 1 убрано «Порядок жёсткий» — текст сразу говорит о деле.
10. `warm` больше не появляется впервые в отрицании: сначала слух (11), потом утверждение (12), и только потом доска.

**По педагогу (REVISE, длина):**
11. Интро 1 сокращено с 292 до нормы, интро 2 добито до нормы полезным фактом об ударении и операцией, а не водой.

**Сохранено без изменений:** все три `best_lines` редактора вкуса — про русское «не маленькая» (переписана только вторая половина, где был неверный факт), про `it` в погоде (заменена только придуманная реакция) и разбор про размер в подстановке `warm`. Обманка с лишним `not` осталась в задании 4; вторая (старое задание 16) снята вместе с перестройкой хвоста практики — её место заняли узнавания `cold` и `clean`, без которых ученик не мог работать с доской.

**Не исправлено:** ничего из `must_fix` не оставлено без правки.

---

🔴 **Требует немедленного вмешательства** — нет.

🟡 **Стоит улучшить** — `quiet` в этой сессии живёт бедно: одна ловушка и доска пар, ни одного задания, где он правильный ответ. Формально Конституция это допускает (слово из прошлых сессий), но при следующем проходе его лучше либо дать в отдельной подстановке, либо заменить на доске словом с полноценной отработкой.

🧩 **Скрытые связи** — перенумерованы задания 11–17, поэтому строка `modes:` для сборщика переписана целиком; таблицы самопроверки пересчитаны под новую нумерацию.

---

- Убрал из объяснений три места, где текст говорил неправду про язык: про пропавшее слово, про произношение «дорого» и про выдуманную реакцию собеседника.
- Вместо них написал простые честные факты о самом английском.
- Убрал два разбора ошибок, которые заканчивались приклеенной шуткой ни о чём.
- Добавил три задания на слух — для слов «холодно», «тепло» и «чисто»: раньше они падали на ученика без единого объяснения.
- Переписал вторую страницу объяснения: было четыре раза об одном и том же, стало коротко и с шуткой по делу.
- Общее число заданий осталось прежним — семнадцать.