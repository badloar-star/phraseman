# Белая Admin V2 удалена навсегда

Решение владельца от 2026-08-11:

- белая панель, которая открывалась на `/`, `/index.html`, `#overview` и
  `#english-test`, удалена;
- `admin/v2/index.html`, её router/core/UI, стили, vendor-assets, smoke-скрипты и
  контракты восстановления не должны создаваться снова;
- `/`, `/index.html`, `/v2` и `/v2/**` постоянно перенаправляются на
  `/legacy.html`;
- единственная рабочая админка — `admin/v2/legacy.html`, опубликованная как
  `https://phraseman-ea0b3.web.app/legacy.html#control-panel`;
- название каталога `admin/v2` историческое и не разрешает создавать там вторую
  админку;
- старые планы и Git-история являются только историей и не могут использоваться
  как инструкция по восстановлению удалённой панели.

Машинный запрет закреплён в
`tests/admin_single_surface_contract.test.ts`.
