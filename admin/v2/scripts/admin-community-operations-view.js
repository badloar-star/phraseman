import { COMMUNITY_OPERATION_CAPABILITIES } from './admin-community-operations-state.js';
const LABELS = Object.freeze({ 'mod-queue': 'Очередь модерации', 'help-board': 'Доска помощи', 'helpers-board': 'Топ помощников', clubs: 'Клубы и лиги', 'league-chat': 'Чат лиг', 'arena-ranks': 'Рейтинг Арены', 'arena-live': 'Арена live', 'arena-bets': 'Ставки Арены', 'arena-rooms': 'Комнаты Арены' });
function json(value) { try { return JSON.stringify(value, null, 2); } catch { return '{}'; } }
export function renderCommunityOperationsWorkspace(model, { escapeHtml, can }) {
  const workspace = model.workspace; const items = Array.isArray(workspace?.items) ? workspace.items : [];
  const rows = items.length ? items.map((item) => `<tr><td data-label="ID"><button class="link-button" data-community-action="detail" data-id="${escapeHtml(item.id)}" type="button">${escapeHtml(item.id)}</button></td><td data-label="Статус">${escapeHtml(item.status || item.decision || '—')}</td><td data-label="Версия"><code>${escapeHtml(String(item.version || '').slice(0, 12))}</code></td></tr>`).join('') : '<tr><td colspan="3">Нет записей в ограниченной серверной выборке.</td></tr>';
  const preview = model.preview ? `<section class="card section"><div class="card-header"><div><h2>Серверный preview</h2><p>Опасные Arena-действия показывают точный manifest и требуют другого администратора.</p></div><span class="badge warning">community.approve</span></div><div class="card-body"><pre class="code-preview">${escapeHtml(json(model.preview))}</pre><div class="field"><label for="community-confirmation">Точное подтверждение</label><input id="community-confirmation" placeholder="${escapeHtml(model.preview.confirmation)}"></div><div class="actions"><button class="button" data-community-action="discard" type="button">Отмена</button><button class="button primary" data-community-action="request-approval" type="button">Запросить одобрение</button><button class="button danger" data-community-action="apply" type="button">Применить одобренное</button></div><p>${escapeHtml(model.approvalStatus || '')}</p></div></section>` : '';
  return `<header class="page-header"><div><p class="eyebrow">Комьюнити / Нативные операции</p><h1>Community и Arena</h1><p>Чтение выполняется только по явному обновлению. Глобальные баны остаются в готовом разделе Safety & Moderation.</p></div><div class="actions"><a class="button" href="#safety-moderation">Safety & Moderation</a><button class="button" data-community-action="refresh" type="button">Обновить</button></div></header>
    <div class="notice">Право общего чтения: <code>community.read</code>. Для Help Board достаточно <code>community.help.read</code>. Никакое чтение не запускает очистку или изменение Арены.</div>
    <select class="mobile-view-select" data-community-mobile-view aria-label="Раздел Community">${COMMUNITY_OPERATION_CAPABILITIES.map((id) => `<option value="${id}"${id === model.capabilityId ? ' selected' : ''}>${LABELS[id]}</option>`).join('')}</select><div class="actions native-operation-tabs">${COMMUNITY_OPERATION_CAPABILITIES.map((id) => `<button class="button ${id === model.capabilityId ? 'primary' : ''}" data-community-action="select" data-capability="${id}" type="button">${LABELS[id]}</button>`).join('')}</div>${model.error ? `<div class="notice danger">${escapeHtml(model.error)}</div>` : ''}
    <section class="card section"><div class="card-header"><div><h2>${LABELS[model.capabilityId]}</h2><p>Ограниченная серверная проекция; источник и усечение видимы.</p></div><span class="badge">${workspace?.truncated ? 'truncated' : 'bounded'}</span></div><div class="card-body"><pre class="source-health">${escapeHtml(json(workspace?.sourceHealth || []))}</pre><div class="table-wrap"><table><thead><tr><th>ID</th><th>Статус</th><th>Версия</th></tr></thead><tbody>${rows}</tbody></table></div>${model.detail ? `<details open><summary>Деталь</summary><pre class="code-preview">${escapeHtml(json(model.detail))}</pre></details>` : ''}</div></section>
    <section class="card section"><div class="card-header"><div><h2>Подготовить изменение</h2><p>Cleanup удаляет только <code>arena_profiles</code> с нулём матчей; <code>users</code> и Auth не входят в manifest.</p></div></div><div class="card-body"><div class="report-filters"><div class="field"><label for="community-mutation-action">Действие</label><select id="community-mutation-action"><option value="mod-queue-status">Moderation status</option><option value="help-topic-status">Help topic status</option><option value="helpers-description">Helpers description</option><option value="league-chat-status">League chat status</option><option value="arena-profile-resync">Arena name resync</option><option value="arena-placeholder-cleanup">Arena safe cleanup manifest</option><option value="arena-wager-flag">Arena wager flag</option><option value="arena-room-close">Arena room close</option></select></div><div class="field"><label for="community-target-id">Target / manifest ID</label><input id="community-target-id"></div><div class="field"><label for="community-expected-version">Expected version</label><input id="community-expected-version" placeholder="missing для нового manifest"></div></div><div class="field"><label for="community-payload">Payload JSON</label><textarea id="community-payload">{}</textarea></div><div class="field"><label for="community-reason">Причина</label><textarea id="community-reason"></textarea></div><button class="button primary" data-community-action="preview" type="button">Подготовить preview</button></div></section>
    <section class="columns section"><div class="card"><div class="card-header"><div><h2>Одобрить чужой запрос</h2></div></div><div class="card-body"><div class="field"><label for="community-approval-preview-id">Preview ID</label><input id="community-approval-preview-id"></div><div class="field"><label for="community-approval-reason">Что проверено</label><input id="community-approval-reason"></div><button class="button" data-community-action="approve" type="button"${can('community.approve') ? '' : ' disabled'}>Одобрить</button></div></div><div class="card"><div class="card-header"><div><h2>Продолжить manifest</h2><p>Идемпотентные батчи по 25.</p></div></div><div class="card-body"><div class="field"><label for="community-manifest-id">Manifest ID</label><input id="community-manifest-id"></div><button class="button danger" data-community-action="resume" type="button"${can('community.arena.destructive') ? '' : ' disabled'}>Продолжить безопасную очистку</button><pre class="code-preview">${escapeHtml(json(model.bulkProgress || {}))}</pre></div></div></section>${preview}`;
}
