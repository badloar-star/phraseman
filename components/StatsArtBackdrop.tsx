import React, { memo } from 'react';
import AppArtBackdrop from './AppArtBackdrop';

function StatsArtBackdrop() {
  return <AppArtBackdrop name="statistics" />;
}

export default memo(StatsArtBackdrop);
