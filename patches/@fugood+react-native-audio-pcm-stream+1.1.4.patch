diff --git a/node_modules/@fugood/react-native-audio-pcm-stream/android/src/main/java/com/imxiqi/rnliveaudiostream/RNLiveAudioStreamModule.java b/node_modules/@fugood/react-native-audio-pcm-stream/android/src/main/java/com/imxiqi/rnliveaudiostream/RNLiveAudioStreamModule.java
index 6d7d17d..dfc3b95 100644
--- a/node_modules/@fugood/react-native-audio-pcm-stream/android/src/main/java/com/imxiqi/rnliveaudiostream/RNLiveAudioStreamModule.java
+++ b/node_modules/@fugood/react-native-audio-pcm-stream/android/src/main/java/com/imxiqi/rnliveaudiostream/RNLiveAudioStreamModule.java
@@ -4,6 +4,7 @@ import android.media.AudioFormat;
 import android.media.AudioRecord;
 import android.media.MediaRecorder.AudioSource;
 import android.util.Base64;
+import java.util.Arrays;
 import android.util.Log;

 import com.facebook.react.bridge.Promise;
@@ -95,16 +96,21 @@ public class RNLiveAudioStreamModule extends ReactContextBaseJavaModule {
             public void run() {
                 try {
                     int bytesRead;
-                    int count = 0;
                     String base64Data;
                     byte[] buffer = new byte[bufferSize];

                     while (isRecording) {
                         bytesRead = recorder.read(buffer, 0, buffer.length);

-                        // skip first 2 buffers to eliminate "click sound"
-                        if (bytesRead > 0 && ++count > 2) {
-                            base64Data = Base64.encodeToString(buffer, Base64.NO_WRAP);
+                        // Keep the complete capture. Dropping the first two
+                        // buffers loses roughly 250ms of the learner's first
+                        // word; click suppression belongs in the audio path,
+                        // not in speech capture.
+                        if (bytesRead > 0) {
+                            byte[] chunk = bytesRead == buffer.length
+                                    ? buffer
+                                    : Arrays.copyOf(buffer, bytesRead);
+                            base64Data = Base64.encodeToString(chunk, Base64.NO_WRAP);
                             eventEmitter.emit("data", base64Data);
                         }
                     }
