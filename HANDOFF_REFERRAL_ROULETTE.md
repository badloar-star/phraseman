# HANDOFF: Реферальная рулетка Plus — доделка (для Codex)

Дата: 2026-07-21. Проект: phraseman (Expo 54 / RN 0.81 / Firebase, прод-проект `phraseman-ea0b3`).
Владелец тестирует на телефоне через LAN Metro (`START_METRO.bat` в корне). НЕ оставлять Metro или другие процессы запущенными после себя.

## Правила работы (обязательно)

- Работать НАПРЯМУЮ в `C:\appsprojects\phraseman`, ветка `feature/referral-roulette`. Никаких копий проекта.
- После коммитов: `git branch -f integration/unified-20260721 HEAD` — обновить указатель integration на HEAD. **НЕ делать checkout на integration** — там чужая несмердженная работа (~88 staged + 5 unstaged файлов, unmerged конфликты). Не трогать чужие файлы. `master` — расходящаяся линия, туда не мерджить.
- UI: только токены темы (`useTheme`/`ds`), никаких хардкод-цветов. Должно чисто выглядеть во всех 8 темах + светлой (владелец сидит на светлой!). `fontWeight` только 400/700. Анимации только Reanimated на UI-треде (withTiming и т.п.), никаких setState в кадрах.
- Деплой функций в прод (`phraseman-ea0b3`) разрешён. Деплоить только изменённые функции.
- Владелец запретил макеты/промежуточные остановки — доделать всё до конца и дать полный отчёт.

## Что уже сделано (в ветке feature/referral-roulette)

- Сервер задеплоен: `referralSpin` (веса 55/30/11.5/2.9/0.55/0.05 → 1/7/30/90/180/365 дней, pity 1-й и каждый 10-й ≥7д, идемпотентность по spinRequestId, стак к vip_until), `referralClaimSpin`, `friendsGetProfiles`, 5 admin-callables (`adminListReferrals`, `adminSpinStats`, `adminSpinLogs`, `adminSetSpinWeights`, `adminReferralHealth`), `referralDevGrantSpin` (гейт `remote_config/app.numbers.referral_dev_grant_enabled=true`, лимит 10/сутки). Rules+indexes задеплоены.
- Клиент: `app/roulette.tsx` (лента 72 карточки, 4.8с), `components/roulette_win_modal.tsx`, `app/roulette_about.tsx`, `app/roulette_prizes.ts` (SHOW_SPIN_ODDS=false — шансы скрыты от юзеров), `app/roulette_spin_client.ts`, `app/invite_code_singleton.ts`, `app/friends_profiles_batch.ts`. В `referrals.tsx`: hero-блок рулетки (testID=`referrals-roulette-hero`) + DEV-кнопка (testID=`referrals-roulette-dev-grant`, только `__DEV__`).
- Карточки `assets/roulette/` — 6 шт, 768×512 JPG, full-bleed фронтально.
- Админка `C:\Users\badlo\Documents\kimi\workspace\phraseman-admin-redesign` — раздел «Рефералы» (моки + TODO-шнуры к admin-callables, логика `spinLogic.ts`, vitest 17/17).

## Проблемы, найденные владельцем на телефоне (скриншоты от 2026-07-21 20:36)

1. `app/referrals.tsx`: блок «Твои приглашения» содержит СТАРЫЙ текст: «Здесь появятся друзья, которым ты отправил приглашение. Как только друг поставит приложение, введёт твой код и закончит первый урок — Plus можно забирать.»
2. `app/(tabs)/friends.tsx` (пустое состояние «Учиться вместе веселее»): СТАРЫЙ текст: «Получите 7 дней полного Plus-доступа ко всему за одного приглашённого друга, который установит приложение, введёт ваш код <КОД> и пройдёт один урок полностью. Друг тоже получит 7 дней полного доступа.»
3. Hero-блок рулетки выглядит черновиком: заголовок «Рулетка Plus» переносится на 2 строки; чипы «Прокрутов: 1» и «DEV +1» в строке; внизу ПРОДУБЛИРОВАНА дев-плашка «+1 прокрут (dev)» (два dev-элемента!); превью-лента = 3 маленьких тамбнейла карточек в рамке. Владелец: «видно что это черновик разработчика, неполноценный раздел который пойдёт в релиз».
4. Анимация спина лагает: «анимация подгружается, подлагивания, только потом появляется не совсем правильно» (первый прокрут).

## ЗАДАЧИ (все обязательны, без остановок)

### A. Тексты — переписать под рулетку
Grep по `app/`, `components/`, модалкам на паттерны: `7 дней`, `Plus можно`, `забирать`, `полного Plus-доступа`, `Друг тоже получит`. Переписать ВСЕ реферальные тексты. Новый смысл: «Пригласи друга → друг ставит приложение, вводит твой код и заканчивает первый урок → тебе открывается прокрут Рулетки Plus. В рулетке — Plus от 1 дня до года.» НИГДЕ не обещать конкретные 7 дней. Сохранить интерполяцию кода приглашения. Проверить: модалку «Ввести код», шаринг-текст кнопки «Пригласить», `friends.tsx`, `referrals.tsx`, `roulette_about.tsx`, `invite_*` файлы.

### B. Фича-флаг referral_roulette_enabled (вкл/выкл из админки без обновления приложения)
- Ключ `remote_config/app.numbers.referral_roulette_enabled` (boolean, дефолт true при отсутствии ключа).
- Клиент: гейт (по аналогии с чтением `referral_dev_grant_enabled`). Флаг ВЫКЛЮЧЕН → `referrals.tsx` без hero-блока и рулеточных текстов; `friends.tsx` пустое состояние без реферального оффера (нейтральный «Учиться вместе веселее»; «Пригласить» и «Ввести код» — реферальные, под гейт); бейджи прокрутов скрыты. Интерфейс полностью чистый от рулетки и рефералки.
- Сервер: `referralSpin` и `referralClaimSpin` при выключенном флаге → `failed-precondition`. `referralDevGrantSpin` не трогать (свой гейт).
- Новый admin-callable `adminSetReferralRouletteEnabled(enabled: boolean)` — пишет тот же ключ. В админке `C:\Users\badlo\Documents\kimi\workspace\phraseman-admin-redesign`, раздел «Рефералы»: переключатель «Рулетка Plus и реферальная программа» + отображение текущего состояния (через `adminReferralHealth` или отдельный getter). Моки заменить на реальный шнур где возможно.
- Выставить флаг `true` на проде сразу.

### C. Редизайн hero-блока (`app/referrals.tsx`, testID=`referrals-roulette-hero`)
- Убрать дублирующую дев-плашку «+1 прокрут (dev)» снизу — оставить ОДНУ DEV-кнопку (testID=`referrals-roulette-dev-grant`, только `__DEV__`), компактную, в строке с «Прокрутов: N».
- Заголовок «Рулетка Plus» — чистая сетка без кривого переноса на 2 строки (заголовок одной строкой, чипы справа или под ним).
- Лента-превью карточек: нормальная высота, во всю ширину блока, карточки прямые (НИКАКИХ наклонов!), нарядно, не «3 тамбнейла в рамке».
- Внутри блока короткий объяснительный текст: «Пригласи друга — друг закончит первый урок — получишь прокрут. В рулетке Plus от 1 дня до года.» Кнопка «Крутить» основная, «Как это работает» — ссылка на `roulette_about`.
- Hero-блок ПОДНЯТЬ НАВЕРХ: первым содержимым под хедером «Рефералы», выше блока «Твой код для друзей» (владелец: «рулетка должна быть вообще вверху, сразу, часть раздела рефералов»).

### D. Фикс лагов анимации (`app/roulette.tsx`)
- Если в `package.json` есть `expo-image` — перевести ленту на него (`cachePolicy="memory-disk"`, priority). Если нет — `Image.prefetch` всех 6 карточек на маунте экрана (плюс прогрев из `referrals.tsx` при наличии прокрутов), старт анимации только после готовности картинок.
- Уменьшить ленту с 72 копий до минимально необходимого числа под 4.8с (обычно 30–40), дальше — зацикливание паттерна.
- Отложенный старт: withTiming только после `onLayout` + готовности картинок; до готовности — статичный первый кадр, без fade-скачков.
- Проверить: нет setState в кадрах, translateX только через shared value на UI-треде, массив карт в memo, нет тяжёлых вычислений в render.

### E. Проверки, коммит, деплой
- `tsc` 0 новых ошибок (клиент + functions), vitest если затронут spinLogic.
- `expo export` собирается.
- Коммит(ы) в `feature/referral-roulette`, затем `git branch -f integration/unified-20260721 HEAD`.
- Деплой в `phraseman-ea0b3` только изменённых функций: `referralSpin`, `referralClaimSpin`, `adminSetReferralRouletteEnabled` (+ `adminReferralHealth` если менялся). Выставить `referral_roulette_enabled=true` в `remote_config/app.numbers` на проде.
- НЕ оставлять запущенных процессов.

## Формат финального отчёта владельцу
По каждому пункту A–E: что сделано (файлы, ключевые изменения), хеши коммитов, статус `integration/unified-20260721`, что задеплоено, как проверить на телефоне (перезапуск `START_METRO.bat`), как выключить фичу в админке. Если что-то не удалось — честно указать что и почему.
