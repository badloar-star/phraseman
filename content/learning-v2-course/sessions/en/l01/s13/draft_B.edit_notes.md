## Изменения

- **Судья бреда, BLOCK — `tall` только про человека.** Неверный факт: `a tall tree`, `a tall building` — обычные сочетания. Интро 3 и карточка 8 переписаны: «про всё высокое и вытянутое: человек, дерево, дом».
- **Судья бреда — карточка `young` «сил много».** Возраст не равен силе, плюс это заранее смешивало `young` со `strong`. Оставлен только возраст.
- **Судья бреда — «ровно теми же тремя словами» и «англичане говорят».** Отсылка к тому, чего в тексте нет, и абстрактный носитель. Заменено фактом о языке.
- **Судья бреда — придуманная причина ошибки «`am` привыкло стоять первым».** В `I am ready` оно второе. Осталась только сама ошибка.
- **Судья бреда — «Не пускайте его к `you` — оно там чужое».** Приписка-украшение после уже сказанного правила. Вычеркнута.
- **Судья бреда — `not` в ловушке задания 13.** Слово в сессии не вводилось. Заменено на `is` — оно уже отработано в задании 5.
- **Ученик, BLOCK — `brave` и `kind` без карточки и без звука** стояли на доске пар (16) и в устном финале (17). Вторая доска убрана, `kind` получил карточку (14) и узнавание на слух (15), `brave` из сессии убран целиком. Финал 17 теперь состоит из слов, которые ученик слышал: `kind`, `tall`, `ready`, `strong`.
- **Ученик — `tired` в сборке.** Оставлен: слово отработано ловушкой в задании 10 и подсказка в задании 16 однозначна. Добавлена лишняя плитка `I` — обманка на внимательность.
- **Редактор вкуса — «Ловушка тут одна и она смешная».** Объявление шутки вместо шутки. Убрано.
- **Редактор вкуса — интро 3 повторяло правило и осталось без своей мысли.** Заменено на новый угол: рост, возраст и настроение в английском лепятся к человеку одной скрепкой (по-русски про рост говорят иначе — это настоящее различие, проверено подстановкой).
- **Редактор вкуса — разгон в интро 1.** Оборот «Между ними и словом о человеке всегда стоит скрепка. Но разная» сжат в одно предложение.
- **Машинный факт — интро 1: `ready` не упомянут в тексте.** Слово стоит в примере правильного ответа и знакомо из прошлых сессий; текст страницы объясняет именно скрепку, а вопрос требует именно её выбора. Разрыва между текстом и вопросом нет — ключ страницы это `am`, а не `ready`.
- **Педагог, PASS** — замечаний к правке не было, границы и структура сохранены: 17 заданий, механики из списка, поддержка убывает.

---

🟡 **Стоит улучшить** — из сессии выпало слово `brave`: его придётся вернуть в одну из следующих сессий урока, иначе оно останется без второго касания в курсе.

🧩 **Скрытые связи** — изменился список `new_words` для сборщика: `kind` теперь вводится как новое слово в этой сессии. Если в плане курса `kind` числится как «уже пройденное», строку плана надо поправить, чтобы карточка не задвоилась позже.

- Починил сессию по замечаниям судей.
- Главное: убрал неправду про слово «высокий» — раньше было написано, что оно только про людей, а на самом деле так же говорят про дерево и дом.
- Убрал из карточки «молодой» приписку про силу: молодой — это про возраст, а не про мышцы.
- Выбросил два слова, которые ученику подсовывали на угадайку без объяснения и без звука. Одно из них теперь введено честно — с карточкой и прослушиванием, второе перенесено на будущее.
- Убрал из текста украшательства и объявление шутки вместо самой шутки.
- Третья страница вступления раньше просто пересказывала первую — теперь на ней своя мысль.
- Заменил ловушку со словом, которого в сессии не было, на слово, уже разобранное ранее.
- Заданий по-прежнему семнадцать, сложность так же нарастает от карточек к разговору вслух.