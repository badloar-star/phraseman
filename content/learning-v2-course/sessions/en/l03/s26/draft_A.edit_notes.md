## Изменения

1. **Интро 1 переписано целиком** (BLOCK судьи бреда: «дотянуться» — ложный критерий). Критерий теперь не досягаемость, а «своё / в стороне»: `this` — то, что у вас, `that` — то, что в стороне, включая вещь у собеседника. Заголовок «Не дотянуться — значит that» заменён на «Своё — this, чужое — that». Живой ход про кривую картину ушёл в карточку 1 — в абзаце его приклеивать было нельзя.
2. **Заголовок интро 2 «Одна вещь — одно that» → «Для нескольких — другое слово»** (BLOCK: `that` не связано с единственным предметом — `That is a good idea`). Сам абзац был верен, оставлен, только добавлено, что про один предмет — `that is a screen`.
3. **«Три шага, и ни один не переставить» → «Пока порядок такой»** (BLOCK: противоречило `Is that a speaker?` на следующей странице).
4. **Убрано выдуманное обоснование `it`** («показывать второй раз незачем» — придуманная связь, подтверждена вторым прогоном). На её месте факт: в коротком ответе `it` и после `this`, и после `that`. То же в разборе задания 13.
5. **Три звуковых разбора по буквам заменены** (BLOCK: букву на слух не слышно, а «в `picture` слышно „к“» — неправда, там «ч»). `pitcher`, `green`, `sneaker` разбираются по смыслу; `pictures`/`speakers` — по добавленному звуку на конце, а не по букве; `scream` — по конечному звуку («н» против «м»).
6. **`clock` получил перевод ДО доски пар** (REVISE ученика, вкуса, педагога): голосовое задание перенесено с 16-го места на 9-е, доска осталась 12-м. Закрывает и жалобу «соединял пару вслепую».
7. **Задание 15 (`That is a box.`) заменено** на пропуск `The box is behind the lamp` — ушла третья сборка со скелетом `That is a ___` (порог §11), а `lamp` и `box` получили второе касание без filler.
8. **Добавлено задание 16** `Where is the box?` — сборка на материале урока 3, пятая сборка сохранена, скелет новый.
9. **Карточки 3 и 6 переписаны** (REVISE вкуса: словарные определения). Вместо «Плоская светящаяся поверхность» — отпечатки пальцев на экране; вместо сухой «коробки со звуком» — сосед за стеной. Юмор растёт из самого предмета, а не приклеен к абзацу.
10. **Задание 9 (`sound_contrast` про `screen`/`green`) убрано**: его разбор держался на букве, а честного звукового признака у пары нет («скр» против «гр» — два звука, ученик потеряет «р»). Различение `screen` осталось в задании 4 по смыслу. Число заданий сохранено — 17.
11. **`speaker` в интро 3 подан без потери:** «та, из которой идёт музыка» оставлено, фраза про порядок не дублируется.

Не исправлено: ничего из `must_fix` не отклонено.

---

🟡 **Стоит улучшить**
- `clock` по-прежнему единственное слово доски пар, которое пришло из прошлых сессий — но теперь оно переведено и произнесено вслух до доски, так что угадывать не нужно.
- Юмор в сессии живёт в трёх карточках слов, а не в абзацах интро. Это сознательно: дописывать шутку в готовый абзац запрещено (§10), а переписывать верные объяснения ради шутки — риск сломать полученные PASS.

🧩 **Скрытые связи**
- Задания 15 и 16 опираются на `behind` и `Where is` из сессий 19–20 урока 3 — материал пройденный, новой грамматики не вводят.
- Убранный `sound_contrast` менял состав механик в строке `modes` — строка обновлена, иначе сборщик развалил бы сессию.

**Простой итог:**
- Переписал первое объяснение: раньше сессия учила неправде — будто слово «that» выбирают, когда до предмета не дотянуться. На самом деле дело не в расстоянии руки, а в том, чьё это и где стоит. Теперь так и написано.
- Убрал ещё четыре неверных утверждения: заголовок, который приписывал слову несуществующее правило; фразу «порядок нельзя менять», которая спорила со следующей страницей; выдуманное объяснение, почему в ответе другое слово; и три разбора произношения, где вместо звуков назывались буквы — на слух букву не услышать.
- Старое слово «часы» раньше появлялось в игре на пары раньше, чем ученик видел перевод. Поменял порядок: сначала перевод и проговаривание, потом игра.
- Одна и та же конструкция собиралась три раза подряд. Заменил одно задание на другое — теперь ученик строит пять разных фраз, а не три одинаковых.
- Двум старым словам добавил второе задание, как положено, — без пустых заданий-затычек.
- Оживил три карточки новых слов: вместо словарных определений — короткая узнаваемая деталь про сам предмет.
- Всего заданий осталось 17, ни одного не выброшено просто так.