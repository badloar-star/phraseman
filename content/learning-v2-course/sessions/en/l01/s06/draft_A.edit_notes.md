## Изменения

**BLOCK (педагог): `here` ни разу не правильный ответ.**
- Задание 12 переписано целиком: было третье подряд `I am ___` с выбором слова (`tired`/`free`/`ok`), стало `scripted_repeat_compare` — друг спрашивает, где вас искать завтра, ученик говорит вслух **I am here**. Теперь `here` — цель, а не фон: 2 касания, из них 1 правильным ответом. Заодно ушёл третий однотипный «вставь слово» подряд (замечание редактора вкуса) и `free` перестал быть шестикратным.

**must_fix редактора вкуса:**
- **Интро 3 отдано одному слову.** Заголовок, объяснение и вопрос теперь про `done`; `ok` со страницы убран — он остаётся героем задания 9, где и так правильный ответ. Заодно исчез дубль «закончил дело» дважды (замечание ученика про воду). Дистрактор `I am ok` заменён на `I am here` — тоже реалистичный сбой «ответил не на тот вопрос», но без объяснения слова, которого на странице больше нет.
- **Задание 14: разбор ловушки появился.** Курсивная ремарка для сборщика заменена настоящим разбором в голосе курса: «Вы бы отказались от прогулки, которой ждёте. Плитку `not` подложили нарочно».
- **Звучание выровнено во всех четырёх карточках.** `home` — «хоум», `back` — «бэк», `ok` — «оу-кей»; у `done` оставлено «дан». Строки `new_words` синхронизированы.
- **Однотипность 9/12 снята** — см. выше, механика 12 сменилась.

**Мёртвые места (ученик):**
- Карточки 6 и 7 были голыми определениями — добавлены живые детали: у `done` — «дела кончились, руки свободны», у `ok` — звучание.

**Не тронуто намеренно:**
- Все три `best_lines` («сняли обувь», «три процента батареи», «`I am ok` — не „прекрасно“…») сохранены. Третья живёт в карточке 7 — она и была её домом в исходнике.
- Дубль обратной связи «интро 2 / задание 10» (педагог) оставлен: в интро 2 разбирается пропажа `am`, в задании 10 — подмена `am` на чужую скрепку. Это разные ошибки, формулировки не совпадают.

---

🟡 **Стоит улучшить** — `free` теперь имеет 5 касаний против 2 у `here`; если появится правило «равномерность касаний», доску 16 или задание 14 придётся балансировать.

🧩 **Скрытые связи** — задание 12 сменило механику, поэтому строка `modes` пересобрана (`context_gap_grammar` 3→2, `scripted_repeat_compare` 1→2); сборщику теперь нужны две озвучки-эталона вместо одной.

- Починил главную блокирующую претензию: слово «здесь» раньше просто мелькало в списке пар, а теперь ученик сам произносит с ним целую фразу.
- Третья страница вступления перестала тянуть два слова сразу — теперь она полностью про «я закончил», и повтор одной мысли дважды убран.
- У ловушки с лишним «не» появилось нормальное объяснение, как и у всех остальных ошибок.
- Все четыре карточки новых слов теперь одинаково подсказывают, как слово звучит вслух.
- Три однотипных задания подряд разбавлены — одно стало заданием на речь вслух.
- Все удачные шутки и образы из исходной версии сохранены без изменений.