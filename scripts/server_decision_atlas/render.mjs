const CSS = `
:root{color-scheme:dark;--bg:#020617;--panel:#101d33;--panel2:#0b1729;--line:#304966;--text:#f8fafc;--muted:#adbdd2;--lime:#a3e635;--cyan:#67e8f9;--amber:#fbbf24;--red:#fb7185}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 80% -10%,#193963 0,transparent 36%),var(--bg);color:var(--text);font:16px/1.5 system-ui,sans-serif}main{width:min(1420px,calc(100% - 28px));margin:auto;padding:32px 0 70px}.eyebrow{color:#d8f2a9;font:700 12px ui-monospace,monospace;letter-spacing:.08em;text-transform:uppercase}h1{margin:13px 0 7px;font:700 clamp(28px,4vw,48px)/1.1 ui-monospace,monospace;letter-spacing:-.055em}.lead{max-width:960px;margin:0;color:var(--muted);font-size:18px}.box{border:1px solid var(--line);border-radius:16px;background:linear-gradient(145deg,#13223b,#0b1628)}.metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:11px;margin:24px 0}.metric{padding:15px}.metric b{display:block;margin-top:6px;font:700 24px ui-monospace,monospace}.metric span{color:var(--muted);font-size:13px}.toolbar{display:grid;grid-template-columns:1fr auto auto auto;gap:9px;padding:12px;margin-bottom:14px}.toolbar input,.toolbar select,.toolbar button,.choice,.more{min-height:44px;border:1px solid #496586;border-radius:10px;background:#0a1628;color:var(--text);padding:10px 12px;font:inherit}.toolbar input:focus-visible,.toolbar select:focus-visible,.toolbar button:focus-visible,.choice:focus-visible,.more:focus-visible,.glossary summary:focus-visible{outline:3px solid var(--cyan);outline-offset:2px}.toolbar button,.choice,.more{cursor:pointer}.toolbar button:hover,.choice:hover,.more:hover{border-color:#81a4cc}.layout{display:grid;grid-template-columns:minmax(0,1fr) 310px;gap:14px}.records{display:grid;gap:12px}.record{padding:18px}.record-top{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.record h2{margin:0;font:700 18px/1.25 ui-monospace,monospace}.record p{margin:7px 0;color:var(--muted)}.badge{display:inline-block;margin:2px 4px 2px 0;padding:4px 7px;border:1px solid #4d6a8d;border-radius:999px;color:#d1e3fb;font:700 10px ui-monospace,monospace}.badge.ok{border-color:#6f963b;color:#e1f7bb}.badge.warn{border-color:#a77c30;color:#ffe4a6}.flow{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin:15px 0}.step{padding:10px;border:1px solid #36516f;border-radius:10px;background:#0c192c}.step small{display:block;color:#9ab6d6;font:700 10px ui-monospace,monospace}.step b{display:block;margin-top:5px;font-size:13px}.step span{display:block;margin-top:3px;color:var(--muted);font-size:12px}.details{margin-top:10px}.details summary,.glossary summary{cursor:pointer;color:#d9ecff;font-weight:700}.facts{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin:12px 0}.fact{padding:10px;border-radius:9px;background:#0b1729;border:1px solid #29415e}.fact b{display:block;font-size:12px}.fact span{display:block;margin-top:4px;color:var(--muted);font-size:12px}.markers{display:flex;flex-wrap:wrap;gap:6px;margin-top:8px}.marker{padding:6px 8px;border:1px solid #4a6689;border-radius:8px;color:#d5e7fa;font-size:12px}.marker.unknown{border-color:#7a653b;color:#f5dca1}.choices{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px;margin-top:13px}.choice-group{padding:11px;border:1px solid #36506f;border-radius:10px;background:#0c192c}.choice-group h3{margin:0;font-size:13px}.choice-group p{font-size:12px}.choice-list{display:flex;flex-wrap:wrap;gap:6px}.choice{min-height:38px;padding:7px 8px;font-size:12px}.choice.selected{background:#294619;border-color:var(--lime);color:#ecffd1}.choice:disabled{cursor:not-allowed;border-color:#765949;background:#2a211d;color:#f0c9ac}.source{margin-top:10px;color:#aebed2;font:11px ui-monospace,monospace}.side{height:max-content;padding:16px;position:sticky;top:12px}.side h2{margin:0;font:700 17px ui-monospace,monospace}.side p{color:var(--muted);font-size:13px}.decision{padding:10px;margin:8px 0;border-left:3px solid var(--lime);background:#14251a;font-size:13px}.empty{padding:12px;border:1px dashed #4b6383;border-radius:10px;color:var(--muted);font-size:13px}.side button{width:100%;margin-top:8px;min-height:42px;border:1px solid #4a6689;border-radius:9px;background:#11213a;color:var(--text);cursor:pointer}.side button:hover{border-color:#83a4cd}.glossary{margin-top:14px;padding:13px}.glossary p{font-size:13px}.glossary dt{margin-top:10px;color:#e8f7d0;font-weight:700}.glossary dd{margin:3px 0 0;color:var(--muted);font-size:13px}.status{min-height:24px;margin:0 0 12px;color:#d7edff;font-size:13px}.more{width:100%;font-weight:700}@media(max-width:980px){.layout{grid-template-columns:1fr}.side{position:static}.metrics{grid-template-columns:repeat(2,1fr)}}@media(max-width:670px){main{width:min(100% - 20px,1420px)}.toolbar{grid-template-columns:1fr 1fr}.toolbar input{grid-column:1/-1}.flow,.facts,.choices{grid-template-columns:1fr}.record-top{display:block}.metrics{grid-template-columns:1fr}}@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important}}`;

function publicBundle(bundle) {
  return {
    ...bundle,
    records: bundle.records.map(({ snippet, ...record }) => record),
  };
}

export function renderServerDecisionAtlas(bundle) {
  const data = JSON.stringify(publicBundle(bundle)).replaceAll('<', '\\u003c');
  const app = String.raw`
const ATLAS=${data};
const STORAGE_KEY='phraseman.server-decision-atlas.v1';
const state={query:'',category:'Все',selectedOnly:false,limit:40,decisions:JSON.parse(localStorage.getItem(STORAGE_KEY)||'{}')};
const esc=(value)=>String(value??'').replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const percentage=(value,total)=>total?Math.round(value*100/total):0;
const categories=['Все',...new Set(ATLAS.records.map((r)=>r.category).sort((a,b)=>a.localeCompare(b)))];
const markerNames={optimistic:'показ сразу',offline:'работа без сети',queue:'очередь',retry:'повтор',idempotency:'защита от дубля',receipt:'квитанция'};
const authorityNames={client:'приложение',server:'сервер',shared:'приложение и сервер',external:'внешний сервис','not-established':'нужно проверить'};
const routeNames={callable:'серверная функция',http:'веб-маршрут',schedule:'по расписанию','firestore-trigger':'реакция на данные','client-callable':'вызов сервера из приложения','client-firestore':'связь с хранилищем','client-http':'прямой интернет-запрос'};
const marker=(item)=>'<span class="marker '+(item.state==='evidence-found'?'':'unknown')+'"><b>'+esc(markerNames[item.name]||item.name)+'</b>: '+esc(item.state==='evidence-found'?'найдена явная отметка в коде':'по коду автоматически не подтверждено')+'</span>';
function save(){localStorage.setItem(STORAGE_KEY,JSON.stringify(state.decisions));}
function filtered(){const needle=state.query.trim().toLocaleLowerCase('ru');return ATLAS.records.filter((r)=>{const found=!needle||[r.title,r.name,r.category,r.plainWhat,r.sourcePath,...(r.searchAliases||[])].join(' ').toLocaleLowerCase('ru').includes(needle);return found&&(state.category==='Все'||r.category===state.category)&&(!state.selectedOnly||Object.values(state.decisions).some((d)=>d.recordId===r.id));});}
function decisionKey(id,key){return id+'::'+key;}
function recordCard(r){const selected=(choice)=>state.decisions[decisionKey(r.id,choice.key)];const choices=r.choices.map((choice)=>'<section class="choice-group"><h3>'+esc(choice.label)+'</h3><p>Сейчас: <b>'+esc(choice.baseline)+'</b>. '+esc(choice.consequence)+'</p><div class="choice-list">'+choice.options.map((option)=>{const active=selected(choice)?.value===option;const locked=choice.locked&&option!==choice.baseline;return '<button class="choice '+(active?'selected':'')+'" '+(locked?'disabled title="'+esc(r.safety.lockedReason)+'"':'data-choice="1" data-record="'+esc(r.id)+'" data-key="'+esc(choice.key)+'" data-value="'+esc(option)+'"')+'>'+esc(option)+(locked?' · заблокировано':'')+'</button>';}).join('')+'</div></section>').join('');const evidence=r.evidence.slice(0,4).map((e)=>esc(e.label)+' — '+esc(e.sourcePath)+':'+e.line).join('<br>');return '<article class="box record"><div class="record-top"><div><h2>'+esc(r.title)+'</h2><p>'+esc(r.plainWhat)+'</p><p class="source">Техническое имя: '+esc(r.name)+'</p></div><div><span class="badge">'+esc(routeNames[r.routeType]||r.routeType)+'</span><span class="badge '+(r.authority.kind==='not-established'?'warn':'ok')+'">итог решает: '+esc(authorityNames[r.authority.kind]||r.authority.kind)+'</span></div></div><div class="flow"><div class="step"><small>1 · СТАРТ</small><b>'+esc(r.routeType==='schedule'?'По расписанию':r.routeType==='firestore-trigger'?'После изменения данных':'Действие или запрос')+'</b><span>'+esc(r.sourcePath)+'</span></div><div class="step"><small>2 · ЭКРАН</small><b>'+esc(r.markers.find((m)=>m.name==='optimistic')?.state==='evidence-found'?'Есть отметка «показать сразу»':'Нужно проверить вручную')+'</b><span>Автоматический разбор честно показывает сомнения.</span></div><div class="step"><small>3 · РЕШЕНИЕ</small><b>'+esc(authorityNames[r.authority.kind]||r.authority.kind)+'</b><span>'+esc(r.authority.explanation)+'</span></div><div class="step"><small>4 · ДАЛЬШЕ</small><b>'+esc(r.routeType==='schedule'||r.routeType==='firestore-trigger'?'Фоновая работа':'Ответ или синхронизация')+'</b><span>Повторы и очередь показаны ниже.</span></div></div><details class="details"><summary>Показать все детали простыми словами</summary><div class="facts"><div class="fact"><b>Кто решает итог?</b><span>'+esc(r.authority.explanation)+'</span></div><div class="fact"><b>Перезапись и конфликт</b><span>Автоматический разбор не доказывает полный порядок перезаписи. Смотрите исходники и пометку «нужно проверить вручную».</span></div><div class="fact"><b>Синхронизация и работа без сети</b><span>'+esc(['client-firestore','client-callable','client-http'].includes(r.routeType)?'Это связь из приложения. Метки ниже показывают, найдены ли слова про работу без сети и очередь.':'Это серверная сторона. Клиентский маршрут показан отдельной карточкой, если он найден.')+'</span></div><div class="fact"><b>Безопасный повтор</b><span>'+esc(r.markers.find((m)=>m.name==='idempotency')?.state==='evidence-found'?'Найдено явное упоминание защиты от повтора.':'Защита от повтора не подтверждена этим автоматическим разбором.')+'</span></div></div><div class="markers">'+r.markerExplanations.map(marker).join('')+'</div><p class="source"><b>Откуда взята информация:</b><br>'+evidence+'</p>'+(r.safety.lockedReason?'<p class="source"><b>Почему часть вариантов заблокирована:</b> '+esc(r.safety.lockedReason)+'</p>':'')+'<section class="choices">'+choices+'</section></details></article>';}
function bindChoices(){document.querySelectorAll('[data-choice]').forEach((button)=>button.addEventListener('click',()=>{const record=ATLAS.records.find((r)=>r.id===button.dataset.record);const choice=record.choices.find((c)=>c.key===button.dataset.key);state.decisions[decisionKey(record.id,choice.key)]={recordId:record.id,title:record.title,label:choice.label,baseline:choice.baseline,value:button.dataset.value,sourcePaths:[record.sourcePath]};save();render();}));}
function renderRecords(){const rows=filtered();const visible=rows.slice(0,state.limit);document.getElementById('atlas-status').textContent='Показано '+visible.length+' из '+rows.length+' совпадений. Всего связей: '+ATLAS.records.length+'. Выборы сохраняются только в этом браузере.';document.getElementById('atlas-records').innerHTML=(visible.length?visible.map(recordCard).join(''):'<div class="box record">Ничего не найдено. Попробуйте другой запрос или фильтр.</div>')+(rows.length>visible.length?'<button id="atlas-more" class="more">Показать ещё '+Math.min(40,rows.length-visible.length)+'</button>':'<button id="atlas-more" class="more" hidden>Все совпадения уже показаны</button>');document.getElementById('atlas-more').addEventListener('click',()=>{state.limit+=40;renderRecords();});bindChoices();}
function render(){const decisions=Object.values(state.decisions);const app=document.getElementById('atlas-app');app.innerHTML='<div class="eyebrow">Phraseman · локальный интерактивный аудит</div><h1>Карта серверных решений</h1><p class="lead">Это не меняет приложение. Здесь видно, что автоматический разбор нашёл в коде, где есть связанный файл проверки и какие решения можно подготовить к отдельной реализации. Связанный тест — это подсказка для аудита, а не заявление, что функция уже проверена и работает.</p><section class="metrics"><div class="box metric"><span>Все найденные связи</span><b>'+ATLAS.metrics.totalRecords+'</b><span>каждая с исходной строкой</span></div><div class="box metric"><span>Серверные входы</span><b>'+ATLAS.metrics.serverExports+'</b><span>функции, HTTP, триггеры, расписание</span></div><div class="box metric"><span>Связанный файл проверки</span><b>'+percentage(ATLAS.metrics.withTestEvidence+ATLAS.metrics.withContractEvidence,ATLAS.metrics.totalRecords)+'%</b><span>'+ATLAS.metrics.withTestEvidence+' обычных · '+ATLAS.metrics.withContractEvidence+' guard/contract</span></div><div class="box metric"><span>Найдены явные метки</span><b>'+percentage(ATLAS.metrics.staticBehaviorEstablished,ATLAS.metrics.totalRecords)+'%</b><span>остальное честно помечено для ручной проверки</span></div></section><p id="atlas-status" class="status" aria-live="polite"></p><section class="box toolbar"><input id="atlas-search" type="search" placeholder="Найти: руны, XP, покупка, voice, admin…" value="'+esc(state.query)+'"><select id="atlas-category" aria-label="Категория">'+categories.map((c)=>'<option '+(c===state.category?'selected':'')+'>'+esc(c)+'</option>').join('')+'</select><button id="atlas-decisions" aria-pressed="'+state.selectedOnly+'">Только мои решения ('+decisions.length+')</button><button id="atlas-glossary">Словарь простыми словами</button></section><section class="layout"><section id="atlas-records" class="records"></section><aside class="box side"><h2>Мои решения</h2><p>Это черновик. Он не меняет ни приложение, ни сервер.</p>'+(decisions.length?decisions.map((d)=>'<div class="decision"><b>'+esc(d.title)+'</b><br>'+esc(d.label)+': '+esc(d.baseline)+' → <b>'+esc(d.value)+'</b></div>').join(''):'<div class="empty">Пока нет изменений. Откройте карточку, выберите вариант — он появится здесь.</div>')+'<button id="atlas-export">Скопировать мой черновик</button><button id="atlas-reset">Сбросить мои решения</button><details class="glossary" open><summary>Словарь простыми словами</summary><dl>'+ATLAS.glossary.map((item)=>'<dt>'+esc(item.term)+'</dt><dd>'+esc(item.simpleMeaning)+'</dd>').join('')+'</dl></details></aside></section>';bind();renderRecords();}
function bind(){document.getElementById('atlas-search').addEventListener('input',(event)=>{state.query=event.target.value;state.limit=40;renderRecords();});document.getElementById('atlas-category').addEventListener('change',(event)=>{state.category=event.target.value;state.limit=40;renderRecords();});document.getElementById('atlas-decisions').addEventListener('click',()=>{state.selectedOnly=!state.selectedOnly;state.limit=40;render();});document.getElementById('atlas-glossary').addEventListener('click',()=>document.querySelector('.glossary').scrollIntoView({behavior:'smooth'}));document.getElementById('atlas-export').addEventListener('click',async()=>{const text=JSON.stringify(Object.values(state.decisions),null,2);try{await navigator.clipboard.writeText(text);document.getElementById('atlas-status').textContent='Черновик скопирован. Его можно прислать мне для отдельного плана изменений.';}catch{document.getElementById('atlas-status').textContent='Копирование недоступно в этом браузере. Черновик остаётся в разделе «Мои решения».';}});document.getElementById('atlas-reset').addEventListener('click',()=>{state.decisions={};save();render();});}
render();`;
  return `<!doctype html><html lang="ru"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Phraseman · Server Decision Atlas</title><style>${CSS}</style></head><body><main id="atlas-app"></main><script>${app}</script></body></html>`;
}
