## Изменения

1. **Интро 3, абзац 2 — BLOCK судьи бреда.** «`Button` — круглая… `Switch` — пластинка» заменено на различие по действию: `button` нажимают и отпускают, `switch` остаётся в положении, в которое его перевели. Признак работает на кнопке любой формы.
2. **Интро 3, вопрос.** «Плоская пластинка, ходит вверх-вниз» заменено на «свет включают и он горит, пока не выключишь» — сцена теперь опирается на тот же честный признак, а не на форму.
3. **Интро 3, разбор `Is that a button?`** — тот же ложный признак убран, оставлено «нажимают и отпускают».
4. **Задание 3, карточка `button`** — убрано «круглая» и самопротиворечие с пуговицей: пуговица подана как отдельное значение слова, а не как пример нажимания.
5. **Задание 4, разбор `butter`** — убрана опора на конечный `r` (в британском его нет). Оба разбора теперь сравнивают одну позицию: что идёт сразу после `t`.
6. **Задание 11 — REVISE педагога.** Было `This is not a remote.` при `key` с нулём верных ответов. Стало `This is a key.`: `key` получает верный ответ и второе касание, скелет остаётся уникальным (утверждение среди вопросов), лишняя плитка `Is` сохраняет обманку на внимательность.
7. **Задание 6, карточка `switch`** — убрано «Пластинка на стене», осталось действие; строка «щёлк вверх — свет есть, щёлк вниз — нет» из `best_lines` сохранена.
8. **Задание 2, интро 2, задание 15** — убраны «круглое» про кнопку на стене и «пластинка у двери», чтобы не тянуть ложный признак формы. Сцена осталась той же.
9. **Задание 17** — «показывает на пластинку у двери» → «показывает на стену у двери».
10. **Не исправлено по вкусу.** Судья вкуса требует юмористический ход внутри объяснения. Дописывать шутку в готовый абзац запрещено §10 Конституции — именно так ломаются полученные PASS. Живые строки из `best_lines` сохранены все три.

---

🟡 **Стоит улучшить** — интро идут ровным тоном; по решению владельца 07.09 отсутствие шутки дефектом не считается, но при следующем переписывании страницы целиком образ стоит вносить внутрь мысли, а не приклеивать.

🧩 **Скрытые связи** — правка задания 11 меняет строку `key` в самопроверке и подсчёт касаний; строка `modes` не изменилась (тип задания тот же `phrase_builder`), сторож однообразных сборок по-прежнему видит четыре разных скелета.

---

- Главное: убрал неправду про «круглую кнопку» и «пластинку-выключатель» — это была выдумка, и она встречалась в трёх местах подряд, включая карточку слова.
- Вместо формы теперь объясняю разницу по действию: кнопку нажал и отпустил, выключатель перевёл — и он там остался. Так работает для кнопки любой формы.
- Поправил карточку «button»: раньше она сама себе противоречила — говорила «круглая, нажимают пальцем» и тут же приводила пуговицу на рубашке.
- Починил объяснение на слух: раньше ученику обещали звук «р» в конце слова butter, которого в британской речи нет. Теперь сравнение идёт по звуку сразу после «т» — он слышен всегда.
- Слово «ключ» раньше висело в сессии только как неправильный вариант. Теперь у него есть задание, где оно — верный ответ.
- Шутку в объяснения дописывать не стал: по правилам это делается только переписыванием страницы целиком, иначе ломается то, что уже одобрено.