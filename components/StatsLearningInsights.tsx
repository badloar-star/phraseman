import React, { useState } from 'react';
import { Modal, Pressable, Text, View } from 'react-native';
import type { Lang } from '../constants/i18n';
import type { StatsLearningInsights as Insights } from '../app/stats_learning_insights';

type Props = { theme: any; fonts: any; lang: Lang; accent: string; insights: Insights; totalStreak: number; bestStreak: number };
const max = (values: number[]) => Math.max(1, ...values);

export default function StatsLearningInsights({ theme: t, fonts: f, accent, insights, totalStreak, bestStreak }: Props) {
  const [open, setOpen] = useState(false);
  const rhythmMax = max(insights.rhythm.map((day) => day.xp));
  const comparisonMax = max([insights.weekComparison.currentXp, insights.weekComparison.previousXp]);
  return <View style={{ gap: 10 }}>
    <View style={{ flexDirection: 'row', gap: 10 }}>
      <View testID="stats-insight-rhythm" style={{ flex: 1, borderRadius: 16, padding: 12, backgroundColor: t.bgSurface2 }}><Text style={{ color: t.textPrimary, fontSize: f.sub, fontWeight: '900' }}>Ритм недели</Text><View style={{ height: 58, flexDirection: 'row', alignItems: 'flex-end', gap: 4, marginTop: 8 }}>{insights.rhythm.map((day) => <View key={day.date} style={{ flex: 1, minWidth: 4, height: `${Math.max(8, Math.round(day.xp / rhythmMax * 100))}%`, borderRadius: 4, backgroundColor: day.active ? accent : t.bgCard }} />)}</View></View>
      <View testID="stats-insight-regularity" style={{ flex: 1, borderRadius: 16, padding: 12, backgroundColor: t.bgSurface2 }}><Text style={{ color: t.textPrimary, fontSize: f.sub, fontWeight: '900' }}>Регулярность</Text><Text style={{ color: accent, fontSize: f.h2, fontWeight: '900', marginTop: 10 }}>{insights.regularity.activeDays}/7</Text><Text style={{ color: t.textMuted, fontSize: f.caption, fontWeight: '700' }}>активных дней</Text></View>
    </View>
    <Pressable testID="stats-insights-open" accessibilityRole="button" accessibilityLabel="Открыть все показатели" onPress={() => setOpen(true)} style={{ minHeight: 48, borderRadius: 14, alignItems: 'center', justifyContent: 'center', backgroundColor: t.bgSurface2 }}><Text style={{ color: accent, fontSize: f.body, fontWeight: '900' }}>Все показатели</Text></Pressable>
    <Modal visible={open} transparent animationType="slide" onRequestClose={() => setOpen(false)}><Pressable style={{ flex: 1, justifyContent: 'flex-end', backgroundColor: 'rgba(0,0,0,0.58)' }} onPress={() => setOpen(false)}><Pressable onPress={(event) => event.stopPropagation()} style={{ gap: 14, borderTopLeftRadius: 24, borderTopRightRadius: 24, padding: 18, backgroundColor: t.bgCard }}><Text style={{ color: t.textPrimary, fontSize: f.h2, fontWeight: '900' }}>Все показатели</Text><View style={{ borderRadius: 16, padding: 12, backgroundColor: t.bgSurface2 }}><Text style={{ color: t.textPrimary, fontSize: f.sub, fontWeight: '900' }}>Скорость обучения</Text><View style={{ height: 48, flexDirection: 'row', alignItems: 'flex-end', gap: 5, marginTop: 8 }}>{insights.speed.map((point) => <View key={point.date} style={{ flex: 1, height: `${Math.max(5, Math.min(100, (point.xpPerMinute ?? 0) * 10))}%`, backgroundColor: accent, borderRadius: 4 }} />)}</View></View><View style={{ flexDirection: 'row', gap: 10 }}><View style={{ flex: 1, borderRadius: 16, padding: 12, backgroundColor: t.bgSurface2 }}><Text style={{ color: t.textPrimary, fontSize: f.sub, fontWeight: '900' }}>Лучшая неделя</Text><Text style={{ color: accent, fontSize: f.h2, fontWeight: '900', marginTop: 6 }}>{insights.bestWeek.xp} XP</Text></View><View style={{ flex: 1, borderRadius: 16, padding: 12, backgroundColor: t.bgSurface2 }}><Text style={{ color: t.textPrimary, fontSize: f.sub, fontWeight: '900' }}>Серия</Text><Text style={{ color: accent, fontSize: f.h2, fontWeight: '900', marginTop: 6 }}>{totalStreak} дн.</Text><Text style={{ color: t.textMuted, fontSize: f.caption }}>рекорд {bestStreak}</Text></View></View><View style={{ borderRadius: 16, padding: 12, backgroundColor: t.bgSurface2 }}><Text style={{ color: t.textPrimary, fontSize: f.sub, fontWeight: '900' }}>Текущая против прошлой</Text><View style={{ height: 54, flexDirection: 'row', alignItems: 'flex-end', gap: 12, marginTop: 8 }}><View style={{ flex: 1, height: `${Math.max(8, insights.weekComparison.previousXp / comparisonMax * 100)}%`, borderRadius: 6, backgroundColor: t.bgCard }} /><View style={{ flex: 1, height: `${Math.max(8, insights.weekComparison.currentXp / comparisonMax * 100)}%`, borderRadius: 6, backgroundColor: accent }} /></View></View><View style={{ borderRadius: 16, padding: 12, backgroundColor: t.bgSurface2 }}><Text style={{ color: t.textPrimary, fontSize: f.sub, fontWeight: '900' }}>Лучшее время</Text><Text style={{ color: t.textMuted, fontSize: f.caption, marginTop: 5 }}>Появится после накопления данных по времени занятий.</Text></View></Pressable></Pressable></Modal>
  </View>;
}
