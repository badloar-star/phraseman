# Аудит «Пульта управления» — что ещё можно добавить (2026-06-21)

Метод: 3 параллельных агента-исследователя + личная перепроверка ключевых находок.
Источник истины настроек: `app/remote_flags.ts` (numbers/bools/texts) ↔ `admin/index.html`
(массивы `RC_NUMBER_FIELDS`, `RC_BOOL_FIELDS`, `CP_FEATURE_TOGGLES`, `COMPASS_TOGGLES`,
`CP_PREMIUM_FEATURES`, `CP_PREMIUM_LIMITS`). Всё пишется в Firestore `remote_config/app`,
клиент читает живьём (onSnapshot) — меняется без релиза.

---

## Часть A. Пробелы среди УЖЕ существующих флагов (есть в коде, забыли вывести в UI)

Дёшево и быстро — ключи уже работают на клиенте, нужен только UI в админке.

| Ключ | Тип | Дефолт | Что делает | Где в коде |
|------|-----|--------|------------|------------|
| `onboarding_ab_welcome_pct` | число | 34 | A/B-сплит первого экрана онбординга (welcome) | remote_flags.ts:31 |
| `onboarding_ab_builder_pct` | число | 33 | A/B-сплит онбординга (builder) | remote_flags.ts:32 |
| `onboarding_ab_quiz_pct` | число | 33 | A/B-сплит онбординга (quiz) | remote_flags.ts:33 |
| `maintenance_uk` | текст | '' | Текст режима обслуживания (укр) — в UI только ru | remote_flags.ts:100 |
| `maintenance_es` | текст | '' | Текст режима обслуживания (исп) — в UI только ru | remote_flags.ts:101 |
| `compass_voice_fallback_ru/uk/es` | текст | '' | Fallback-комментарий дня Компаса (когда ИИ-голос выкл/бюджет) | remote_flags.ts:103-106 |
| `weekly_boons_config` | текст(JSON) | '' | Расписание недельных бонусов + вкл/выкл | remote_flags.ts:114-117 |

> Итого: ~57 из 62 ключей выведены; пробел — 3 числа + 5 текстов.

---

## Часть B. Новые рычаги (захардкожено в коде, нет флага вообще)

### ВЫСОКИЙ приоритет (деньги / удержание / экстренное управление)

1. **Force-update (минимальная версия приложения).** ОТСУТСТВУЕТ полностью (0 совпадений
   по force_update/min_version — перепроверено лично). Нужен, чтобы экстренно заблокировать
   старые сборки с критичным багом и попросить обновиться. Требует флага + экрана-гейта в app.
2. **Промокоды / купоны / акции-скидки.** Системы нет (есть только промо-баннер магазина в
   review-promo и maintenance-баннер). Нужны для маркетинговых кампаний и возврата ушедших.
3. **Стоимость заморозки серии** `FREEZE_COST_SHARDS = 10` — захардкожено в home.tsx:1489.
   Прямой момент монетизации (юзер в риске потерять серию). Хочется крутить из админки.
4. **Дроп коллекционных карточек** — functions/src/collectibles.ts: `FLAT_DROP_CHANCE=0.15`,
   капы в день free/premium (3/4), pity-гарантии. Прямой рычаг экономики и retention.
5. **Реферальные награды** — functions/src/referral.ts: дней премиума за друга (7), капы
   3/день и 30/мес. Управление вирусностью и анти-фрод без релиза.
6. **Осколки за активности** — app/shards_system.ts:52-71 (~15 источников: урок, идеал,
   серия 7/30, арена и т.д.). Главный пул премиум-валюты, частые A/B-крутки баланса.

### СРЕДНИЙ приоритет

7. **Множители серии** `streakMultiplier` (×1.2/1.4/1.6/1.8) — hall_of_fame_utils.ts:38.
8. **Окно бесплатной энергии вечером** 19:00–22:00 — boons/boon_effects_energy.ts:17-18.
9. **Награды «пари на серию» (Wager)** — streak_wager.ts:52-59 (6 тиров: ставка/дни/💎/XP).
10. **Лиговые бусты** (групповой/персональные/сундук) — league_*_boosts.ts, league_chest_rewards.ts.
11. **Длина trial / intro-доступа** (72ч хардкод) — для региональных/сезонных тестов.
12. **A/B-тексты пейвола** (paywall_copy.ts захардкожен) — тест офферов без релиза.
13. **Расширенная сегментация push** (сейчас только all/free/premium).

### НИЗКИЙ приоритет

14. Лимит переводов в диалоге (3) — dialog_translate_limit.ts:17.
15. Награды друзьям/Arena Hill — functions/src/friend_gifts.ts, arena_hill.ts.
16. Награды ежедневных заданий (xp по каждому) — daily_tasks.ts.

---

## ✅ РЕАЛИЗОВАНО (2026-06-21)

Все 4 направления сделаны, закоммичены в master атомарными коммитами:

1. **Забытые ключи в UI** (`df82ecde`): онбординг A/B (3 числа), fallback Компаса
   (ru/uk/es), weekly_boons_config (JSON-редактор с валидацией). + paywall_timers.
2. **Экономика** (`c1b2658f`): заморозка серии (streak_freeze_cost_shards) +
   дроп карточек (шанс/капы/pity/бонус сета — CollectiblesDropConfig). Тесты +5/+2.
3. **Рефералы** (`1cdde1ba`): дни награды + антифрод-капы (день/месяц) — ReferralConfig.
4. **Force-update** (`58078261`): флаг + min_app_version + ссылки, ForceUpdateGate,
   semver-сравнение. Тесты +10.
5. **Промокоды + промо-баннер** (`0818b09f`): callable promoCodeRedeem/Upsert
   (коды→дни премиума), экран ввода, firestore.rules; промо-баннер (флаг+тексты+срок).
   ВЫКЛЮЧЕНО по умолчанию — коды/акции пользователь создаёт позже (от сторов не зависит).

Бэкенд читает remote_config/app.numbers тем же резолвером, что арена
(resolveCollectiblesDropConfig/resolveReferralConfig). Чистые функции вынесены и покрыты тестами.

НУЖНО: deploy functions (promoCodeRedeem, promoCodeUpsert, collectiblesClaimDrop,
referral*) + deploy firestore.rules + билд приложения (force-update gate, промо-баннер,
экран промокода, заморозка серии — это клиентский код). Эффект самих тумблеров/чисел
для уже выпущенных частей — живьём без релиза.

## Рекомендация

- **Сделать сразу (дёшево, без правок клиента):** Часть A — вывести 8 забытых ключей в UI.
- **Топ нового функционала:** force-update (операционная страховка) + заморозка серии +
  дроп карточек + реферальные награды (всё это прямые рычаги денег/удержания).
- Каждый новый числовой рычаг = ключ в remote_flags.ts (с границами) + замена хардкода на
  геттер + поле в admin/index.html. Force-update и промокоды требуют ещё кода в приложении.
