import { COMMUNITY_OPERATION_CAPABILITIES } from './admin-community-operations-state.js';

const LABELS = Object.freeze({
  'mod-queue': 'Очередь модерации', 'help-board': 'Доска помощи', 'helpers-board': 'Топ помощников', clubs: 'Клубы и лиги',
  'league-chat': 'Чат лиг', 'arena-ranks': 'Рейтинг Арены', 'arena-live': 'Арена live', 'arena-bets': 'Ставки Арены', 'arena-rooms': 'Комнаты Арены',
});
function json(value) { try { return JSON.stringify(value, null, 2); } catch { return '{}'; } }
function version(value) { return String(value || 'missing'); }
function targetFields(defaultId = '') { return `<div class="report-filters"><div class="field"><label for="community-target-id">ID записи</label><input id="community-target-id" value="${defaultId}" autocomplete="off"></div><div class="field"><label for="community-expected-version">Текущая версия</label><input id="community-expected-version" placeholder="missing — только для новой записи" autocomplete="off"></div></div>`; }
function reasonField() { return `<div class="field"><label for="community-reason">Причина изменения</label><textarea id="community-reason" required placeholder="Что меняется и зачем"></textarea></div>`; }
function button(operation, label, permission, can, tone = 'primary') { return `<button class="button ${tone}" data-community-action="preview" data-operation="${operation}" type="button" title="Подготовить операцию: ${label}"${can(permission) ? '' : ' disabled'}>${label}</button>`; }

function editor(capabilityId, can) {
  if (capabilityId === 'mod-queue') return `${targetFields()}<div class="field"><label for="community-status">Решение</label><select id="community-status"><option value="approve">Одобрить и опубликовать</option><option value="request_changes">Запросить изменения</option><option value="reject">Отклонить</option></select></div><div class="field"><label for="community-resolution">Сообщение автору</label><textarea id="community-resolution"></textarea></div>${reasonField()}<div class="actions">${button('mod-queue-status', 'Проверить решение', 'community.moderate', can)}</div><p class="muted">Одобрение публикует канонический community-пак и уведомляет автора. Глобальные блокировки выполняются только в Safety & Moderation.</p>`;
  if (capabilityId === 'help-board') return `${targetFields()}<div class="report-filters"><div class="field"><label for="community-help-content-status">Статус темы или комментария</label><select id="community-help-content-status"><option value="visible">Видим</option><option value="hidden">Скрыт</option><option value="deleted">Удалён</option></select></div><div class="field"><label for="community-help-queue-decision">Решение по очереди проверки</label><select id="community-help-queue-decision"><option value="approved">Одобрить</option><option value="rejected">Отклонить</option></select></div><div class="field"><label for="community-help-restriction">Ограничение создания тем</label><select id="community-help-restriction"><option value="restrict">Ограничить</option><option value="clear">Снять ограничение</option></select></div></div><div class="field"><label for="community-resolution">Решение репорта / пояснение</label><textarea id="community-resolution"></textarea></div><div class="report-filters"><div class="field"><label for="community-target-lang">Изучаемый язык темы</label><input id="community-target-lang" value="en"></div><div class="field"><label for="community-ui-lang">Язык ответа</label><input id="community-ui-lang" value="ru"></div><div class="field"><label for="community-post-as-name">Публичное имя администратора</label><input id="community-post-as-name" value="Phraseman Support"></div></div><div class="report-filters"><div class="field"><label for="community-topic-id">ID темы для комментария</label><input id="community-topic-id" autocomplete="off"></div><div class="field"><label for="community-reply-to-comment-id">ID комментария для ответа (необязательно)</label><input id="community-reply-to-comment-id" autocomplete="off"></div></div><div class="field"><label for="community-title">Заголовок официальной темы</label><input id="community-title"></div><div class="field"><label for="community-body">Текст официальной темы, комментария или ответа</label><textarea id="community-body"></textarea></div><label><input id="community-compass-enabled" type="checkbox"> Запросить ответ Компаса для новой темы</label>${reasonField()}<div class="actions">${button('help-topic-status', 'Статус темы', 'community.help.write', can)}${button('help-comment-status', 'Статус комментария', 'community.help.write', can)}${button('help-report-resolve', 'Закрыть репорт', 'community.help.write', can)}${button('help-queue-status', 'Решение по очереди', 'community.help.write', can)}${button('help-restriction', 'Ограничение Help Board', 'community.help.write', can, 'danger')}${button('help-admin-post', 'Опубликовать официальную тему', 'community.help.write', can)}${button('help-admin-comment', 'Опубликовать комментарий / ответ', 'community.help.write', can)}</div><p class="muted">Официальный комментарий создаётся как новая запись; тема и необязательная цитата берутся из полей выше или прямо из строки таблицы.</p>`;
  if (capabilityId === 'helpers-board') return `${targetFields('app')}<div class="field"><label for="community-description">Публичное описание доски помощников</label><textarea id="community-description"></textarea></div>${reasonField()}${button('helpers-description', 'Проверить описание', 'community.help.write', can)}`;
  if (capabilityId === 'clubs') return `<div class="notice">Раздел только для наблюдения: группы, сундуки и короны объединены в одной ограниченной выборке. Изменяющих кнопок здесь нет.</div>`;
  if (capabilityId === 'league-chat') return `${targetFields()}<div class="report-filters"><div class="field"><label for="community-chat-decision">Решение по очереди</label><select id="community-chat-decision"><option value="approved">Одобрить и опубликовать</option><option value="rejected">Отклонить</option></select></div><div class="field"><label for="community-chat-message-status">Статус опубликованного сообщения</label><select id="community-chat-message-status"><option value="deleted">Удалить</option><option value="visible">Восстановить</option></select></div><div class="field"><label for="community-chat-restriction">Ограничение пользователя</label><select id="community-chat-restriction"><option value="mute">Mute</option><option value="ban">Chat-ban</option><option value="clear">Снять ограничение</option></select></div><div class="field"><label for="community-duration-hours">Mute, часов</label><input id="community-duration-hours" type="number" min="1" max="168" value="24"></div></div><div class="field"><label for="community-resolution">Решение репорта / пояснение</label><textarea id="community-resolution"></textarea></div><div class="report-filters"><div class="field"><label for="community-room-id">ID комнаты лиги</label><input id="community-room-id"></div><div class="field"><label for="community-week-id">ID недели</label><input id="community-week-id"></div><div class="field"><label for="community-league-id">Номер лиги</label><input id="community-league-id" type="number" min="1"></div><div class="field"><label for="community-post-as-name">Публичное имя администратора</label><input id="community-post-as-name" value="Phraseman Support"></div></div><div class="field"><label for="community-body">Сообщение администратора</label><textarea id="community-body"></textarea></div>${reasonField()}<div class="actions">${button('league-chat-status', 'Решение по очереди', 'community.chat.write', can)}${button('league-chat-report', 'Закрыть репорт', 'community.chat.write', can)}${button('league-chat-message-status', 'Удалить / восстановить сообщение', 'community.chat.write', can, 'danger')}${button('league-chat-restriction', 'Mute / chat-ban / снять', 'community.chat.write', can, 'danger')}${button('league-chat-admin-message', 'Сообщение администратора', 'community.chat.write', can)}</div><p class="muted">Ограничения используют канонические состояния muted, banned и cleared. Глобальные баны остаются в Safety & Moderation.</p>`;
  if (capabilityId === 'arena-ranks') return `${targetFields()}${reasonField()}<div class="actions">${button('arena-profile-resync', 'Синхронизировать имя профиля', 'community.arena.write', can)}${button('arena-placeholder-cleanup', 'Построить точный manifest очистки', 'community.arena.destructive', can, 'danger')}</div><div class="notice warning">Очистка включает только arena_profiles с placeholder-именем и нулём матчей. users и Auth никогда не входят в manifest.</div>`;
  if (capabilityId === 'arena-live') return `${targetFields()}<div class="field"><label for="community-resolution">Причина принудительного завершения зависшей сессии</label><textarea id="community-resolution"></textarea></div>${reasonField()}${button('arena-session-finish', 'Проверить завершение сессии', 'community.arena.write', can, 'danger')}<p class="muted">Обычное чтение очереди, сессий и комнат ничего не завершает.</p>`;
  if (capabilityId === 'arena-bets') return `${targetFields('arena_feature_flags')}<label><input id="community-active" type="checkbox"> Рейтинговые ставки включены</label>${reasonField()}${button('arena-wager-flag', 'Проверить флаг экономики', 'community.arena.economy.write', can, 'danger')}`;
  return `${targetFields()}${reasonField()}<div class="actions">${button('arena-room-close', 'Закрыть комнату и участников', 'community.arena.write', can, 'danger')}${button('arena-room-delete', 'Удалить устаревшую комнату', 'community.arena.destructive', can, 'danger')}</div>`;
}

function rowAction(item, capabilityId, can, escapeHtml) {
  const attrs = `data-id="${escapeHtml(item.id)}" data-version="${escapeHtml(version(item.version))}"`;
  if (capabilityId === 'mod-queue' && item.source === 'community_pack_submissions') return `<button class="button" data-community-action="preview" data-operation="mod-queue-status" ${attrs} type="button" title="Подготовить решение по заявке"${can('community.moderate') ? '' : ' disabled'}>Решение</button>`;
  if (capabilityId === 'mod-queue' && item.source === 'help_board_reports') return `<button class="button" data-community-action="preview" data-operation="help-report-resolve" ${attrs} type="button" title="Подготовить закрытие репорта Help Board"${can('community.help.write') ? '' : ' disabled'}>Закрыть репорт</button>`;
  if (capabilityId === 'mod-queue' && item.source === 'league_chat_moderation_queue') return `<button class="button" data-community-action="preview" data-operation="league-chat-status" ${attrs} type="button" title="Подготовить решение по очереди чата"${can('community.chat.write') ? '' : ' disabled'}>Решение</button>`;
  if (capabilityId === 'help-board' && item.source === 'help_board_topics') return `<button class="button" data-community-action="preview" data-operation="help-topic-status" ${attrs} type="button" title="Подготовить изменение статуса темы"${can('community.help.write') ? '' : ' disabled'}>Статус</button> <button class="button" data-community-action="preview" data-operation="help-admin-comment" data-topic-id="${escapeHtml(item.id)}" type="button" title="Опубликовать официальный комментарий в теме"${can('community.help.write') ? '' : ' disabled'}>Комментарий</button>`;
  if (capabilityId === 'help-board' && item.source === 'help_board_comments') return `<button class="button" data-community-action="preview" data-operation="help-comment-status" ${attrs} type="button" title="Подготовить изменение статуса комментария"${can('community.help.write') ? '' : ' disabled'}>Статус</button> <button class="button" data-community-action="preview" data-operation="help-admin-comment" data-topic-id="${escapeHtml(item.topicId || '')}" data-reply-to="${escapeHtml(item.id)}" type="button" title="Ответить на комментарий от имени администратора"${can('community.help.write') ? '' : ' disabled'}>Ответить</button>`;
  if (capabilityId === 'help-board' && item.source === 'help_board_reports') return `<button class="button" data-community-action="preview" data-operation="help-report-resolve" ${attrs} type="button" title="Подготовить закрытие репорта"${can('community.help.write') ? '' : ' disabled'}>Закрыть</button>`;
  if (capabilityId === 'help-board' && item.source === 'help_board_moderation_queue') return `<button class="button" data-community-action="preview" data-operation="help-queue-status" ${attrs} type="button" title="Подготовить решение по очереди Help Board"${can('community.help.write') ? '' : ' disabled'}>Решение</button>`;
  if (capabilityId === 'help-board' && item.source === 'help_board_restrictions') return `<button class="button danger" data-community-action="preview" data-operation="help-restriction" ${attrs} type="button" title="Изменить ограничение создания тем"${can('community.help.write') ? '' : ' disabled'}>Ограничение</button>`;
  if (capabilityId === 'league-chat' && item.source === 'league_chat_moderation_queue') return `<button class="button" data-community-action="preview" data-operation="league-chat-status" ${attrs} type="button" title="Подготовить решение по сообщению"${can('community.chat.write') ? '' : ' disabled'}>Решение</button>`;
  if (capabilityId === 'league-chat' && item.source === 'league_chat_reports') return `<button class="button" data-community-action="preview" data-operation="league-chat-report" ${attrs} type="button" title="Подготовить закрытие репорта чата"${can('community.chat.write') ? '' : ' disabled'}>Закрыть</button>`;
  if (capabilityId === 'league-chat' && item.source === 'league_chat_messages') return `<button class="button danger" data-community-action="preview" data-operation="league-chat-message-status" ${attrs} type="button" title="Удалить или восстановить опубликованное сообщение"${can('community.chat.write') ? '' : ' disabled'}>Статус</button>`;
  if (capabilityId === 'league-chat' && item.source === 'league_chat_bans') return `<button class="button danger" data-community-action="preview" data-operation="league-chat-restriction" ${attrs} type="button" title="Изменить mute или chat-ban"${can('community.chat.write') ? '' : ' disabled'}>Ограничение</button>`;
  if (capabilityId === 'arena-ranks') return `<button class="button" data-community-action="preview" data-operation="arena-profile-resync" ${attrs} type="button" title="Подготовить синхронизацию имени Arena"${can('community.arena.write') ? '' : ' disabled'}>Имя</button>`;
  if (capabilityId === 'arena-live' && item.source === 'arena_sessions') return `<button class="button danger" data-community-action="preview" data-operation="arena-session-finish" ${attrs} type="button" title="Подготовить безопасное завершение сессии"${can('community.arena.write') ? '' : ' disabled'}>Завершить</button>`;
  if (capabilityId === 'arena-bets') return `<button class="button danger" data-community-action="preview" data-operation="arena-wager-flag" ${attrs} type="button" title="Подготовить изменение флага ставки"${can('community.arena.economy.write') ? '' : ' disabled'}>Флаг</button>`;
  if (capabilityId === 'arena-rooms' && item.source === 'arena_rooms_live') return `<button class="button danger" data-community-action="preview" data-operation="arena-room-close" ${attrs} type="button" title="Подготовить закрытие комнаты"${can('community.arena.write') ? '' : ' disabled'}>Закрыть</button> <button class="button danger" data-community-action="preview" data-operation="arena-room-delete" ${attrs} type="button" title="Подготовить удаление комнаты"${can('community.arena.destructive') ? '' : ' disabled'}>Удалить</button>`;
  return '—';
}

export function renderCommunityOperationsWorkspace(model, { escapeHtml, can }) {
  const workspace = model.workspace; const items = Array.isArray(workspace?.items) ? workspace.items : [];
  const rows = items.length ? items.map((item) => `<tr><td data-label="ID"><button class="link-button" data-community-action="detail" data-id="${escapeHtml(item.id)}" data-source="${escapeHtml(item.source || '')}" type="button" title="Открыть детали записи">${escapeHtml(item.id)}</button></td><td data-label="Источник">${escapeHtml(item.source || LABELS[model.capabilityId])}</td><td data-label="Статус">${escapeHtml(item.status || item.decision || item.state || '—')}</td><td data-label="Версия"><code>${escapeHtml(version(item.version).slice(0, 12))}</code></td><td data-label="Действие">${rowAction(item, model.capabilityId, can, escapeHtml)}</td></tr>`).join('') : '<tr><td colspan="5">В ограниченной серверной выборке записей нет.</td></tr>';
  const preview = model.preview ? `<section class="card section"><div class="card-header"><div><h2>Серверный preview</h2><p>Опасные Arena-операции показывают точный manifest и требуют другого администратора.</p></div><span class="badge warning">community.approve</span></div><div class="card-body"><pre class="code-preview">${escapeHtml(json(model.preview))}</pre><div class="field"><label for="community-confirmation">Введите точное подтверждение</label><input id="community-confirmation" placeholder="${escapeHtml(model.preview.confirmation)}"></div><div class="actions"><button class="button" data-community-action="discard" type="button" title="Отменить текущий предпросмотр">Отмена</button><button class="button primary" data-community-action="request-approval" type="button" title="Отправить неизменяемый предпросмотр другому администратору">Запросить одобрение</button><button class="button danger" data-community-action="apply" type="button" title="Применить ранее одобренную операцию">Применить одобренное</button></div><p>${escapeHtml(model.approvalStatus || '')}</p></div></section>` : '';
  return `<header class="page-header"><div><p class="eyebrow">Комьюнити / Нативные операции</p><h1>Community и Arena</h1><p>Очереди, Help Board, клубы, чат и Arena перенесены в отдельные рабочие формы. Глобальные баны остаются в готовом Safety & Moderation.</p></div><div class="actions"><a class="button" href="#safety-moderation">Safety & Moderation</a><button class="button" data-community-action="refresh" type="button" title="Обновить данные раздела">Обновить</button></div></header>
    <select class="mobile-view-select" data-community-mobile-view aria-label="Раздел Community">${COMMUNITY_OPERATION_CAPABILITIES.map((id) => `<option value="${id}"${id === model.capabilityId ? ' selected' : ''}>${LABELS[id]}</option>`).join('')}</select><div class="actions native-operation-tabs">${COMMUNITY_OPERATION_CAPABILITIES.map((id) => `<button class="button ${id === model.capabilityId ? 'primary' : ''}" data-community-action="select" data-capability="${id}" type="button" title="Открыть раздел: ${LABELS[id]}">${LABELS[id]}</button>`).join('')}</div>${model.error ? `<div class="notice danger">${escapeHtml(model.error)}</div>` : ''}
    <section class="card section"><div class="card-header"><div><h2>${LABELS[model.capabilityId]}</h2><p>Источник, границы выборки и версия каждой записи видны до изменения.</p></div><span class="badge">${workspace?.truncated ? 'truncated' : 'bounded'}</span></div><div class="card-body"><div class="report-filters"><div class="field"><label for="community-filter-query">Поиск в загруженной выборке</label><input id="community-filter-query" value="${escapeHtml(model.filters?.query || '')}"></div><div class="field"><label for="community-filter-status">Статус</label><input id="community-filter-status" value="${escapeHtml(model.filters?.status || '')}"></div><button class="button" data-community-action="refresh" type="button" title="Применить фильтры и обновить выборку">Применить фильтры</button></div><details><summary>Состояние источников</summary><pre class="source-health">${escapeHtml(json(workspace?.sourceHealth || []))}</pre></details><div class="table-wrap"><table><thead><tr><th>ID</th><th>Источник</th><th>Статус</th><th>Версия</th><th>Действие</th></tr></thead><tbody>${rows}</tbody></table></div>${model.detail ? `<details open><summary>Детали записи</summary><pre class="code-preview">${escapeHtml(json(model.detail))}</pre></details>` : ''}</div></section>
    <section class="card section"><div class="card-header"><div><h2>Подготовить изменение</h2><p>Доступны только операции выбранного раздела; произвольного JSON нет.</p></div></div><div class="card-body">${editor(model.capabilityId, can)}</div></section>
    <section class="columns section"><div class="card"><div class="card-header"><div><h2>Одобрить чужой запрос</h2></div></div><div class="card-body"><div class="field"><label for="community-approval-preview-id">Preview ID</label><input id="community-approval-preview-id"></div><div class="field"><label for="community-approval-reason">Что проверено</label><input id="community-approval-reason"></div><button class="button" data-community-action="approve" type="button" title="Одобрить запрос, созданный другим администратором"${can('community.approve') ? '' : ' disabled'}>Одобрить</button></div></div><div class="card"><div class="card-header"><div><h2>Продолжить Arena manifest</h2><p>Идемпотентные пакеты по 25 профилей.</p></div></div><div class="card-body"><div class="field"><label for="community-manifest-id">Manifest ID</label><input id="community-manifest-id"></div><div class="field"><label for="community-resume-key">Ключ повтора (сохраните для безопасного retry)</label><input id="community-resume-key"></div><button class="button danger" data-community-action="resume" type="button" title="Продолжить одобренный Arena manifest со следующего пакета"${can('community.arena.destructive') ? '' : ' disabled'}>Продолжить безопасную очистку</button><pre class="code-preview">${escapeHtml(json(model.bulkProgress || {}))}</pre></div></div></section>${preview}`;
}
