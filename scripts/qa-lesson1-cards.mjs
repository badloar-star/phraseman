/**
 * Устарело: текстовые «карточки разбора» (correct/wrong/secret по фразам урока) удалены из приложения.
 */
console.error(
  '[qa-lesson1-cards] Отключено: lesson_cards_data / PhraseCard больше не используются. См. error_report.lessonPhraseReportDataId для dataId репортов.',
);
process.exit(0);
