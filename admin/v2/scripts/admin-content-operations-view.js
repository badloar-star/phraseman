import { CONTENT_OPERATION_CAPABILITIES } from './admin-content-operations-state.js';
const LABELS = Object.freeze({ 'community-packs': 'Пакеты сообщества', 'card-packs': 'Наборы карточек', 'daily-phrases': 'Фразы дня', 'french-quizzes': 'Французские квизы', 'explain-reports': 'Репорты объяснений', 'full-content-control': 'Контроль контента' });
function json(value) { try { return JSON.stringify(value, null, 2); } catch { return '{}'; } }

export function renderContentOperationsWorkspace(model, { escapeHtml, can }) {
  const workspace = model.workspace; const items = Array.isArray(workspace?.items) ? workspace.items : [];
  const rows = items.length ? items.map((item) => `<tr><td data-label="ID"><button class="link-button" data-content-action="detail" data-id="${escapeHtml(item.id)}" type="button">${escapeHtml(item.id)}</button></td><td data-label="Статус">${escapeHtml(item.status || (item.published === true ? 'published' : '—'))}</td><td data-label="Версия"><code>${escapeHtml(String(item.version || '').slice(0, 12))}</code></td></tr>`).join('') : '<tr><td colspan="3">Нет записей в ограниченной серверной выборке.</td></tr>';
  const frenchHold = model.capabilityId === 'french-quizzes' ? '<div class="notice warning"><strong>HOLD</strong> · activationApproved=false · productionReady=false. Доступны только детерминированный draft и rollback draft. Генерация и активация отсутствуют.</div>' : '';
  const preview = model.preview ? `<section class="card section"><div class="card-header"><div><h2>Серверный preview</h2><p>Изменение требует другого администратора.</p></div><span class="badge warning">content.approve</span></div><div class="card-body"><pre class="code-preview">${escapeHtml(json(model.preview))}</pre><div class="field"><label for="content-confirmation">Точное подтверждение</label><input id="content-confirmation" placeholder="${escapeHtml(model.preview.confirmation)}"></div><div class="actions"><button class="button" data-content-action="discard" type="button">Отмена</button><button class="button primary" data-content-action="request-approval" type="button">Запросить одобрение</button><button class="button danger" data-content-action="apply" type="button">Применить одобренное</button></div><p>${escapeHtml(model.approvalStatus || '')}</p></div></section>` : '';
  return `<header class="page-header"><div><p class="eyebrow">Контент / Нативные операции</p><h1>Операции с контентом</h1><p>Language Factory остаётся отдельным готовым экраном; здесь только обслуживание существующего контента.</p></div><div class="actions"><a class="button" href="#content">Language Factory</a><button class="button" data-content-action="refresh" type="button">Обновить</button></div></header>
    <select class="mobile-view-select" data-content-mobile-view aria-label="Раздел контентных операций">${CONTENT_OPERATION_CAPABILITIES.map((id) => `<option value="${id}"${id === model.capabilityId ? ' selected' : ''}>${LABELS[id]}</option>`).join('')}</select><div class="actions native-operation-tabs">${CONTENT_OPERATION_CAPABILITIES.map((id) => `<button class="button ${id === model.capabilityId ? 'primary' : ''}" data-content-action="select" data-capability="${id}" type="button">${LABELS[id]}</button>`).join('')}</div>${model.error ? `<div class="notice danger">${escapeHtml(model.error)}</div>` : ''}${frenchHold}
    <section class="card section"><div class="card-header"><div><h2>${LABELS[model.capabilityId]}</h2><p>Ограниченная серверная проекция и честное состояние источника.</p></div><span class="badge">${workspace?.truncated ? 'truncated' : 'bounded'}</span></div><div class="card-body"><pre class="source-health">${escapeHtml(json(workspace?.sourceHealth || []))}</pre><div class="table-wrap"><table><thead><tr><th>ID</th><th>Статус</th><th>Версия</th></tr></thead><tbody>${rows}</tbody></table></div>${model.detail ? `<details open><summary>Деталь</summary><pre class="code-preview">${escapeHtml(json(model.detail))}</pre></details>` : ''}</div></section>
    <section class="card section"><div class="card-header"><div><h2>Подготовить изменение</h2><p>French всегда сохраняет activationApproved=false и productionReady=false.</p></div></div><div class="card-body"><div class="report-filters"><div class="field"><label for="content-mutation-action">Действие</label><select id="content-mutation-action"><option value="community-pack-status">Community pack status</option><option value="card-pack-update">Card pack update</option><option value="daily-phrase-upsert">Daily phrase upsert</option><option value="french-draft">French HOLD draft</option><option value="french-rollback">French HOLD rollback</option><option value="explain-report-status">Explain report status</option></select></div><div class="field"><label for="content-target-id">Target ID</label><input id="content-target-id"></div><div class="field"><label for="content-expected-version">Expected version</label><input id="content-expected-version" placeholder="missing для нового draft"></div></div><div class="field"><label for="content-payload">Payload JSON</label><textarea id="content-payload">{}</textarea></div><div class="field"><label for="content-reason">Причина</label><textarea id="content-reason"></textarea></div><button class="button primary" data-content-action="preview" type="button"${can('content.draft.write') || can('content.publish') || can('content.reports.write') ? '' : ' disabled'}>Подготовить preview</button></div></section>
    <section class="card section"><div class="card-header"><div><h2>Одобрить чужой запрос</h2></div></div><div class="card-body"><div class="field"><label for="content-approval-preview-id">Preview ID</label><input id="content-approval-preview-id"></div><div class="field"><label for="content-approval-reason">Что проверено</label><input id="content-approval-reason"></div><button class="button" data-content-action="approve" type="button"${can('content.approve') ? '' : ' disabled'}>Одобрить</button></div></section>${preview}`;
}
