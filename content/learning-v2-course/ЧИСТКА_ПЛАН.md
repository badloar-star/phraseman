# План чистки старой системы Learning V2

> Решение владельца 2026-09-03: «архив + удалить из дерева». Выполняется
> ТОЛЬКО после остановки всех остальных сессий по Learning V2 — на момент
> составления плана другая сессия держала незакоммиченные правки в
> `docs/v2/СТАРТ В2.md`, `БИБЛИЯ`, `HANDOVER.md`, `AGENTS.md`, 24+ файлах
> `modules/learning-v2/content/source/` и 142 неотслеживаемых файлах.

## Шаг 0 — страховка

```bash
git tag learning-v2-archive-2026-09-03
```

Тег на текущий HEAD. Плюс один сжатый слепок в
`docs/v2/_archive/learning-v2-2026-09-03.tar.gz` (docs + content/source +
tests/learning_v2*), чтобы восстановить за минуту без раскопок в истории.

## Шаг 1 — что уходит из дерева

| Что | Объём | Примечание |
|---|---|---|
| `docs/v2/*.md` (кроме `_archive/`) | 50 файлов, 3,2 МБ | все handover/contract/bible/checklist |
| `docs/v2/curriculum`, `reviews`, `reference-evidence`, `frontend-handoff`, `evidence` | ~0,3 МБ | в архив |
| `docs/v2/mockups/**` | 2 МБ | **ОСТАВИТЬ** — это планка дизайна режимов; сессия четырёх новых режимов делает макеты сюда же (`modes/ЗАДАНИЕ_4_РЕЖИМА.md`) |
| `modules/learning-v2/content/source/**` | 392 файла, 11 МБ | ВЕСЬ контент, включая испанский (владелец: всё) |
| `modules/learning-v2/curriculum/**` | 716 КБ | exact packets, blueprint validation |
| `modules/learning-v2/authoring/**` | 64 КБ | реестры/пре-флайты авторства |
| `modules/learning-v2/preview/**` + `app/_learning_v2_authoring_preview.tsx` | 48 КБ | превью старого контента |
| `tests/learning_v2_*` | 417 файлов | контентные гейты. Тесты рантайма (modes/runtime/progress без импорта content/source) — ОСТАВИТЬ, отобрать по импорту |
| `scripts/learning_v2*` + `scripts/build_learning_v2_*` | 24 файла | гейты и сборщики owner-map/HTML/PDF |
| `package.json` → 23 скрипта `learning-v2:*` | — | |
| `.codex-tmp/learning-v2-*` | 13+ МБ | не в git, просто удалить |
| `output/pdf/learning-v2-course-continuation-pack-*` | 11 МБ | если в git — в архив |
| `docs/superpowers/specs/*learning-v2*` | 9 файлов | дизайн-спеки UI можно оставить (они про рантайм) — решить при чистке |

## Шаг 2 — что ОСТАЁТСЯ (движок)

- `modules/learning-v2/{runtime,modes,contracts,policies,progress,map,migration}` — плеер, 8 механик, компилятор, прогресс, карта.
- `app/learning-v2/**`, `app/learning_v2_*.ts` — экраны и загрузчик.
- Аудио-модули `app/learning_v2_*_production_audio_v1.ts`.

## Шаг 3 — чтобы приложение продолжало собираться

Импортёры `content/source` в рантайме:

| Файл | Что импортирует | Замена |
|---|---|---|
| `app/learning_v2_course_released_session_client_v3.ts:60-62` | `authored_sessions_v1`, `es_authored_sessions_v1`, `session_package_from_shard_v1` | новый `modules/learning-v2/release/release_loader_v1.ts`: читает `release/*.json`; пока релиза нет — пустой реестр, экран курса показывает «скоро» |
| `modules/learning-v2/progress/learning_session_rune_reward_composite_v1.ts:19-20` | те же | тот же загрузчик |
| `modules/learning-v2/modes/mode_router_v1.tsx:16` | только комментарий | убрать строку |
| `modules/learning-v2/preview/*` | реестры/источники | удаляются вместе с превью |

Компилятор `session_shard_from_source_v1.ts` и `session_package_from_shard_v1.ts`
переезжают в `content/learning-v2-course/pipeline/` (они — часть сборки
релиза, не приложения). В приложении остаётся только парсер JSON.

## Шаг 4 — документы правил

- `CLAUDE.md`: два раздела «⛔ LEARNING V2 …» заменить одним: «Курс пишется в
  `content/learning-v2-course/` по `КОНСТИТУЦИЯ.md`; приложение читает только
  `release/`».
- `AGENTS.md`: 11 упоминаний — вычистить ссылки на `docs/v2`.
- Память Claude: пометить старые заметки Learning V2 как исторические.

## Шаг 5 — проверка после чистки

Точечно, под слотом светофора:
```bash
npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "learning|learning-v2" | head
```
Ожидание: 0 ошибок в learning-v2 путях. Экран курса в dev открывается и
показывает пустое состояние.
