import { CONTENT_OPERATION_CAPABILITIES } from './admin-content-operations-state.js';

const LABELS = Object.freeze({
  'community-packs': 'Пакеты сообщества', 'card-packs': 'Наборы карточек', 'daily-phrases': 'Фразы дня',
  'french-quizzes': 'Французские квизы', 'explain-reports': 'Репорты объяснений', 'full-content-control': 'Контроль контента',
});
function json(value) { try { return JSON.stringify(value, null, 2); } catch { return '{}'; } }
function version(value) { return String(value || 'missing'); }
function commonTarget() { return `<div class="report-filters"><div class="field"><label for="content-target-id">ID записи</label><input id="content-target-id" autocomplete="off"></div><div class="field"><label for="content-expected-version">Текущая версия</label><input id="content-expected-version" placeholder="missing — только для новой записи" autocomplete="off"></div></div>`; }
function commonReason() { return `<div class="field"><label for="content-reason">Причина изменения</label><textarea id="content-reason" required placeholder="Что меняется и зачем"></textarea></div>`; }
function button(operation, label, permission, can, tone = 'primary') { return `<button class="button ${tone}" data-content-action="preview" data-operation="${operation}" type="button" title="Подготовить операцию: ${label}"${can(permission) ? '' : ' disabled'}>${label}</button>`; }

function capabilityEditor(model, can) {
  const capabilityId = model.capabilityId;
  const fromDate = /^\d{4}-\d{2}-\d{2}$/.test(model.dateRange?.fromDate || '') ? model.dateRange.fromDate : '';
  const toDate = /^\d{4}-\d{2}-\d{2}$/.test(model.dateRange?.toDate || '') ? model.dateRange.toDate : '';
  if (capabilityId === 'community-packs') return `${commonTarget()}<div class="report-filters"><div class="field"><label for="content-decision">Решение по заявке</label><select id="content-decision"><option value="approve">Одобрить</option><option value="request_changes">Запросить изменения</option><option value="reject">Отклонить</option></select></div><div class="field"><label for="content-expected-status">Ожидаемый статус</label><input id="content-expected-status" value="pending"></div><div class="field"><label for="content-pack-decision">Решение по активному пакету</label><select id="content-pack-decision"><option value="require_revision">Вернуть на доработку</option><option value="remove">Снять с публикации</option></select></div><div class="field"><label for="content-pack-report-status">Статус репорта</label><select id="content-pack-report-status"><option value="reviewed">Просмотрен</option><option value="resolved">Закрыт</option></select></div></div><div class="field"><label for="content-message">Сообщение автору / заметка по репорту</label><textarea id="content-message"></textarea></div>${commonReason()}<div class="actions">${button('community-submission-decision', 'Решение по заявке', 'community.moderate', can)}${button('community-pack-status', 'Изменить активный пакет', 'content.publish', can, 'danger')}${button('community-pack-report-status', 'Закрыть репорт пакета', 'community.moderate', can)}</div>`;
  if (capabilityId === 'card-packs') return `${commonTarget()}<div class="report-filters"><div class="field"><label for="content-title">Название</label><input id="content-title"></div><div class="field"><label for="content-price">Цена в шардах</label><input id="content-price" type="number" min="0" max="1000000"></div><div class="field"><label for="content-category">Категория</label><input id="content-category"></div><div class="field"><label for="content-card-status">Статус</label><select id="content-card-status"><option value="draft">Черновик</option><option value="published">Опубликован</option><option value="unpublished">Снят с публикации</option><option value="archived">Архив</option></select></div></div>${commonReason()}<div class="actions">${button('card-pack-draft', 'Сохранить метаданные черновика', 'content.draft.write', can)}${button('card-pack-update', 'Проверить цену и публикацию', 'content.publish', can)}</div>`;
  if (capabilityId === 'daily-phrases') return `${commonTarget()}<div class="report-filters"><div class="field"><label for="content-english">Английская фраза</label><input id="content-english"></div><div class="field"><label for="content-literal">Дословно</label><input id="content-literal"></div><div class="field"><label for="content-meaning">Значение</label><input id="content-meaning"></div><div class="field"><label for="content-date">Дата показа</label><input id="content-date" type="date"></div><div class="field"><label for="content-order">Порядок</label><input id="content-order" type="number" min="1" value="1"></div></div><div class="actions"><label><input id="content-active" type="checkbox" checked> Активна</label><label><input id="content-allow-save" type="checkbox" checked> Можно сохранить</label></div><div class="field"><label for="content-import-lines">Импорт CSV — id,english,literal,meaning,date,order,active,allowSave,expectedVersion (до 100 строк)</label><textarea id="content-import-lines" placeholder="phrase-001,Hold on,держись,подожди,2026-07-14,1,true,true,missing"></textarea></div><div class="field"><label for="content-reorder-lines">Новый порядок CSV — id,date,expectedVersion (до 100 строк)</label><textarea id="content-reorder-lines" placeholder="phrase-001,2026-07-14,abc123"></textarea></div>${commonReason()}<div class="actions">${button('daily-phrase-draft', 'Сохранить неактивный черновик', 'content.draft.write', can)}${button('daily-phrase-upsert', 'Запланировать или активировать', 'content.publish', can)}${button('daily-phrase-import', 'Проверить импорт', 'content.publish', can)}${button('daily-phrase-reorder', 'Проверить порядок', 'content.publish', can)}${button('daily-phrase-rollback', 'Откатить выбранную', 'content.publish', can, 'danger')}</div>`;
  if (capabilityId === 'french-quizzes') return `<div class="notice warning"><strong>HOLD:</strong> activationApproved=false, productionReady=false. Генерации и активации на этом экране нет. Разрешены только детерминированный операционный черновик и черновик отката.</div>${commonTarget()}${commonReason()}<div class="actions">${button('french-draft', 'Подготовить HOLD-черновик', 'content.draft.write', can)}${button('french-rollback', 'Подготовить HOLD-откат', 'content.publish', can, 'danger')}</div>`;
  if (capabilityId === 'explain-reports') return `${commonTarget()}<div class="field"><label for="content-report-status">Статус</label><select id="content-report-status"><option value="done">Разобрано (done)</option><option value="new">Новое</option><option value="reviewing">На проверке</option><option value="resolved">Исправлено</option><option value="dismissed">Отклонено</option></select></div><div class="field"><label for="content-report-lines">Массовая обработка — id,expectedVersion (до 100 строк)</label><textarea id="content-report-lines"></textarea></div><div class="field"><label for="content-cache-collection">Коллекция кэша</label><select id="content-cache-collection"><option value="phrase_explanations">phrase_explanations</option><option value="mistake_explanations">mistake_explanations</option><option value="quiz_explanations">quiz_explanations</option></select></div><div class="field"><label for="content-message">Комментарий администратора</label><textarea id="content-message"></textarea></div>${commonReason()}<div class="actions">${button('explain-report-status', 'Обновить один репорт', 'content.reports.write', can)}${button('explain-reports-bulk', 'Проверить массовое завершение', 'content.reports.write', can)}${button('explain-report-delete', 'Удалить запись репорта', 'content.reports.write', can, 'danger')}${button('explain-cache-delete', 'Удалить кэш', 'content.reports.write', can, 'danger')}${button('explain-counter-delete', 'Сбросить счётчик', 'content.reports.write', can, 'danger')}</div><p class="muted">Удаление записи, кэша и счётчика — три разные операции. Каждая показывает точную цель и требует второго администратора.</p>`;
  return `<div class="report-filters"><div class="field"><label for="content-from-date">С даты</label><input id="content-from-date" type="date" value="${fromDate}"></div><div class="field"><label for="content-to-date">По дату</label><input id="content-to-date" type="date" value="${toDate}"></div></div><button class="button primary" data-content-action="refresh" type="button" title="Загрузить телеметрию за выбранный период">Загрузить телеметрию за период</button><p class="muted">Чтение идёт из plan_content_telemetry_events/{day}/events, максимум 31 день и 1000 событий.</p>`;
}

function rowAction(item, capabilityId, can, escapeHtml) {
  const attrs = `data-id="${escapeHtml(item.id)}" data-version="${escapeHtml(version(item.version))}" data-status="${escapeHtml(item.status || '')}"`;
  if (capabilityId === 'community-packs' && item.source === 'community_pack_submissions') return `<button class="button" data-content-action="preview" data-operation="community-submission-decision" ${attrs} type="button" title="Подготовить решение по заявке"${can('community.moderate') ? '' : ' disabled'}>Решение</button>`;
  if (capabilityId === 'community-packs' && item.source === 'community_packs') return `<button class="button danger" data-content-action="preview" data-operation="community-pack-status" ${attrs} type="button" title="Подготовить изменение статуса опубликованного пака"${can('content.publish') ? '' : ' disabled'}>Модерировать</button>`;
  if (capabilityId === 'community-packs' && item.source === 'community_pack_reports') return `<button class="button" data-content-action="preview" data-operation="community-pack-report-status" ${attrs} type="button" title="Подготовить решение по репорту"${can('community.moderate') ? '' : ' disabled'}>Репорт</button>`;
  if (capabilityId === 'card-packs') return `<button class="button" data-content-action="preview" data-operation="card-pack-update" ${attrs} type="button" title="Подготовить изменение карточного пака"${can('content.publish') ? '' : ' disabled'}>Изменить</button>`;
  if (capabilityId === 'daily-phrases') return `<button class="button" data-content-action="preview" data-operation="daily-phrase-upsert" ${attrs} type="button" title="Подготовить изменение ежедневной фразы"${can('content.publish') ? '' : ' disabled'}>Изменить</button> <button class="button danger" data-content-action="preview" data-operation="daily-phrase-rollback" ${attrs} type="button" title="Подготовить откат ежедневной фразы"${can('content.publish') ? '' : ' disabled'}>Откат</button>`;
  if (capabilityId === 'explain-reports' && item.source === 'explain_report_entries') return `<button class="button" data-content-action="preview" data-operation="explain-report-status" ${attrs} type="button" title="Подготовить изменение статуса репорта"${can('content.reports.write') ? '' : ' disabled'}>Статус</button> <button class="button danger" data-content-action="preview" data-operation="explain-report-delete" ${attrs} type="button" title="Подготовить удаление записи репорта"${can('content.reports.write') ? '' : ' disabled'}>Удалить</button>`;
  if (capabilityId === 'explain-reports' && item.source === 'explain_reports') return `<button class="button danger" data-content-action="preview" data-operation="explain-counter-delete" ${attrs} type="button" title="Подготовить сброс счётчика"${can('content.reports.write') ? '' : ' disabled'}>Сбросить</button>`;
  if (capabilityId === 'explain-reports' && ['phrase_explanations', 'mistake_explanations', 'quiz_explanations'].includes(item.source)) return `<button class="button danger" data-content-action="preview" data-operation="explain-cache-delete" data-source="${escapeHtml(item.source)}" ${attrs} type="button" title="Подготовить удаление выбранной записи кэша"${can('content.reports.write') ? '' : ' disabled'}>Удалить кэш</button>`;
  return '—';
}

export function renderContentOperationsWorkspace(model, { escapeHtml, can }) {
  const workspace = model.workspace; const items = Array.isArray(workspace?.items) ? workspace.items : [];
  const rows = items.length ? items.map((item) => `<tr><td data-label="ID"><button class="link-button" data-content-action="detail" data-id="${escapeHtml(item.id)}" data-source="${escapeHtml(item.source || '')}" type="button" title="Открыть детали записи">${escapeHtml(item.id)}</button></td><td data-label="Источник">${escapeHtml(item.source || LABELS[model.capabilityId])}</td><td data-label="Статус">${escapeHtml(item.status || (item.published === true ? 'published' : '—'))}</td><td data-label="Версия"><code>${escapeHtml(version(item.version).slice(0, 12))}</code></td><td data-label="Действие">${rowAction(item, model.capabilityId, can, escapeHtml)}</td></tr>`).join('') : '<tr><td colspan="5">В ограниченной серверной выборке записей нет.</td></tr>';
  const preview = model.preview ? `<section class="card section"><div class="card-header"><div><h2>Серверный preview</h2><p>Изменение ещё не применено. Его должен одобрить другой администратор.</p></div><span class="badge warning">content.approve</span></div><div class="card-body"><pre class="code-preview">${escapeHtml(json(model.preview))}</pre><div class="field"><label for="content-confirmation">Введите точное подтверждение</label><input id="content-confirmation" placeholder="${escapeHtml(model.preview.confirmation)}"></div><div class="actions"><button class="button" data-content-action="discard" type="button" title="Отменить текущий предпросмотр">Отмена</button><button class="button primary" data-content-action="request-approval" type="button" title="Отправить неизменяемый предпросмотр другому администратору">Запросить одобрение</button><button class="button danger" data-content-action="apply" type="button" title="Применить ранее одобренную операцию">Применить одобренное</button></div><p>${escapeHtml(model.approvalStatus || '')}</p></div></section>` : '';
  return `<header class="page-header"><div><p class="eyebrow">Контент / Нативные операции</p><h1>Операции с контентом</h1><p>Здесь обслуживается существующий контент. Генерация языков и готовая Language Factory не изменяются.</p></div><div class="actions"><a class="button" href="#content">Language Factory</a><button class="button" data-content-action="refresh" type="button" title="Обновить данные раздела">Обновить</button></div></header>
    <select class="mobile-view-select" data-content-mobile-view aria-label="Раздел контентных операций">${CONTENT_OPERATION_CAPABILITIES.map((id) => `<option value="${id}"${id === model.capabilityId ? ' selected' : ''}>${LABELS[id]}</option>`).join('')}</select><div class="actions native-operation-tabs">${CONTENT_OPERATION_CAPABILITIES.map((id) => `<button class="button ${id === model.capabilityId ? 'primary' : ''}" data-content-action="select" data-capability="${id}" type="button" title="Открыть раздел: ${LABELS[id]}">${LABELS[id]}</button>`).join('')}</div>${model.error ? `<div class="notice danger">${escapeHtml(model.error)}</div>` : ''}
    <section class="card section"><div class="card-header"><div><h2>${LABELS[model.capabilityId]}</h2><p>Источник, границы выборки и версия каждой записи видны до изменения.</p></div><span class="badge">${workspace?.truncated ? 'truncated' : 'bounded'}</span></div><div class="card-body"><div class="report-filters"><div class="field"><label for="content-filter-query">Поиск в загруженной выборке</label><input id="content-filter-query" value="${escapeHtml(model.filters?.query || '')}"></div><div class="field"><label for="content-filter-status">Статус</label><input id="content-filter-status" value="${escapeHtml(model.filters?.status || '')}" placeholder="например pending"></div><button class="button" data-content-action="refresh" type="button" title="Применить фильтры и обновить выборку">Применить фильтры</button></div><details><summary>Состояние источников</summary><pre class="source-health">${escapeHtml(json(workspace?.sourceHealth || []))}</pre></details><div class="table-wrap"><table><thead><tr><th>ID</th><th>Источник</th><th>Статус</th><th>Версия</th><th>Действие</th></tr></thead><tbody>${rows}</tbody></table></div>${model.detail ? `<details open><summary>Детали записи</summary><pre class="code-preview">${escapeHtml(json(model.detail))}</pre></details>` : ''}</div></section>
    <section class="card section"><div class="card-header"><div><h2>Подготовить изменение</h2><p>Форма показывает только допустимые для этого раздела поля и операции.</p></div></div><div class="card-body">${capabilityEditor(model, can)}</div></section>
    <section class="card section"><div class="card-header"><div><h2>Одобрить чужой запрос</h2><p>Автор запроса не может одобрить его сам.</p></div></div><div class="card-body"><div class="report-filters"><div class="field"><label for="content-approval-preview-id">Preview ID</label><input id="content-approval-preview-id"></div><div class="field"><label for="content-approval-reason">Что проверено</label><input id="content-approval-reason"></div></div><button class="button" data-content-action="approve" type="button" title="Одобрить запрос, созданный другим администратором"${can('content.approve') ? '' : ' disabled'}>Одобрить</button></div></section>${preview}`;
}
