## Изменения

- **Задание 3 (BLOCK судьи бреда).** Убран дистрактор `old` и неверный факт «в `called` два слога». `called` произносится одним слогом — сравнение по числу слогов было ложью, а сравнивать `cold` и `called` на слух вообще ненадёжно. Взят дистрактор `gold` с настоящим различием в первом звуке, второй дистрактор `called` разведён по смыслу, а не по звуку.
- **Задание 12 (must_fix судьи вкуса).** Убрана запись английского гласного русской буквой «а». `sit` теперь отличается по числу слогов и ударению — это слышно и это правда. Дистрактор `up` заменён на `sorry`: `up` — часть самого слова `upset`, такая ловушка сбивала бы, а не проверяла.
- **Интро 2 (must_fix судьи вкуса).** Абзац 3 повторял абзац 2 — повтор вырезан, оставшееся сжато в один абзац с фактом о порядке слов. Осторожно с формулировкой: сказано «`he not is` не бывает» про конкретную пару слов, а не «язык не тянет» вообще.
- **Задание 15 (REVISE педагога).** Убрана строка про лишнюю плитку — обманка на внимательность осталась одна, в задании 13. Плитка `upset` в наборе сохранена: ученик и так знает из интро 1, что слово из вопроса не повторяют, а фокус финальной трети возвращён на саму операцию.
- Всё из `best_lines` сохранено дословно: «Приятная лень…», «самая обидная ошибка на свете…», «Берёте два слова прямо из вопроса…».
- Самопроверка обновлена под новые дистракторы; число заданий, механики и структура не менялись.

---

🟡 **Стоит улучшить**
- Задание 15 формально осталось с лишней плиткой `upset`, но без подписи-подсказки. Если сборщик показывает лишние плитки только при явной пометке — плитку надо убрать из набора; проверьте контракт `phrase_builder`.

🧩 **Скрытые связи**
- Сессия не имеет новых слов, поэтому сторож `check_new_words.mjs` её не тронет.
- Слова `gold` и `sorry` появились только как дистракторы в звуковых заданиях — в целевые фразы они не входят, границы материала (§3) не нарушены.

---

- Исправил главную ошибку: в задании на слух было написано, что слово `called` состоит из двух слогов — это неправда, слог там один. Заменил на пару, которая действительно различается на слух.
- Убрал попытку записать английский гласный русской буквой в другом звуковом задании — вместо этого объяснил разницу через число слогов и ударение, это честно.
- Вырезал из второй страницы объяснения абзац, который повторял предыдущий своими словами.
- Оставил только одну ловушку с лишней плиткой вместо двух — чтобы к концу занятия человек думал о самом правиле, а не о подвохах.
- Все удачные фразы, которые хвалили проверяющие, остались нетронутыми.