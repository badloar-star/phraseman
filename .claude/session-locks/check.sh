#!/usr/bin/env bash
# зачем: было ДВА разных светофора (session-locks с 1 слотом и semaphore с 3),
# сессии читали разные README и соблюдали разные правила — то есть мешали друг
# другу вместо координации. Единый источник правды теперь .claude/semaphore.
# Этот файл оставлен переходником, чтобы сессии со старыми командами не сломались.
exec bash "$(dirname "$0")/../semaphore/slot.sh" status
