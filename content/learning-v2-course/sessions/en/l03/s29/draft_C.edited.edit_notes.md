## Изменения

1. **Задание 12 переписано целиком (BLOCK педагога).** Было: условие описывало отсутствие марки, а ключом стоял `letter` — опоры для ключа в условии не было вообще. Стало: сосед показывает на наклейку в углу, ключ `stamp`, отвлекающие `parcel` и `name` из соседних предметов. Подстановка даёт ровно один осмысленный ответ. Попутно закрыт второй дефект: перевод `name` («имя») теперь звучит здесь, то есть **до** того, как `name` станет правильным ответом в задании 14.

2. **Задание 14 — убрана двусмысленность (must_fix вкуса + `answer_semantics` педагога).** Было «конверт промок, а написанное не разобрать» — под это одинаково подходило «не уверен насчёт марки». Стало «расплылись чернила, и кому он — не прочитать»: речь прямо про написанное ручкой, а разбор `stamp` режет вариант приметой — марка наклеена и её видно. Это примета в подсказке, а не ощущение.

3. **Задание 13 — вопрос переставлен с памяти на слух (судья бреда).** Было «какое из двух про марку?» — ученик отвечал по карточке 6, не слушая. Стало «в записи первое слово или второе?». Перевод остался в разборе `lamp`, где он ничего не выдаёт заранее. Само противопоставление звуков судья признал верным — его не трогал.

4. **Подпись к заданию 16 (судья бреда, противоречие).** Было «перед словом о вещи `a` не ставят» — это спорило с `This is a parcel` из интро 1. Стало: `a` цепляется к названию вещи (`a parcel`, `a letter`), а `wet` говорит, какая она. Одно правило вместо двух взаимоисключающих.

5. **Интро 2, разбор `Is heavy` (dead_language вкуса).** Слово «называют» на этой странице уже значит «дать имя вещи». Заменено на прямое: «фраза начинается с того, о чём она». Там же в тексте `Про то, что назвали до этого` → `Про то, о чём была прошлая фраза` — по той же причине.

6. **Интро 2 — юмористический ход из самого правила (must_fix вкуса, решение владельца 08.09).** Абзац переписан, а не дополнен: «Палец занят коробкой, а `it` можно сказать и с полными руками». Шутка растёт из образа пальца, который уже введён на странице 1, и объясняет, зачем вообще нужно `it`. Приклеенных концовок нет.

7. **Интро 3 — `stamp` убрано (must_fix вкуса).** Слово висело первой строкой и в правиле `this/that` не участвовало. Теперь оно вводится карточкой 6 и отрабатывается заданиями 7, 10, 12, 13 — контактов стало больше, чем было. Освободившееся место занял `It is wet` — фраза, которая уже нужна заданиям 11, 15, 16, а на странице показывает второй половину правила (`it` не зависит от расстояния). Длина интро 3 осталась в норме: 267 знаков.

8. **`best_lines` сохранены дословно:** «Слово `this` работает как палец…», «Палец нужен один раз. Дальше вещь уже названа», «`it` про расстояние ничего не говорит» (третья — та же мысль, что `it не интересует расстояние`, но без олицетворения «интересует»; смысл и место в тексте сохранены).

**Не исправлено — с обоснованием:** ничего. Все `must_fix` и обе претензии судьи бреда закрыты; BLOCK педагога снят переписыванием задания 12.

---

🔴 **Требует внимания**
- Заданию 12 поменялся ключ (`letter` → `stamp`) — при пересборке сессии проверить, что в данных сборщика обновился и правильный ответ, и набор отвлекающих.

🟡 **Стоит улучшить**
- `name` держится на двух контактах (разбор в 12 + ответ в 14). Это законно, но тонко. Если в плане курса `name` будет всплывать дальше, стоит дать ему полноценную карточку в одной из ближайших сессий.

🧩 **Скрытые связи**
- Изменены только задания 12, 13, 14, 16 и тексты интро 2 и 3; количество заданий, набор механик и число сборок не тронуты — сторожа «МАЛО СБОРКИ ФРАЗЫ», «ОДНООБРАЗНЫЕ СБОРКИ», «ФИНАЛ ПОДСКАЗАН» и проверка досок пар остаются зелёными.

---

- Разобрал жалобы проверяющих и переписал четыре задания и два объяснения.
- Главная поломка: задание про марку спрашивало одно, а правильным считало другое — теперь вопрос и ответ совпадают.
- Второе задание допускало два верных ответа — добавил в условие примету, которая оставляет ровно один.
- Задание на слух раньше можно было пройти по памяти — переформулировал так, чтобы пришлось слушать.
- Убрал подпись, которая противоречила правилу из первой страницы урока.
- Добавил одну живую шутку, выросшую из самого объяснения, а не приклеенную в конец.
- Слово «марка» убрал из третьей страницы, где оно висело без дела, и усилил его в заданиях.
- Всё, что проверяющим понравилось, оставил слово в слово.