// Neutral runtime seam for the lesson's geometry-preserving first frame.
// The implementation stays shared with existing lesson surfaces.
export { default } from '../components/LessonLoadingSkeleton';
