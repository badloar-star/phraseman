/**
 * personal_plan_activation.ts — остаток модуля личных учебных планов.
 *
 * зачем 16.08.2026: сами планы выведены из продукта, модуль был удалён. Но новый
 * онбординг (components/CleanOnboarding.tsx) писался, когда модуль ещё жил, и
 * импортирует отсюда один ключ — им онбординг помечает «после покупки вернуть
 * пользователя на шаг Имя». Файл онбординга берётся из макета БЕЗ правок, поэтому
 * недостающий модуль восстановлен здесь, а не подчищен в нём.
 *
 * Значение обязано СОВПАДАТЬ с литералом в app/cloud_sync.ts (список ключей,
 * которые не уезжают в облако) — разойдись они, флаг начал бы синхронизироваться.
 */
export const PERSONAL_PLAN_ONBOARDING_NICKNAME_PENDING_KEY = 'personal_plan_onboarding_nickname_pending_v1';
