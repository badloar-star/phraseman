## Изменения

- **Интро 1, безусловное «вежливой формы просто нет» (BLOCK судьи бреда)** — вычеркнуто. Осталась проверяемая часть: отдельного вежливого «вы» нет, `you` одно на всех. Строка про соседа/прохожего/полицейского из `best_lines` сохранена.
- **Интро 1, сцена «парень третий раз крутит карту» (REVISE педагога — копия сцены из l02_s05)** — заменена: женщина у светофора с телефоном, оглядывается на дома. Задание 2 приведено в соответствие.
- **Интро 2, ложное противопоставление «по-русски хватает одного слова» (BLOCK)** — удалено вместе с фразой «получится не вопрос, а вывеска». На их месте факт об английском: полная форма `Are you ok?`, короткое `Ok?` в жизни слышно, учим целиком. Противоречие с разбором `You ok?` снято — сам разбор `You ok?` тоже убран, вместо него ловушка с чужой скрепкой `Is you ok?`.
- **Интро 3, второй абзац (вода + пустота, BLOCK)** — вычеркнут. На его месте одна строка про рамку и хвост; пересказ трёх переводов удалён.
- **Интро 3, слабая нелепица `Are you cold cold?`** — заменена на реалистичную ловушку `Is you cold?` с разбором, который не учит чужую форму, а только отсекает её.
- **Задание 1, «нет „с“ в середине»** — исправлено: «с» стоит в конце перед «т», разбор теперь указывает верное место.
- **Задание 3, `dirty` через «th»** — разбор переведён на смысл (грязный ≠ человек у фонтанчика), ложное описание звука убрано.
- **Задание 7, `called` «слышно „л“ перед концом»** — исправлено: `called` заканчивается на «л», в `cold` после «л» ещё «д».
- **Задание 8, разбор `ok` «слишком общо»** — заменён на разбор по делу: `Are you ok?` про непонятную беду, здесь причина видна.
- Таблица самопроверки пересчитана (добавлены касания `ok` в 8 и `cold` в 5).

---

🟡 **Стоит улучшить**
- Задание 3: `thirty` всё ещё разведено по звуку («с» перед последней частью). Разбор верный, но по правилам файла особенностей языка смысловое разведение надёжнее — при следующем круге можно заменить на «тридцать — это число, а не состояние».

🧩 **Скрытые связи**
- Ловушка `Is you …?` теперь стоит на двух страницах интро подряд (2 и 3). Разборы разные, но если судья-дедупликатор сочтёт это повтором приёма — заменить одну на `Are you you cold?` нельзя (нелепица), лучше на `You are cold?` с разбором про порядок слов.

---

- Убрал из текста два утверждения, которые были неправдой: будто в английском вообще нет вежливых форм и будто короткое «Ok?» по-английски сказать нельзя.
- Убрал воду в третьем вступлении — там абзац пересказывал сам себя.
- Поправил три разбора звуков, где было написано неверно, где именно слова различаются.
- Заменил сцену с парнем и картой, потому что почти такая же была в соседней сессии.
- Слабые ловушки-нелепицы заменил на настоящую ошибку, которую живой человек правда делает.
- Всё остальное — сцена, 17 заданий, лучшие фразы — оставил как было.