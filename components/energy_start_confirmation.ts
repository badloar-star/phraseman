/**
 * Результат платного старта активности.
 *
 * зачем: владелец 2026-08-24 убрал окно с вопросом о трате энергии перед
 * стартом — цена видна прямо на кнопке знаком «−1 ⚡», а лишний тап мешал.
 * Тексты вопроса удалены вместе с окном, но сам тип остался: через него
 * единая точка списания
 * (confirmSpendOne/confirmSpendAmount в EnergyContext) сообщает экранам, чем
 * закончился старт — списали, безлимит или не хватило энергии.
 * Ветка 'cancelled' сохранена как часть контракта: экраны её обрабатывают.
 */
export type EnergyStartResult = 'spent' | 'unlimited' | 'cancelled' | 'insufficient';
