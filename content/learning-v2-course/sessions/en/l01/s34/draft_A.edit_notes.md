## Изменения

1. **Интро 3, ложное правило чтения (BLOCK судьи бреда).** Убрано «`ou` читается как в русском „оу“… не „лоуд“ … „лауд“» — текст спорил сам с собой и давал неверный факт. На месте — один ориентир без русской транскрипции: «гласная в середине — как в „ау!“, когда зовут через двор». Совпадает с подсказкой в задании 8.
2. **Интро 1, приписка-украшение (судья бреда).** «Тянется — и промахивается» удалено, ошибка названа прямо: «По-английски так нельзя». Заодно снят повтор «не `is`… `is` живёт при одном человеке» — правило сказано один раз.
3. **Интро 3, третий повтор правила про `are` (вода).** Абзац «И снова та же связка… держит фразу, как и с любым другим словом» удалён. Вместо него — свой угол страницы: `loud` работает не только про людей (музыка, телевизор, улица).
4. **Интро 2, пересказ правила вместо смысла (вода).** Убрано «Скрепка одна на всех — заучивать по слову не придётся». Добавлен факт о самом слове: назвать `funny` того, кто ведёт себя чудно, — и поймут наоборот. Это ровно то, что различает `funny` и «странный».
5. **Задание 2, пустой разбор.** «bus — „Автобус“. Оборван хвост» → названо, какой именно хвост («-зи», он в `busy` слышен ясно), как в эталоне.
6. **Задание 13, расхождение сцены и разбора.** Инструкция была «надели куртки, идём есть», а разбор ссылался на несуществующий «вопрос у двери». Сцена и оба разбора приведены к одной картинке: вы встали и оделись → `We are ready`.
7. **Возврат `together` без правильного ответа (REVISE педагога).** Добавлено задание 14 — вставь слово `We are together` (там `together` именно цель, а не отвлекающий вариант). Прежняя сборка сдвинулась на 15 и получила свою инструкцию («сами или со своими»), чтобы не дублировать 14. `we` теперь правильный ответ в 13, 14, 15 и 17; `together` — в 14, 15, 17.
8. **Длина интро.** Интро 1 — 329 → в норме, интро 3 — 317 → в норме: убраны повтор про `is` и третий пересказ правила, ничего нового на их место не дописано, кроме проверенного факта о слове.
9. **Строка `modes` пересобрана** под новое задание 14 (context_gap_grammar) и сдвиг сборки на 15.

Сохранено без изменений: все три `best_lines` судьи-редактора («рука тянется написать `They busy`», «`funny` — не „странные“, а те, с кем смеёшься», «`nice` — здороваются, придержат дверь, объяснят, где принтер»), сцена, число заданий (17), состав новых слов и доска пар.

---

🟡 **Стоит улучшить** — интро 2 теперь единственная страница с двумя новыми словами сразу; если владелец захочет ещё плотнее, `nice` можно вынести на карточку, а страницу отдать целиком `funny`.

🧩 **Скрытые связи** — правки задели строку `modes` для сборщика (добавлен режим, сдвинута нумерация) и таблицу самопроверки по возвращаемым словам; обе пересчитаны в этом же файле.

- Убрал главную ошибку: в объяснении было написано, что слово «loud» читается через «оу», хотя двумя строками ниже говорилось «лауд». Теперь там один понятный ориентир — как в возгласе «ау!».
- Вычеркнул три места, где одно и то же правило пересказывалось по второму и третьему разу. На освободившееся место не стал вписывать красивые фразы — только полезные факты о самих словах.
- Починил задание, где условие говорило одно, а разбор ошибки ссылался на другое.
- Добавил задание, чтобы слово «вместе» хотя бы раз было правильным ответом, а не только ложным вариантом.
- Две страницы объяснения были длиннее нормы — теперь укладываются, при этом шутка и смысл на месте.