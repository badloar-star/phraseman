## Изменения

- **Интро 1, ловушка `I am glad not` → `Me glad`** (must_fix редактора). Старый вариант — нелепица, которую никто не делает. `Me glad` — живой перенос: русское «мне радостно» и общая привычка начинать с «меня». Разбор не учит чужую форму, только возвращает к `I`.
- **Интро 3 переписано под другой угол** (must_fix редактора: «его место сразу после `am`» звучало трижды). Теперь страница не повторяет правило места, а показывает цену пропуска: `not` слышно, и без него фраза значит противоположное. Заголовок сменён на «Scared — одно слово решает всё».
- **Интро 3, вторая ловушка `I am scared not` → `I am scared`.** Новый угол требует ловушки на смысл, а не на порядок: ученик собирает грамматически верную фразу, но признаётся в обратном. Порядок `not` по-прежнему разбирается — в первой ловушке `I not scared`.
- **Задание 10: убрано `anymore`** (must_fix редактора — слово вне пройденного стояло в цели). Смысл «больше не блуждаю» перенесён в инструкцию сцены («наконец стоите у нужной двери»), цель стала `I am not lost.` Разбор `tired` подправлен под новую формулировку.
- **Добавлено задание 16 — `hungry` в контексте** (BLOCK педагога: `hungry` имел 1 касание). Живая сцена в лифте, ловушки `full`/`sure` работают на контраст с уже пройденным. Теперь `hungry`: касаний 3, правильным ответом 2.
- **`fine` добрано касанием** (BLOCK педагога): слово остаётся на доске 17 и раньше не имело второго касания — второе даёт разбор ловушки в задании 14? нет: `fine` теперь встречается на доске пар (17) и как отрабатываемое слово не годилось, поэтому вместо него в устный финал (18) добавлены `tired`, `not lost`, `hungry`, `glad`, а `fine` заменён на доске… — см. следующий пункт.
- **`fine` заменён на доске 17 на `sure`.** Честнее, чем таскать слово с одним касанием: `sure` отработан в заданиях 14 и 15 (3 касания), а `fine` в этой сессии не отрабатывается вовсе и на доске памяти был бы угадыванием. Доска 17: `hungry — голоден · tired — устал · done — закончил · sure — уверен`; пересечений с доской 12 нет. `fine` уходит из списка «Возвращаются».
- **Старое задание 17 (`scripted_repeat_compare` на две фразы) убрано** (must_fix редактора: «две фразы против четырёх в 18 и без ощущения финала»). Его роль забрал усиленный финал 18, куда добавлено отрицание, которого в устной части не было: `I am not lost`. Освободившийся слот отдан заданию с `hungry`. Число заданий — 18, как было.
- **Финал 18 привязан к сцене плотнее**: двери лифта открылись прямо здесь, а не в предыдущем задании; звонок домой убран, чтобы сцена не разъезжалась на две локации.
- **Задание 16 старой нумерации (Speed Match старых слов)** — жалоба ученика «голая таблица без привязки к лифту» снята: доска теперь стоит между живой сценой с голодом (16) и финалом у дверей (18), и все её слова только что прозвучали в сцене.
- `best_lines` редактора сохранены дословно: про «комплимент повару», про «четвёртый кусок», про «особенно лифт».

**Не исправлено:** ничего из `must_fix` не осталось.

---

🟡 **Стоит улучшить** — «Возвращаются» в шапке теперь: hungry, lost, tired, done, sure (без `fine`); если план курса жёстко требует шесть возвращаемых слов, `fine` придётся вернуть отдельным контекстным заданием, а не доской.

🧩 **Скрытые связи** — строка `modes:` пересчитана под новую нумерацию: `scripted_repeat_compare` теперь один (задание 18), `context_gap_grammar` — пять, `speed_match` — два. Сборщик читает эту строку буквально.

---

- Починил три придирки редактора: нелепую ловушку заменил на живую ошибку, убрал непройденное слово из правильного ответа, переписал третью страницу так, чтобы она не повторяла уже сказанное.
- Закрыл главную блокирующую претензию: слово «голоден» теперь по-настоящему отрабатывается в сценке с лифтом, а не мелькает один раз.
- Слово «хорошо» убрал с доски памяти и заменил на «уверен» — его в этой сессии реально тренируют, а «хорошо» пришлось бы угадывать.
- Скучное безликое повторение в конце убрал, а устный финал сделал длиннее и живее: четыре фразы прямо у открывшихся дверей лифта.
- Все удачные шутки, которые хвалил редактор, остались на месте.