## Изменения

**По судье бреда (BLOCK — правились первыми):**

1. **Задание 10** — `sit` и `seat` получали одинаковое объяснение через счёт слогов, которое их между собой не различало. Заменено на смысловой разбор: `sit` — действие «сидеть», `seat` — предмет «сиденье». Слоговой счёт убран совсем.
2. **Задание 8** — `flag` сравнивался с `flat` русской записью звука («на конце „г“ вместо „т“»). Заменено на смысл: «полотнище на палке, в нём не живут» — как в соседних заданиях 2 и 4.
3. **Интро 1, разбор `It new`** — объяснение про букву `i` подменяло настоящую причину ошибки. Теперь разбор говорит про связку: нужна либо полная форма, либо короткая.
4. **Повтор объяснения связки трижды подряд** — развёрнутое объяснение оставлено один раз (Интро 2), в Интро 1 и 3 конструкция не дублируется.

**По судье вкуса (REVISE):**

5. **Интро 3: заголовок обещал отрицание, вопрос требовал утверждения.** Вопрос переписан на отрицание: мама испугалась цены — успокойте. Верный ответ теперь `It isn't expensive`, ровно то, чему учит страница. Третий дистрактор заменён на `It not expensive` — реалистичный перенос из родного языка.
6. **`its` без значка убрано из текста Интро 1** — опоры на странице для него не было. Оставлено только в разборе варианта `Its new`, где оно реально возникает.
7. **Первое предложение про `job` убрано из Интро 1** (дословный повтор карточки 1). Слово теперь названо одной строкой в вопросе — там, где оно нужно.
8. **Абзац про `He isn't kind` убран из Интро 2** — он дублировал Интро 3 и уводил от темы страницы.
9. **`city` убрано из Интро 3** — было названо и брошено. Теперь вводится карточкой в практике.
10. **Юмор добавлен на все три страницы интро**, и растёт он из самого правила: язык экономит на `is` за вас (Интро 1); назовёте начальника `it` — выйдет «оно», как про стул (Интро 2); редкий случай, когда язык даёт выбрать (Интро 3).
11. **`city` получило фразу** — в задании 16 третья строка заменена на `The city is new`, теперь слово стоит в собранной речи, а не только в карточке и на слух.

**По педагогу (REVISE):**

12. **Длина интро приведена в норму:** 295/362/325 знаков → 268/263/279. Резались разгон и повторы, образы и шутки сохранены.

**Не исправлено (судья ошибся фактически):**

- Судья вкуса поставил в `must_fix` «убрать первое предложение про `job`, слово даст карточка» — но тогда вопрос страницы остался бы без опоры (машинная проверка требует, чтобы ключ ответа был объяснён на странице). Слово оставлено, но перенесено в строку вопроса и не дублирует карточку.

**Сохранено из `best_lines` без изменений:**
карточка `boss`, карточка `flat`, разбор `small` в задании 12.

---

🟡 **Стоит улучшить**
- `flat` и `city` перед доской пар имеют по одной отработке (слух), тогда как `job` и `boss` — по две. Формально правило соблюдено, но доска для них чуть жёстче.

🧩 **Скрытые связи**
- Задание 16 теперь содержит `The city is new` — артикль `the` в списке изученного есть (§«Слова»: артикль упомянут в Конституции как известное различие), но в фразовых сборках сессии он не участвует, только в проговаривании вслух.

---

**Итог простыми словами:**

- Убрал из объяснений про звучание слов всё, что было неправдой: раньше два похожих слова получали одинаковое объяснение, которое их не различало. Теперь разница объясняется через смысл — «сидеть» против «города», «флаг» против «квартиры».
- Третья страница обещала научить говорить «не», а спрашивала обратное. Переделал вопрос: теперь мама пугается цены, а вы её успокаиваете — и это ровно то, чему учит страница.
- Добавил шутки на все три страницы объяснений — раньше их там не было вообще, всё живое ушло в практику. Шутки про сам язык, а не приклеенные для красоты.
- Сократил объяснения примерно на четверть: убрал повторы одного и того же правила три раза подряд и разгон в начале абзацев.
- Слово «город» раньше называлось и бросалось — теперь у него есть карточка, звук и живая фраза.