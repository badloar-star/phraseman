## Изменения

**По судье бреда (BLOCK — первыми, без обсуждения):**

1. **Задание 2, разбор `cover`** — убрана неверная опора на слух («в середине там „в“, а в `corner` — „р“»). В `corner` отдельного согласного «р» на слух нет, ученик его не найдёт. Заменено на разбор по смыслу: «„Накрывать“. Из этой тройки про угол улицы только `corner`».
2. **Интро 1, самоотрицающая фраза** — «Не угол в комнате и не угол дома — … работает и там, и там» вычеркнута целиком. На её месте прямое утверждение: «И угол комнаты, и угол улицы, где одна упирается в другую».

**По судье вкуса (must_fix):**

3. **Интро 1** — 344 знака сжаты до нормы: убран повтор «место встречи двух улиц» (та же мысль уже сказана выше) и трёхшаговое перечисление порядка слов свёрнуто в одну фразу «`is` стоит вторым, сразу за `where`, и никуда оттуда не двигается».
4. **Интро 1 — шутка по теме** появилась в разборе неверного варианта `What is the corner?` («прохожий начнёт объяснять про две улицы») — она была и раньше, но теперь страница не выглядит справкой: правило дано в одну строку, а не абзацем-инструкцией.
5. **Интро 2** — снят дублирующий список переводов в конце абзаца (`near` — «рядом», `behind` — «за», `shops` — «магазины»): переводы уже стоят внутри самих примеров.
6. **Интро 3** — порядок восстановлен: сначала объяснено, из чего собирается ответ (`it is` + место), и только потом сказано про голое `near`. Готовый кусок ответа больше не выдан до объяснения.
7. **Безличные вопросы получили лица**: «Вам показали рукой» → «Женщина у витрины машет рукой»; «Вас спрашивают, где вокзал» → «Парень с рюкзаком тормозит вас».
8. **Задание 11** — подсказка ужесточена до «магазины видно, только когда перейдёшь мост», разбор `near` переписан под неё («тогда бы вы их увидели с этого берега»). Теперь `near` подстановкой не проходит.
9. **Задание 14** — `behind the corner` («за углом» — другое значение `behind`, чем отработанное в задании 8) заменено на `The shops are behind the station`. Значение одно и то же во всей сессии.

**Не тронуто:**
- Все три `best_lines` судьи вкуса сохранены дословно.
- Задание 4, разбор `bread`: судья бреда его не оспорил, слова действительно начинаются одинаково — формулировка уточнена («начинается так же, на „бр“»), чтобы ученик не искал разницу в начале.
- Число заданий 17, набор механик и доска пар не менялись.

---

🔴 **Требует немедленного вмешательства** — нет.

🟡 **Стоит улучшить**
- Задание 12: лишняя плитка `it` — слабая обманка, ученик её не тронет, потому что фраза начинается с `Where`. Реалистичнее была бы `are` (перенос «где улицы?»), но это меняло бы смысл задания — оставлено как есть.

🧩 **Скрытые связи**
- Задание 14 теперь повторяет пару `shops`/`station` из задания 8 в другой роли — счётчик касаний `station` вырос с 9 до 11, `shops` остался на 11. Оба всё ещё в норме.
- `corner` в практике потерял одно касание (задание 14), но остаётся с карточкой, слухом, выбором и доской пар — минимум по Конституции перекрыт.

---

**Итог простыми словами:**
- Убрал из занятия две неправды: объяснение звука в слове «угол», которое ученик на слух проверить не смог бы, и фразу, которая сама себе противоречила в одном предложении.
- Первую страницу сократил до нормальной длины: выкинул повтор одной и той же мысли и длинную инструкцию про порядок слов.
- На третьей странице переставил объяснение по порядку — сначала как строится ответ, потом почему коротко отвечать некрасиво.
- В вопросах появились живые люди вместо безличного «вам показали» и «вас спрашивают».
- Починил два упражнения: в одном подходили сразу два ответа — теперь подсказка допускает только один; во втором слово «за» использовалось в другом смысле, чем учили выше — заменил пример.