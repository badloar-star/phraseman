## Изменения

1. **BLOCK судьи бреда, задание 2 (`called`).** Строка описывала различие «`cr` против `ca`», которого на слух нет, и спорила с собственным началом. Заменено фактом: первый звук общий, дальше в `crowded` идёт «р», в `called` сразу гласная.
2. **BLOCK судьи бреда, задание 12 (`cloudy`).** Отсылка к буквам («после первой буквы идёт `l`») в звуковом задании: ученик слов не видит. Переписано на то, что он слышит: «После первого звука в `cloudy` слышно „л“, в `crowded` — „р“». Тот же приём применён к `cloudy` в задании 2 — там была та же болезнь в мягкой форме.
3. **REVISE редактора, Интро 1 — нет шутки.** Сухое «Порядок жёсткий. Поменяли местами — получилось сообщение» заменено на объяснение с наглядным последствием: перестановка двух слов превращает вопрос в сообщение, и это показано целой фразой `It is cold.` Шутка растёт из самого правила, ничего постороннего не добавлено.
4. **REVISE редактора, Интро 2 — нет шутки.** Абзац про хвост `-ed` переписан: теперь он показывает, что получится при ошибке (`Is it crowd?` — «толпа ли это место»). Соль в самом слове, не приклеена.
5. **REVISE редактора, Интро 3 — абзац-повтор.** Строка «Одно про уши, другое про спину — и `is it` перед обоими одинаковое» удалена целиком. На её место ничего не вписано (правило: бред и вода вычёркиваются, а не заменяются).
6. **Замечание ученика и машинных фактов: `cold` шёл к доске пар без отработки в этой сессии.** Добавлено задание 8 — `listen_choose` на `cold` (дистракторы `called`, `gold`, оба разобраны по слуху корректно). Доска пар сдвинулась на 9, нумерация ниже пересчитана, строка `modes` обновлена, таблицы самопроверки пересчитаны. Заданий по-прежнему 17.
7. **`best_lines` редактора сохранены дословно** — все три строки на месте.

🟡 **Стоит улучшить**
- В интро 1 теперь два предложения подряд про порядок слов — при следующем сжатии первым кандидатом на подрезку будет фраза «Порядок тут решает всё».

🧩 **Скрытые связи**
- Сдвиг нумерации задел строку `modes` для сборщика и обе таблицы самопроверки — пересчитаны вручную, машинные факты по числу заданий (17) не изменились.
- `check_new_words.mjs` не затронут: строки «Новые слова» и «Возвращаются» не менялись.

---

**Итог простыми словами:**

- Убрал две неправды про звучание слов: раньше ученику предлагали услышать разницу, которой нет, и смотреть на буквы в задании, где слов не видно. Теперь обе подсказки говорят про то, что реально слышно.
- Добавил юмор на две первые страницы объяснения — не приклеенный, а из самого материала: показал, что будет, если переставить слова местами или потерять хвост у слова.
- Вычеркнул лишнюю строчку на третьей странице, которая повторяла уже сказанное. Ничего взамен не вписал.
- Добавил одно задание на слух со словом «холодно», чтобы перед игрой «соедини пары» человек это слово свежо услышал, а не вспоминал с прошлого занятия. Заданий осталось 17, номера ниже сдвинулись, все служебные списки пересчитал.
- Хорошие строки, которые судья отметил как удачные, оставил слово в слово.