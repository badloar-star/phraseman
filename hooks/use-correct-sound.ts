import { useCallback } from 'react';

/**
 * Совместимость для экранов, которые ещё зовут звук верного ответа.
 *
 * зачем (решение владельца, подтверждено 2026-09-15): ответы озвучиваются
 * ТОЛЬКО вибрацией, без звука. Решение уже было принято раньше и охраняется
 * контрактом `tests/learning_verdict_sounds_removed_contract.mjs`, но звук
 * успел вернуться в обход него и жил на трёх экранах (диагностика, карточки,
 * персональный план) — контракт был красным и охранял отменённое правило.
 *
 * Хук намеренно сохраняет свой интерфейс и остаётся пустым: у него
 * одиннадцать точек вызова, и переписывать их значило бы трогать логику
 * ответов, начисление опыта и вибрацию ради удаления звука. Вибрация на этих
 * экранах вызывается отдельно и не затронута.
 */
export function useCorrectSound() {
  const playCorrect = useCallback((): void => {
    // Намеренно тихо: вердикт ответа выражается вибрацией.
  }, []);

  return { playCorrect };
}
