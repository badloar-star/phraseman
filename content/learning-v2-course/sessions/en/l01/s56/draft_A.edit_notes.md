## Изменения

**По судье бреда (BLOCK — правились первыми):**

1. **Интро 1, «`am` держится за `I` и никуда от него не отходит»** — утверждение перенесено в Интро 3 и переписано на проверяемое: «скрепку `am` берут только с `I`, с другими словами она не идёт». Не обещает жёсткого соседства, поэтому `Am I late?` его не опровергает.
2. **Интро 1, «неделя длинная и как будто множественная»** — придуманная связь удалена. Осталось «`are` берут, когда речь о нескольких. Неделя одна». Строку из `best_lines` («даже если по ощущениям их было три») сохранил, но привязал к числу, а не к длительности.
3. **Интро 2, «спотыкаются даже те, кто говорит давно»** — вычеркнуто. Осталось рабочее: людей много, рука тянется к `are`, но английский считает по одному.
4. **Интро 3, «на отрицании `am` теряется чаще всего»** — абзац удалён целиком, новый на его место не написан.
5. **Задание 2, омофон `weak`** — убран из вариантов, заменён на `walk`. На слух `week`/`weak` неразличимы, задание не имело верного ответа.
6. **Задание 11, разбор `worm`** — русская транскрипция и ссылка на букву убраны. Различие даётся по смыслу: `worm` — «червяк», про квартиру и погоду говорят `warm`.
7. **Задание 7, «квартира добрая не бывает»** — заменено на «`kind` — про характер человека, не про комнату». Безусловного запрета нет.

**По судье вкуса (REVISE):**

8. **Длина** — все три страницы в норме 240–290. Резались повторы разбора ошибок, а не образы.
9. **Заголовок Интро 1** обещал слово «неделя», а страница была про `it is`. Теперь заголовок «Неделя — это `it`», и `week` реально работает в тексте: `The week is hard` → `It is hard`.
10. **`hard`** переведён прямо в тексте страницы, а не впервые в правильном ответе.
11. **Шутка на каждой странице, из правила:** Интро 1 — «даже если по ощущениям их было три»; Интро 2 — «„все“ в этом слове только в переводе»; Интро 3 — «руку тянет выбросить лишнее слово».
12. **«скрепку не выбрасывают»** (канцелярская безличность) → «Скрепка остаётся на месте».
13. **Однотипные механики.** Задание 13 было вторым подряд «вставьте скрепку» (дублировало 5) — заменено на сборку `It is not hard` с ловушкой `no`. Теперь `context_gap_grammar` — 5 штук, `phrase_builder` — 5.

**Не исправлено (судья ошибся):**

- Судья вкуса требовал убрать тему `not` из Интро 2. Она там осталась в дистракторе `Everyone is not kind` — но это разбор ошибки смысла («не все добрые»), а не второе правило: правило `not` живёт только в Интро 3. Убирать дистрактор нельзя, он ловит подмену смысла.

---

🟡 **Стоит улучшить**
- Интро 2 держится на одной мысли («everyone = один»), из-за чего страница самая короткая по содержанию. Если сессия покажется лёгкой — туда можно добавить второй пример с `nobody`, но слово должно уже быть отработано.

🧩 **Скрытые связи**
- Задание 13 добавило пятый `phrase_builder`; строка `modes` для сборщика обновлена — без неё сборка развалится.
- Замена `weak` → `walk` в задании 2 трогает озвучку: нужен новый аудио-вариант дистрактора.

---

- Убрал из объяснений всё, что было неправдой про язык: неверное правило про «am всегда рядом с I», выдуманную связь между длиной недели и множественным числом, категоричное «квартира доброй не бывает».
- Выбросил задание, где на слух предлагалось отличить два слова, которые звучат абсолютно одинаково, — заменил на нормальную пару.
- Сократил все три страницы объяснения примерно на треть: убрал куски, которые повторяли то же самое двумя строчками ниже.
- Заголовок первой страницы обещал «неделю», а страница была про другое — теперь слово «неделя» в тексте реально работает.
- На каждой из трёх страниц теперь есть живая фраза с юмором, вырастающая из самого правила, а не приклеенная в конце.
- Одно однотипное задание заменил на другое, чтобы шесть похожих упражнений не шли подряд.