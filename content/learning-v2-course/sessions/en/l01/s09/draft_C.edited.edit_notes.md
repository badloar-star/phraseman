## Изменения

- **Интро 3 переписано под одно слово** (`must_fix` редактора): убраны `kind` и «Кстати, `right`…» — страница теперь один угол вокруг `funny` («комплимент, который не звучит как флирт»). Оба слова и так входят в сессию через карточки (3, 7) и отработку (5, 6, 13). Шутка про «невинные лица англичан» из `best_lines` сохранена дословно.
- **Интро 3, ловушки перестроены на операцию**: вместо смысловой `You are kind` теперь перенос из родного языка `You funny` («скрепку съели») — обе ловушки бьют по `are`, как в эталоне s03.
- **Задание 12**: убрана лишняя плитка `am` — диктант её не оправдывал, роль обманки честно несёт задание 15.
- **Задание 16 перестроено** (BLOCK-причина педагога: `sorry` ни разу не правильный ответ): вместо `You are early` — сцена с пролитым кофе и правильным ответом `sorry`. Слово получило касание правильным ответом; `early` и `new` остались дистракторами со своими разборами и сохранили по касанию.
- **Задание 17 расширено до четырёх реплик**: добавлена `You are kind` — раз `kind` ушёл из интро 3, он закрывает финал и связывает новую операцию с благодарностью за кофе.
- Сцена, порядок механик, число заданий (17) и доска пар (все четыре слова к моменту доски прошли карточку и отработку) не тронуты.

---

- Убрал перегруз третьей страницы вступления: там теперь одно слово и один образ, а не список из трёх.
- Ловушки на этой странице теперь проверяют именно новое правило («ты смешная» без связки), а не значение слова.
- Починил слово «sorry»: раньше оно нигде не было верным ответом, теперь есть живая сценка с пролитым кофе, где его надо выбрать.
- Убрал лишнюю плитку из задания на диктант — обманка осталась только там, где её оправдывает сцена.
- Финал сессии стал из четырёх фраз: добавилось «ты добрая», чтобы слово не потерялось после правки вступления.