---
description: DEPRECATED — PhraseCard (correct/wrong/secret по фразам) удалены из приложения
globs: docs/pipelines/lesson-cards-generation.ru.md
alwaysApply: false
---

# C2-RU — отключено

Конвейер **PhraseCard** и файл **`app/lesson_cards_data.ts`** удалены. Правки русских подсказок после ответа в упражнении фраз — только через **`lesson_data_*`** / **`lesson_help`** / тексты в **`lesson1`**, не через отдельное дерево карточек.

Для машинного **`dataId`** репортов по фразе см. **`lessonPhraseReportDataId`** в `app/error_report.ts`.
