# Выбор из экспортов редактора — 12 сентября 2026

Финальное owner approval после просмотра: все выбранные поверхности подтверждены;
осталась одна UI-правка ленты CEFR. Приёмка: кнопки по содержимому (минимум
64 × 46), без растягивающего flex, горизонтальная прокрутка при переполнении,
действующие переходы к A1/A2/B1 сохранены. Учебный контент не расширяется.

Восемь файлов сверены по времени и SHA-256. Последний содержит все восемь
комментариев предыдущих экспортов; расхождений текста нет. Receipt:
`docs/v2/mockups/2026-09-12-five-directions/owner-approved.json`.

1. Подключить Pulse course к существующим props/callbacks каталога. Все 32 урока
   доступны скроллом; A1/A2/B1 переводят к разделам. Прогресс без произвольных цифр.
2. Карта: все 56 сессий, current центрируется после layout, узлы 101 / шаг 128,
   анимация останавливается вне видимой области и при reduced motion.
3. Модал Pulse, три интро Pulse, карточка Panorama и карман Pulse. Сохранить
   authored copy, доступ, once-only callbacks, first-frame unlock и card flight.
4. Обновить редактируемый макет выбранным пресетом. Фиксировать результаты
   узких тестов и независимого review; device QA не подменять статическими gates.

Геометрия берётся из экспортов, тема — из useTheme. UI-only: не менять release,
баланс, списание энергии, прогрессию, Firestore или остальные разделы приложения.
Не использовать старую lesson/[id] как источник all-course progress: там 12 live
sessions и legacy lesson-1 scope. Основная граница — Horizons в lessons.tsx.
