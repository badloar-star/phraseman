## Изменения

- **Интро 3 переписано под `done`** (машинный факт: ключевое слово не упоминалось в тексте; педагог — REVISE). Теперь страница прямо про `done` — «сказали `I am done` — и планшет закрывается», а мысль «не приклеивай `not` из вежливости» осталась и получила образ, который просил редактор вкуса: человек, отвечающий на всё `I am not`, выглядит так, будто первый день ему не понравился.
- **Убрано пересечение с l01_s07** (педагог, cross_session_repeats): лифт исчез из сцены — вместо него остывший кофе и придержанная дверь. Из интро 1 убран образ «`am` осталась в лифте / потерялась в дороге», который шёл третью сессию подряд; вместо него причина, а не картинка транспорта: «чем быстрее человек говорит, тем чаще роняет именно эту скрепку — она короткая и незаметная». В задании 17 «лифт держат» заменено на «дверь придержали».
- **Убрана фраза «Тут нечего вспоминать»** (редактор вкуса, must_fix 3) — обещание лёгкости перед контрольной; абзац стал плотнее.
- **Снято задание `done` / `down`** (педагог: `down` вне границы; редактор вкуса, must_fix 1). Вместо него — `listen_choose` на `free` с дистракторами `three`/`fry`: оба разбираются только как звук, ничего нового не учат. Это же закрыло претензию «у `free` ни одной сборки»: задание 16 переехало с `new` на `free`, теперь у `free` слух + выбор + сборка.
- **`new` не потерялся**: остаётся в задании 1 (дистрактор с разбором), 4 (правильный ответ, скрепка) и 14 (доска пар) — два касания с разбором и одно правильным ответом, норма §4 выдержана.
- **Дублирование интро 1 ↔ 2 снято** (ученик: «почти дословный повтор»). Интро 1 больше не объясняет позицию `not` в тексте — эта мысль осталась только в разборе ошибки; целиком про `not` теперь одна страница, вторая.
- **Всё из `best_lines` сохранено дословно**: «Про себя — всегда `I am`» (в новой оправе), «Как охранник — вплотную за `am`, никуда не отходит», строка про `I am hungry` — здесь единственное отступление: она жила в старом интро 3, которое пришлось переписать под `done`; её мысль («не отрицай из скромности») перенесена в новую формулировку про «первый день не понравился», а само слово `hungry` продолжает работать в заданиях 5, 7 и 17.
- **Порядок и число заданий**: 17, механики только из разрешённых семи, доски пар (6 и 14) не пересекаются, каждое слово доски отработано раньше — `free` теперь тоже (задание 9 идёт до доски 14).

🟡 **Стоит улучшить** — единственная неполная замена: строка про `I am hungry` из `best_lines` не сохранена дословно, потому что её страница целиком противоречила машинному факту (интро 3 обязано было объяснять `done`). Если такая потеря нежелательна, альтернатива — вернуть вопрос интро 3 к `I am hungry` вместо `I am done`, но тогда `done` останется без объяснения в интро.

🧩 **Скрытые связи** — касания слов после правки: `ready` 1/11/17, `tired` 2/3/15, `lost` 1/12/17, `sure` 8/14, `hungry` 5/7/17, `free` 9/10/14/16, `done` 7/11/13/14/17, `new` 1/4/14. Все восемь — минимум два касания, каждое хотя бы раз правильным ответом.

- Переписал третью вступительную страницу: теперь она объясняет именно то слово, которое ученик должен ответить.
- Убрал лифт из сцены и образ «связка потерялась в дороге» — они уже были в двух соседних сессиях подряд.
- Заменил задание на слух со словом, которого ученик не проходил, на задание с уже знакомым словом.
- Добавил этому знакомому слову полноценную отработку: на слух, выбором и сборкой фразы.
- Убрал повтор одной и той же мысли на двух вступительных страницах и лишнюю фразу, обещавшую, что будет легко.
- Заданий по-прежнему семнадцать, все слова возвращаются по два-три раза, доски на скорость не пересекаются.