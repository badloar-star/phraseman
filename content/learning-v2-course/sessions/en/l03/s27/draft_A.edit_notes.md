## Изменения

**По судье бреда (BLOCK, правится первым):**
1. **Интро 3** — убрана выдуманная причина «Кнопку нажимают, выключатель переключают — поэтому по-английски это разные слова». Осталось только различие двух предметов, без «поэтому».
2. **Задание 4** — дистрактор `bottom` убран: округление гласной верно лишь для британского варианта и на слух не проверяется. Поставлен `bottle` — различие `n` / `l` в середине слышно в любом произношении.
3. **Задание 9 (было 10)** — разбор `Are` переписан. «`Are` идёт с несколькими предметами» — неправда: `Are you ready?` ученик уже знает. Теперь привязано к слову: «После `this` стоит `is`».

**По судье вкуса:**
4. **Задание 12 (было 13)** — из инструкции убрано слово «лампа», ответ больше не назван по-русски. Вместо него примета: «круглое, стеклянное, светит».
5. **Задание 16** — убрано «вы спрашиваете про дверь». Вместо него примета: «толкает деревянную створку в стене, она не поддаётся».
6. **Интро 3, вторая ловушка** — `Is that switch a?` (нелепица, так не ошибаются) заменена на `That is a switch?` — реалистичный перенос порядка слов из родного языка.
7. **Интро 2, разбор пропущенного `a`** — переписан, чтобы не дублировать интро 1 слово в слово: теперь про то, что `a` стоит перед вещью каждый раз, с тремя примерами.
8. **Интро 1** — убран штамп «Дешевле не бывает», на его место встал факт: в вопросе не меняется ни одно слово, кроме порядка.

**По судье-ученику и педагогу:**
9. **Доска пар перенесена** с позиции 9 на позицию 13 — теперь `lamp` получает перевод и отработку (задание 12) ДО доски. Это то, что автор признал в самопроверке и не исправил.
10. **Интро 3, сцена** — «Сосед трогает что-то у двери» противоречило правилу `this`/`that` (потрогал — значит рядом). Теперь «показывает на стену у самой двери»: предмет на расстоянии, ключ `that` верен.
11. **Задание 14** — добавлен перевод `door` прямо в задании: ученик видит слово раньше, чем сборку 16.

**По машинным фактам:**
12. **Все три интро добрали длину** до нормы (было 205 / 181 / 187 при норме 240–290) — дописан факт о самом языке, не вода: что `is` выходит вперёд и больше ничего не меняется (интро 1), что меняется только `this`/`that`, а `is` и `a` стоят на местах (интро 2), чем `button` отличается от `switch` (интро 3).
13. **Однообразные сборки** — из пяти сборок три были `Is this a ___?`. Теперь чередование `this` (3) / `that` (2) сохранено, но каждая сборка отличается набором плиток: 4 плитки без ловушки → 5 с ловушкой `this` → 5 с ловушкой `that` → 5 с ловушкой `button` → 5 с ловушкой `switch`. Глубже развести нельзя: сессия учит ровно одну конструкцию `Is this/that a…?`, а отрицания и других лиц в этой теме нет.

---

🔴 **Требует немедленного вмешательства**
- Нет. Оба неверных факта о языке (про гласную в `bottom` и про `are` для нескольких предметов) устранены — BLOCK снят.

🟡 **Стоит улучшить**
- Пункт 13: однообразие сборок машинно всё ещё может подсветиться — конструкция в теме одна. Если сторож блокирует, единственный честный выход — заменить одну сборку на `context_gap_grammar`, но тогда сборок станет четыре (норма §11 — минимум четыре, всё ещё проходит).
- Слово `door` в этой сессии живёт только в задании 14 и 16; третье касание не помешает, но в 17 заданий оно уже не влезает без выкидывания нужного.

🧩 **Скрытые связи**
- Перенос доски пар сдвинул нумерацию заданий 9–13 — строка `modes` для сборщика пересобрана целиком, иначе сборщик поставил бы `speed_match` на место `context_gap_grammar`.
- Замена `bottom` → `bottle` затрагивает звуковой набор: `bottle` в курсе уже встречалось как слово, поэтому дистрактор не вводит новую лексику.

---

**Итог простыми словами:**

- Убрал из урока три неправды про английский: про то, как звучит гласная в одном слове, про слово «are», и выдуманную причину, почему кнопка и выключатель называются по-разному.
- В двух заданиях ответ был написан по-русски прямо в вопросе — выбирать было нечего. Заменил на приметы: «круглое, стеклянное, светит» вместо «лампа».
- Слово «лампа» встречалось в задании на пары раньше, чем его вообще переводили. Поменял задания местами — теперь перевод идёт первым.
- Поправил сценку, где сосед трогал рукой то, на что по правилу нужно было показывать издалека.
- Три вступительные страницы были слишком короткими — дописал по факту про сам язык, без воды.
- Одну нелепую ошибку-ловушку, которую живой человек не сделает, заменил на настоящую — ту, что делают русскоязычные.