## Изменения

**По судье бреда (BLOCK, правится первым):**
1. Задание 2, `pilot` — строка обещала разобрать первый слог, а давала транскрипцию `polite`. Теперь названо ударение самого `pilot`: PIE-lot против po-LITE.
2. Задание 4, `series` — убрано неверное «похоже началом» (начала совпадают, а не похожи) и исправлено число слогов: SI-riz — два, а не три.
3. Задание 4, `service` — слова разведены по гласной первого слога («сёр» против «си»), а не только по счёту слогов: путают их именно на гласной.
4. Задание 8, `lock` — было «гласная другая: LAK, а не LA-ки», то есть одна и та же буква в обоих словах. Теперь сказано прямо: в `lock` гласная ближе к «о», в `lucky` — к «а».
5. Задание 10, `said` — задание на слух, а разбор объяснял только значение. Добавлен звук: в `said` гласная как в «мэр», в `sad` рот шире.

**По судье-ученику (BLOCK):**
6. `calm`, `honest`, `kind` были правильными ответами, но в сессии не вводились ничем — только угадывание. Каждому дана карточка перед его заданием: 14 (calm), 16 (honest), 18 (kind). `tired` разбирается в задании 5 как ловушка с переводом и потом идёт в сборку 19 — этого достаточно.
7. Задание 20 (голос) теперь опирается только на слова, объяснённые внутри сессии.

**По редактору вкуса (REVISE):**
8. Интро 3 переписано: убрано «Радуйтесь: новых правил учить не надо, всё уже знакомо» (отсылка к пройденному), страница отдана одному слову `lucky`, добавлен смысл, ради которого она нужна — похвала относится к дню, а не к человеку. `sad` перенесён в практику (карточка 9 + слух 10 + диктант 13), где получает больше внимания, чем строчкой в интро.
9. Интро 3, ловушка `You are sad` заменена: она проверяла память о реплике, а не язык. Теперь `You is lucky` и `You lucky are` — обе ошибки языковые.
10. Интро 2: «куда бы вас ни занесло» вычеркнуто, вместо штампа — сам порядок слов.
11. Интро 1: убрано «Не „добрый“, а именно тот, кто…» — объяснение через отрицание того, чего никто не думал. Начинается сразу с того, чем `polite` является.
12. Интро 1: «Скрепка меняется, слово остаётся» → «Меняется только `am` на `are`, само слово одно и то же».
13. Задание 5, разбор `tired` — убрано уклончивое «может, и так, но…».
14. Задание 12, разбор `is` — убрана приписка «и точка».
15. Две одинаковые механики подряд разведены: бывшие 14 и 15 теперь 15 (вставь слово) и 17 (сборка фразы), между ними карточка.

**По педагогу (REVISE):**
16. Задание 15 переписано: было «коллега весь день говорил ровным голосом» — почти дословный повтор сессии 12. Теперь другая ситуация: на человека накричали в трубку, а он ответил ровно.

**Служебное:**
17. Блок «Самопроверка» с двумя версиями заданий убран из сдаваемого текста — сборщик брал бы старую редакцию. Строка `modes` пересобрана под итоговую нумерацию, добавлен блок `review_cards` для трёх карточек повторяемых слов.

**Не исправлено:**
- Замечание судьи-ученика на лишние плитки-ловушки (`is` в 17, `am` в 19) не принято: Конституция §3 такие ловушки требует прямо, и в разборах чужая форма не объясняется.

**Машинные факты после правки:** заданий 20 (было 17, минимум 12 — соблюдён). У каждого возвращаемого слова карточка и минимум два касания: calm — 14, 15, 20; honest — 15 (ловушка), 16, 17; kind — 18, 19; tired — 5 (ловушка), 19. Доска пар (11) стоит после карточки и слуха для всех четырёх слов. Каждое интро проверяет то слово, которое объяснено над вопросом.

---

🟡 **Стоит улучшить** — заданий стало 20 вместо ориентира 17; три карточки повторяемых слов удлиняют сессию. Если владелец хочет держаться ближе к 17, можно объединить `honest` и `kind` в одну доску повторения вместо двух отдельных карточек.

🧩 **Скрытые связи** — блок «Для сборщика» получил новую секцию `review_cards`: если сборщик её не поддерживает, три карточки повторения придётся класть в `new_words`, и тогда счётчик новых слов покажет семь вместо четырёх (нарушение §3 формально, по факту — нет).

- Починил главное, за что сессию блокировали: подсказки к заданиям «послушай и выбери» врали о звуках — в одном месте два разных слова были записаны одинаковой гласной, в другом вместо разбора неверного ответа стояло произношение верного. Теперь везде сказано, чем слова реально отличаются на слух.
- Убрал нечестные задания: три слова требовались как правильный ответ, но нигде в уроке не объяснялись. Каждому дал карточку с человеческим определением перед тем заданием, где оно нужно.
- Переписал третью страницу объяснения: там не было шутки, была отсылка «вы это уже проходили» (запрещена — человек мог открыть урок первым) и два слова на одной странице, из-за чего второе оставалось голым переводом.
- Заменил ловушку, которая проверяла память о реплике собеседника, а не знание языка.
- Поменял ситуацию в одном задании — она почти дословно повторяла соседний урок.
- Выбросил из файла черновой блок с двумя версиями трёх заданий: сборщик взял бы неправильную.