import { MONEY_OPERATION_CAPABILITIES } from './admin-money-operations-state.js';

const LABELS = Object.freeze({
  'ugc-purchases': 'Покупки UGC', refunds: 'Возвраты провайдеров', referrals: 'Рефералы',
  'telegram-payments': 'Telegram-оплаты', 'website-payments': 'Веб-оплаты',
});
function json(value) { try { return JSON.stringify(value, null, 2); } catch { return '{}'; } }

export function renderMoneyOperationsWorkspace(model, { escapeHtml, can }) {
  const workspace = model.workspace; const items = Array.isArray(workspace?.items) ? workspace.items : [];
  const tabs = MONEY_OPERATION_CAPABILITIES.map((id) => `<button class="button ${model.capabilityId === id ? 'primary' : ''}" data-money-action="select" data-capability="${id}" type="button">${LABELS[id]}</button>`).join('');
  const source = workspace?.sourceHealth || [];
  const rows = items.length ? items.map((item) => `<tr><td data-label="ID"><button class="link-button" data-money-action="detail" data-id="${escapeHtml(item.id)}" type="button">${escapeHtml(item.id)}</button></td><td data-label="Статус">${escapeHtml(item.status || item.eventType || '—')}</td><td data-label="Версия"><code>${escapeHtml(String(item.version || '').slice(0, 12))}</code></td></tr>`).join('') : '<tr><td colspan="3">Нет записей в ограниченной серверной выборке.</td></tr>';
  const providerNote = model.capabilityId === 'refunds' ? '<div class="notice">provider-owned · Возвраты App Store / RevenueCat доступны только для просмотра. Оформляйте возврат в кабинете провайдера.</div>' : '';
  const preview = model.preview ? `<section class="card section"><div class="card-header"><div><h2>Серверный preview</h2><p>Денежное изменение требует одобрения другого администратора.</p></div><span class="badge warning">money.approve</span></div><div class="card-body"><pre class="code-preview">${escapeHtml(json(model.preview))}</pre><div class="field"><label for="money-confirmation">Точное подтверждение</label><input id="money-confirmation" placeholder="${escapeHtml(model.preview.confirmation)}" autocomplete="off"></div><div class="actions"><button class="button" data-money-action="discard" type="button">Отмена</button><button class="button primary" data-money-action="request-approval" type="button">Запросить одобрение</button><button class="button danger" data-money-action="apply" type="button">Применить одобренное</button></div><p>${escapeHtml(model.approvalStatus || '')}</p></div></section>` : '';
  return `<header class="page-header"><div><p class="eyebrow">Деньги / Нативные операции</p><h1>Операции с оплатами</h1><p>Ограниченные серверные выборки, отдельный preview, второй администратор и единый аудит.</p></div><button class="button" data-money-action="refresh" type="button">Обновить</button></header>
    <select class="mobile-view-select" data-money-mobile-view aria-label="Раздел денежных операций">${MONEY_OPERATION_CAPABILITIES.map((id) => `<option value="${id}"${id === model.capabilityId ? ' selected' : ''}>${LABELS[id]}</option>`).join('')}</select><div class="actions native-operation-tabs">${tabs}</div>
    ${model.error ? `<div class="notice danger">${escapeHtml(model.error)}</div>` : ''}${providerNote}
    <section class="card section"><div class="card-header"><div><h2>${LABELS[model.capabilityId]}</h2><p>Право чтения: <code>money.read</code>. Источник и усечение показаны явно.</p></div><span class="badge">${workspace?.truncated ? 'truncated' : 'bounded'}</span></div><div class="card-body"><pre class="source-health">${escapeHtml(json(source))}</pre><div class="table-wrap"><table><thead><tr><th>ID</th><th>Статус</th><th>Версия</th></tr></thead><tbody>${rows}</tbody></table></div>${model.detail ? `<details open><summary>Деталь</summary><pre class="code-preview">${escapeHtml(json(model.detail))}</pre></details>` : ''}</div></section>
    <section class="card section"><div class="card-header"><div><h2>Подготовить изменение</h2><p>Доступны только серверные действия с reason, confirmation, idempotencyKey и expectedVersion.</p></div></div><div class="card-body"><div class="report-filters"><div class="field"><label for="money-mutation-action">Действие</label><select id="money-mutation-action"><option value="ugc-refund">UGC refund · money.refunds.write</option><option value="provider-refund">Provider refund · provider-owned</option><option value="referral-status">Referral status · money.payment_orders.write</option><option value="telegram-activate">Telegram activate · money.payment_orders.write</option><option value="web-order-close">Web order close · money.payment_orders.write</option><option value="web-checkout-config">Checkout config · money.payment_config.write</option></select></div><div class="field"><label for="money-target-id">Target ID</label><input id="money-target-id"></div><div class="field"><label for="money-expected-version">Expected version</label><input id="money-expected-version"></div></div><div class="field"><label for="money-payload">Payload JSON</label><textarea id="money-payload">{}</textarea></div><div class="field"><label for="money-reason">Причина</label><textarea id="money-reason" maxlength="500"></textarea></div><button class="button primary" data-money-action="preview" type="button"${can('money.refunds.write') || can('money.payment_orders.write') || can('money.payment_config.write') ? '' : ' disabled'}>Подготовить preview</button></div></section>
    <section class="card section"><div class="card-header"><div><h2>Одобрить чужой запрос</h2><p>Автор запроса не может быть одобряющим.</p></div></div><div class="card-body"><div class="field"><label for="money-approval-preview-id">Preview ID</label><input id="money-approval-preview-id"></div><div class="field"><label for="money-approval-reason">Что проверено</label><input id="money-approval-reason"></div><button class="button" data-money-action="approve" type="button"${can('money.approve') ? '' : ' disabled'}>Одобрить</button></div></section>${preview}`;
}
