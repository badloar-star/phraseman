## Изменения

1. **Интро 1, «`is` теряется первым» → «и `is` пропадает чаще всего».** Единственная претензия, подтверждённая ДВУМЯ судьями (бреда и вкуса). Слово «первым» обещало продолжение списка, которого на странице нет — ровно тот дефект, за который заблокирована сессия 29.

2. **Интро 2 переписано целиком** (судья вкуса: «сухая справка»). Убран порядок «слово — перевод — правило»: теперь страница начинается с самой причины выбора — рука не дотягивается. Развит `best_line` «Пальцем показываете одинаково, слово меняете» — он не удалён, а поставлен на место главной мысли абзаца. Заодно снято безусловное «Разница только в шагах», о котором предупреждал судья бреда: близость у `this`/`that` бывает не только в пространстве (Кембридж, `this morning`).

3. **Интро 3 переписано**: вместо пересказа формулы — что будет, если ошибиться. «Ошибётесь словом — получите не тот кошелёк» растёт из самого выбора между двумя словами, а не приклеено к абзацу. Порядок подачи сохранён: `wallet` назван и переведён до того, как войти во фразу.

4. **Задание 14 (было 13), инструкция.** «Какое слово начинается с языка между зубами?» выдавало ответ до записи: `cat` ученик знает, значит выбирал глазами. Теперь «какое слово прозвучало первым?», а признак остался в разборе `cat` — как требует решение владельца 08.09 о том, что инструкция не должна раскрывать ответ.

5. **Задание 2, разбор `sunshine`.** Прежний признак («начинается с „са“, а `suitcase` — с „су“») описывал первый звук `s` у обоих слов, то есть строил различие на общем признаке — запрет свода. Оба дистрактора переведены на одну проверяемую ось: одинаковое начало `s`, разная гласная.

6. **`phone` получил три касания вместо одного и дважды стал правильным ответом.** Это признавала сама самопроверка предыдущей версии («ни разу правильным ответом»). Задание про телефон поднято на позицию 10 — до доски пар, чтобы слово было отработано перед проверкой памяти; в доску пар `phone` встал вместо `key`, а `key` остался в сборке 13. Прежняя сборка 16 (`Give me that backpack`) заменена на `Give me that phone`: `backpack` уже был правильным ответом в задании 6, а `phone` не был нигде.

**Не исправлено — судья ошибся фактически:**
- Судья бреда цитирует задание 13 как пару `that`/`hat` с «языком внизу» и разбор `seaside` со звуком `d`. В присланном тексте стоят `that`/`cat` и разбор по гласной — он читал более раннюю версию. Его же предложенная замена (`that`/`cat`, «начало в глубине рта») совпадает с тем, что уже стоит.
- Он же цитирует «Разница между `this` и `that` — только в шагах до вещи» — такой строки в присланном тексте нет. Ограничение всё равно учтено при переписывании интро 2.

---

🟡 **Стоит улучшить**
- Судья вкуса поставил REVISE при расхождении прогонов (BLOCK/REVISE) — по §10 Конституции это шум, выпуску не мешает. Его содержательные претензии (сухие интро) закрыты.
- Два вердикта судьи бреда цитируют текст, которого в файле нет. Это признак рассинхрона: судья получил не ту версию черновика. Стоит проверить, какой файл подаётся на вход `judge_nonsense` — иначе следующие круги правки будут гоняться за призраками.

🧩 **Скрытые связи**
- Перенумерация заданий сдвинула строку `modes:` — она обновлена, иначе сборщик расставил бы механики не тем заданиям.
- Замена слова в доске пар (`key` → `phone`) требовала проверки, что `phone` отработан ДО доски. Поэтому задание про телефон и поднято выше доски, а не оставлено на месте.

---

- Убрал из первой страницы фразу, которая обещала продолжение и обрывалась — на неё ругались сразу двое проверяющих.
- Переписал вторую и третью страницы: раньше это была сухая справка «слово — перевод — правило», теперь объяснение начинается с живой причины выбирать одно слово вместо другого.
- Поправил вопрос в задании со звуками: он сам подсказывал ответ, ученик мог не слушать запись вовсе.
- Починил разбор в задании на слух: различие строилось на признаке, который есть у обоих слов.
- Слово «телефон» встречалось всего один раз и ни разу не было правильным ответом — теперь оно работает трижды и ученик действительно его вспоминает.
- Два замечания не исправлял: проверяющий ссылался на текст, которого в сессии нет — похоже, ему показали старую версию.