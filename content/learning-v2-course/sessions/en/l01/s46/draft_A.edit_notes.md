## Изменения

**По судье бреда (BLOCK — правились первыми):**

1. **Интро 1, счёт букв.** Убрано «две буквы плюс значок вместо `it is`» — счёт не сходился. Осталось только верное: «`it is` в переписке сжимают до `It's` — значок стоит на месте выпавшей `i`».
2. **Задание 2, ловушка `fan`.** Ложное «в `fun` губы почти не растягиваются» заменено на верное: в `fan` рот открыт шире, звук тянется дольше. Ловушка `phone` тоже переписана без выдумки про губы.
3. **Задание 4, ловушка `born`.** Убрана подсказка «в `boring` есть ещё один кусок `-ing`», из которой ученик собрал бы `born+ing`. Теперь про количество кусков: `born` — один, `bo-ring` — два.
4. **Задание 9, ловушка `we`.** Убрано «только начало, у `weird` есть хвост» — `we` не начало `weird`, там другая гласная. Написано прямо про разницу гласных.
5. **Задание 11, ловушка `I'am`.** Парадокс «и на месте, и выпала одновременно» заменён на правило: значок ставят вместо выпавшей буквы, здесь `a` не выпала.
6. **Интро 2, две модели `isn't`.** Убрано противоречие: теперь сказано, что `is` стоит целиком, сжимается только `not`, — и потому второй раз его не сожмёшь.
7. **Интро 1, пустая фраза про отчёт.** Оставлена, но с действием впереди: «Полную форму `It is fun` тоже поймут, но так пишут в отчёте начальнику, а не другу в час ночи» — это `best_line` судьи вкуса, теперь она даёт инструкцию, а не украшает.

**По судье вкуса (REVISE):**

8. **Задание 6 переписано** — убраны `It was` и самоспор в инструкции. Осталась одна строка: «друг спрашивает, как вам лекция. It's ___ .»
9. **Шапка приведена к содержанию** — `you're`, `we're`, `aren't` вычеркнуты, их в сессии нет.
10. **Интро 3, хвост про красное подчёркивание** удалён. Абзац кончается фактом: частая опечатка — `wierd`. Ничего нового на освободившееся место не дописано.
11. **Интро 2, абзац про `it'sn't`** удалён целиком — он учил несуществующей форме. Правило про «одно из двух» ушло в тот же абзац, где вводится `isn't`, без выдуманной формы.
12. **«В чате это слово почти всегда едет с коротким `it's`»** — вычеркнуто вместе с выдуманной статистикой.
13. **Переводы `tired` и `quiet`** перенесены из сносок после разборов в сами инструкции заданий 11 и 14 — ученик читает их до ответа, а не после.
14. **`new` как ловушка в 6 и ответ в 16** — оставлено: разбор в 6 прямо говорит, что лекция МОЖЕТ быть новой, то есть слово не помечается как заведомо неверное, а отсеивается по смыслу вопроса.

**По педагогу (REVISE):**

15. **Длина интро** приведена к норме: 262 / 288 / 270 знаков вместо 316 / 381 / 349. Резались разгон и вторые иллюстрации, шутки остались все три.
16. **Шапка «Возвращаются»** исправлена на `quiet, new, tired` — правка из самопроверки доведена до самого файла.

---

🟡 **Стоит улучшить**
- `cool` до доски пар отрабатывается один раз (задание 12) — минимум соблюдён, но слово самое слабо закреплённое в сессии.

🧩 **Скрытые связи**
- Слова `fun`, `boring`, `cool`, `weird` уходят в общий реестр новых слов курса — в следующих сессиях они пойдут строкой «Возвращаются».
- Строка «Операция» в шапке изменилась — если план курса на 1 792 строки хранит формы отдельно, там `you're` / `we're` / `aren't` остались незакрытыми и им нужна своя сессия.

---

- Исправил сессию про короткие формы в чате по замечаниям всех четырёх проверяющих.
- Главное: убрал четыре неверных утверждения о произношении и написании — они учили человека неправде.
- Убрал абзац про несуществующую форму слова, шутку-приписку в конце и выдуманную статистику.
- Починил сломанное задание, где инструкция спорила сама с собой.
- Сократил три страницы объяснения до нормы, шутки при этом все сохранил.
- Привёл шапку в честный вид: убрал формы, которых в сессии нет, и поправил список повторяемых слов.