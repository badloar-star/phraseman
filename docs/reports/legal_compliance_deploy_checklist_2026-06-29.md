# Чек-лист: что нужно сделать, чтобы юр-фиксы реально заработали

Дата: 2026-06-29. Весь КОД готов и закоммичен (`515ae4180`, `7d24b6694`). Но он «спит», пока не задеплоен и не настроен. Ниже — ровно то, что должен сделать ты.

> Кратко: **(1) задеплоить 3 вещи → (2) включить Telegram-алерты → (3) выпустить билд → (4) обновить store-дисклоужеры.** Без шага 1 ничего не собирается; без шага 2 алерты молчат; без шага 3 у юзеров старая версия без возрастного экрана.

---

## 1. ДЕПЛОЙ (обязательно — иначе данные не собираются)

```bash
cd C:/appsprojects/phraseman

# 1.1 Серверная функция ИИ-диалога (safety-детектор + блок ИИ для <16 + ageBracket)
firebase deploy --only functions:premiumDialogSend

# 1.2 Правила Firestore (без них клиент НЕ сможет писать user_consents — отклонит)
firebase deploy --only firestore:rules

# 1.3 Веб-страницы legal + админка (если хостится на Firebase Hosting)
firebase deploy --only hosting
```

Проверка после деплоя:
- `firebase deploy` без ошибок;
- правила: в консоли Firestore → Rules видно `safety_flags` и `user_consents`.

## 2. ВКЛЮЧИТЬ TELEGRAM-АЛЕРТЫ (иначе сигналы о детях/опасных сообщениях НЕ придут)

Алерт по умолчанию **выключен**. Чтобы заработал — 3 шага:

```bash
# 2.1 Задать токен бота (секрет; бот @codex_reportbot или твой)
firebase functions:secrets:set ADMIN_ALERT_BOT_TOKEN
# вставить токен от @BotFather, затем заново задеплоить функции, которые его используют:
firebase deploy --only functions
```

```
# 2.2 В админке → таб «🔔 Alerts»:
#   - Master-тумблер «Алерты включены» = ON
#   - Chat ID = твой telegram chat_id (как узнать: написать боту, открыть
#     api.telegram.org/bot<token>/getUpdates, взять chat.id)
#   - Галочка «🆘 Опасные сообщения в ИИ» = ON (по умолчанию on)
#   - Нажать «💾 Сохранить», затем «📨 Тест» — должно прийти сообщение в Телеграм
```

Если тест не пришёл → проверь токен (2.1), chat_id, что функции задеплоены.

## 3. ВЫПУСТИТЬ НОВЫЙ БИЛД ПРИЛОЖЕНИЯ (иначе у юзеров старая версия)

```bash
# OTA-обновление (быстро, если только JS менялся — а тут менялся app.json → нужен НОВЫЙ нативный билд!)
# app.json изменён (убран AD_ID) → ТРЕБУЕТСЯ полная пересборка, не OTA:
eas build --platform all --profile production
# затем submit в сторы
```

> ВАЖНО: убран `AD_ID` из `app.json` (нативная конфигурация) → **OTA не подхватит**, нужен полноценный build + сабмит.

После релиза проверь на устройстве:
- новый пользователь видит экран «год рождения + галочки» первым;
- младше 16 — мягкая блокировка;
- существующий пользователь видит блокирующий модал согласия;
- ИИ-чат и арена недоступны подростку; покупки у подростка показывают «с 16 лет».

## 4. ОБНОВИТЬ STORE-ДИСКЛОУЖЕРЫ (App Store + Google Play) — вручную в консолях

Это НЕ в коде — заполняется в App Store Connect и Google Play Console:

**Apple App Privacy (Nutrition Label):**
- Возрастной рейтинг — пройти новый опросник Apple про ИИ-чат-ботов (если ещё не сделано).
- Data Linked to User: User ID/stable ID, purchases, user content (ИИ-сообщения, чат, паки), product interaction, diagnostics, identifiers, learning data.
- Tracking: «No» (мы не используем IDFA/кросс-апп рекламу).

**Google Play Data Safety:**
- Personal info (email/имя при логине), Financial (покупки), App activity, App info/performance (краши), Device/other IDs (stable id, push token), User content (ИИ/чат/паки).
- **AD_ID:** теперь НЕ декларируем (убран) — снять галочку Advertising ID, если стояла.
- Data deletion URL: https://knowlyapps.com/legal/data-deletion/ (проверь, что открывается).

## 5. ОРГАНИЗАЦИОННОЕ (не код, но нужно для юр-защиты)

- [ ] **Юрлицо/sole-trader** Maksym Babiev оформлен в Ирландии (адрес уже вписан в документы).
- [ ] **GDPR-представитель в ЕС (Art.27)** — для не-EU оператора; но раз оператор уже в Ирландии (в ЕС) — этот пункт закрыт автоматически. ✅
- [ ] **UK-представитель** — нужен отдельно, если есть пользователи в UK (Ирландия ≠ UK после Brexit). Проверь с юристом.
- [ ] Прогнать готовый текст Privacy/Terms через юриста (финальная вычитка).
- [ ] Решить порог для India (DPDP вступает 14.05.2027) — пока время есть.

---

## Что КОД уже делает сам (готово, проверять не надо)
- Возрастной экран в онбординге + блокирующий модал для старых.
- Аналитика молчит без согласия (Crashlytics работает).
- ИИ ловит опасные слова (4 языка) + мягко направляет к помощи.
- ИИ недоступен <16 (сервер + клиент).
- Покупки недоступны <16.
- safety_flags + user_consents пишутся, два раздела в админке их показывают.
- Все документы: Maksym Babiev / Ирландия / право Ирландии / 16+.

## Простыми словами (итог)
- Код полностью готов и сохранён — но он как новая проводка в доме: пока «рубильник» не включишь, свет не горит.
- Тебе нужно: 1) залить серверную часть и правила (команды выше), 2) включить Телеграм-сигналы (токен + chat id + галочка + тест), 3) собрать и выложить новую версию приложения (обязательно полную сборку — поменялась нативная настройка), 4) вручную обновить карточки приватности в App Store и Google Play.
- Отдельно, не про код: оформить себя как ирландского предпринимателя (адрес уже вписан) и показать готовые тексты юристу для финальной вычитки.
- Пока не сделаешь шаги 1–3, разделы в админке будут пустыми, а сигналы в Телеграм молчать — это не баг, просто «рубильник» ещё не включён.
