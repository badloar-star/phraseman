## Изменения

1. **Задание 8 — новая карточка `curious`** (BLOCK судьи-ученика). Слово стояло правильным ответом, а перевод появлялся только на доске пар, четырьмя шагами позже. Теперь карточка идёт перед первым использованием. Нумерация дальше сдвинута на единицу.
2. **Задание 11 — новая карточка `upset`.** Ученик выводил значение из контекста и не был уверен. Карточка встала перед заданием 12, где слово впервые нужно выбрать.
3. **Задание 15 (было 16) — сборка `Is she upset?` убрана**, чтобы число заданий осталось 17 при двух добавленных карточках. Фраза `Is she upset?` не потеряна: она собирается в задании 16 (вставьте `Is`).
4. **Задание 15 (было 13) — обманка с лишней плиткой `not` убрана вместе со сборкой `Is he cheerful?`** (`must_fix` судьи вкуса: обманка не кусалась, инструкция и так исключала отрицание). Ложной обманки в сессии больше нет — лучше ни одной, чем беззубая.
5. **Интро 1 — `she` убрана со страницы**, осталось `he` и один человек. Готовая формула теперь не выдаётся тремя вещами сразу.
6. **Интро 2 — вычеркнуто «`he` не меняет `handsome` под себя… без всяких хвостов»** (объяснение того, о чём никто не спрашивал). На месте — факт о слове: `beautiful` про мужчину почти не говорят.
7. **Интро 3 — вычеркнуто «Меняется только одно слово — `he` на `she`»** (пересказ того, что видно в примере). `she` с переводом переехала сюда из интро 1.
8. **Задание 2 — переписан разбор `handy`:** запись «заканчивается на „эм“» была неточной, теперь различие по длине слова.
9. **Заголовок интро 1** — убрано `Is` из заголовка, чтобы страница не начиналась с формулы.

**Не исправлено, с объяснением:** требование судьи вкуса добавить шутку в интро 1 и 3. Шутка допустима только та, что растёт из самого правила (ГЛАВНОЕ_ТРЕБОВАНИЕ). В интро 1 правило — порядок слов, и живая деталь там уже есть: `He is cold?` = «не верю ушам». В интро 3 та же роль у «`is` хочется подправить под неё — подправлять нечего». Дописывать шутку ради шутки — прямой запрет владельца, он дороже замечания судьи.

---

🟡 **Стоит улучшить**
- В сессии не осталось ни одной обманки на внимательность (Конституция §4 их приветствует, но не требует). Убранная была фальшивой; настоящую можно вернуть позже — например, плитка `not` при сцене, где спутник действительно сомневается.
- `pretty` в карточке заявлено и про вещи, но все задания только про людей — значение «красивая чашка» останется без отработки.

🧩 **Скрытые связи**
- Число заданий осталось 17, строка `modes:` пересобрана целиком — сборщик читает её по индексам, старая сломала бы соответствие.
- Слова `curious` и `upset` теперь получают карточки в этой сессии; если они уже имели карточки раньше по плану курса, строка «Возвращаются» всё равно верна — в «Новые слова» они не попали (§8г).

---

- Починил главную дыру: слово «любопытный» требовалось раньше, чем объяснялось — теперь у него есть карточка до первого задания.
- То же самое сделал для слова «расстроен» — раньше его значение приходилось угадывать.
- Убрал из первой страницы лишнее местоимение, чтобы объяснение шло по одному шагу, а не вываливало всё сразу.
- Вычеркнул из второй страницы ответ на вопрос, который ученик не задавал, и поставил вместо него живой факт: мужчину по-английски хвалят особым словом.
- С третьей страницы убрал пересказ того, что и так видно в примере.
- Выбросил фальшивую ловушку с лишним кубиком — она никого не могла обмануть.
- Число заданий осталось прежним, семнадцать.