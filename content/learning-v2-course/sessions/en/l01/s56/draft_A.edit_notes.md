## Изменения

**По судье бреда (BLOCK — правились первыми):**

1. **Интро 1, «`am` держится за `I` и никуда от него не отходит»** — удалено целиком. Утверждение ложно: `Am I late?` — `am` стоит перед `I`. Строка про `am` из Интро 1 убрана (там речь о `it`, `am` вообще не по теме), а в Интро 3 формулировка заменена на проверяемую: «форму `am` берут только с `I`». То же в разборе задания 5: «Форму `am` берут с `I`».
2. **Интро 1, «неделя длинная и как будто множественная»** — придуманная связь, удалена. Осталась только рабочая часть: `are` — про несколько, неделя одна. Одобренная судьёй строка «даже если по ощущениям их было три» сохранена (`best_lines`).
3. **Интро 2, «спотыкаются даже те, кто говорит по-английски давно»** — пустота, удалена. Заменено на действие: «рука сама тянется к `are`» + факт, почему `is`.
4. **Интро 3, «на отрицании `am` теряется чаще всего»** — статистика ниоткуда плюс повтор дистрактора, абзац вычеркнут.
5. **Задание 2, омофон `weak`** — убран из звукового задания (омофон на слух неразличим, задание не имело верного ответа). Заменён на `walk`.
6. **Задание 11, `worm`** — транскрипционное объяснение через «букву `a`» удалено (ученик слышит, а не видит; признак не различал пару). Осталось различение по смыслу.
7. **Задание 7, «квартира добрая не бывает»** — безусловное «не бывает» убрано, оставлен факт: `kind` — про характер человека.

**По судье вкуса (REVISE):**

8. **Длина всех трёх интро** приведена в норму (251 / 258 / 285 против 346 / 391 / 327). Резалась вода: третий абзац Интро 1 (дублировал дистрактор), третий абзац Интро 3 (дублировал разбор), тема `not` из Интро 2 (полностью повторялась в Интро 3).
9. **Заголовок Интро 1** переименован в «Неделя — это `it`» — теперь совпадает с содержанием страницы.
10. **`hard`** переведён в первой строке Интро 1, а не только в разборе ответа.
11. **Интро 2 и Интро 3 получили шутку из правила**: «Слово про толпу, а скрепка при нём одиночная… английский считает здесь не головы, а „каждого“ — по одному» и «выбросьте её, и держать фразу будет нечему».
12. **Канцелярское «скрепку не выбрасывают»** заменено на обращение к читателю: «выбросьте её, и…».
13. **Механики разведены**: `context_gap_grammar` было 6, стало 5. Задание 13 («вставьте скрепку — про неделю») дублировало задание 5 тем же действием — переделано в `phrase_builder` на отрицание с неделей: `It is not hard`. Это заодно закрывает пробел — отрицание отрабатывалось только на `I` и `everyone`, теперь и на `it`.
14. **Интро 2, третий дистрактор** заменён: было «Everyone is not kind» (разбор объяснял смысл, а не ошибку), стало `Everyone kind` — реалистичный пропуск скрепки, тот же класс ошибки, что и на других страницах.

**По судье-педагогу:** длина интро — единственное замечание, закрыто (п. 8).

**Не исправлено:** нет.

---

🟡 **Стоит улучшить**
- Задание 11 (`warm` / `worm`) остаётся самым слабым местом сессии: пара действительно различима на слух, но объяснить разницу русскими буквами нельзя, поэтому разбор опирается на смысл. Если в банке есть более контрастная пара для `warm`, её лучше поставить сюда.

🧩 **Скрытые связи**
- Задание 13 сменило механику (`context_gap_grammar` → `phrase_builder`) — строка `modes` для сборщика обновлена.
- Замена `weak` → `walk` в задании 2: `walk` не входит в список известных слов ученика, но стоит **дистрактором**, а не целью — Конституция §3 такие ловушки разрешает, разбор чужую форму не учит.

---

- Переписал 56-ю сессию по замечаниям четырёх проверяющих.
- Убрал три неверных утверждения о языке: про то, что «am» всегда стоит рядом с «я»; задание, где на слух надо было различить два одинаково звучащих слова; и объяснение про «червяка», по которому различить слова было невозможно.
- Вырезал воду: каждая из трёх страниц объяснения была в полтора раза длиннее нормы, теперь все три в норме.
- Добавил шутку на двух страницах, где её не было — обе про само правило, а не приклеенные в конец.
- Поправил заголовок первой страницы: он обещал разговор про слово «неделя», а страница была про другое.
- Одно задание дублировало другое — заменил его на новое, где отрицание отрабатывается на слове «неделя».