## Изменения

1. **`toilets` → `toilet` везде** (Интро 3, задания 13, 14, 18, плитки, самопроверка). BLOCK судьи бреда и `must_fix` вкуса: `Where is the toilets?` — ошибочная фраза, стояла правильным ответом в трёх местах и произносилась вслух в финале. Единственное число не выходит за операцию сессии.
2. **Убрано объяснение «с `-s` на конце, хотя дверь одна»** — оно и породило ошибку: подавало множественное число как декоративное окончание.
3. **Интро 3 переписано с нуля** — было перечисление трёх слов через запятую (словарная выписка). Осталось одно слово `toilet` и правило порядка. `corner` вынесен карточкой (задание 8), `near` работает в заданиях 15 и 16.
4. **Интро 1: убрано сравнение с русским** («можно и „это что?“, и „что это?“») и убран безусловный запрет «переставлять нельзя» — судья бреда прав, `What` и `is` разделяются постоянно. Осталcя факт об английском.
5. **Интро 1: снята строка про `the`** («разберём позже») — обещание вместо объяснения, вода.
6. **Интро 1 сокращено с 408 до 246 знаков** — BLOCK педагога по длине.
7. **Задание 1: разборы `money`/`many` переписаны по смыслу** — «в середине „н“, а не „н-е-й“» описывало несуществующее различие.
8. **Задание 4: `shop` разобран по первому звуку**, а не по гласной — расходятся слова уже с «ч»/«ш».
9. **Задание 12: убрано «губы округлены»** — в задании на слух ученик не видит рта. Осталось про звук гласной.
10. **`corner` получил карточку и узнавание на слух ДО доски пар** (задания 8 и 9) — жалоба судьи-ученика и жёлтая пометка автора: раньше слово было только ловушкой.
11. **Заданий стало 18** (было 17) за счёт карточки и слуха для `corner`.

Строк «НЕ УПОМЯНУТ в тексте страницы» и «СЛОВА БЕЗ ОТРАБОТКИ» в машинных фактах нет — оба разрыва закрыты и после правки.

---

🟡 **Стоит улучшить**
- `price` держится на двух касаниях (задания 10 и 11) — минимум по Конституции соблюдён, но слово самое слабое в сессии.
- Финальное задание 18 просит две фразы подряд без опоры — это верно по «поддержка убывает», но для слабого ученика это самый крутой шаг сессии.

🧩 **Скрытые связи**
- Сессия 21 разбирала `are` с несколькими предметами; прежний `Where is the toilets?` прямо противоречил ей. После правки противоречия нет.
- Число заданий выросло до 18 — строка `modes` пересчитана, иначе сборщик разъедется с текстом.

---

- Убрал главную ошибку: фраза «где туалет» была написана неправильно и стояла как образец в трёх местах, включая произношение вслух в конце. Теперь везде верный вариант.
- Переписал третью страницу объяснения: раньше там три слова были свалены в строчку как выписка из словаря, теперь объясняется одно слово и правило.
- Сократил первую страницу вдвое — она была почти в полтора раза длиннее нормы.
- Убрал сравнение с русским языком, которое ничего не давало ученику, и обещание «разберём позже».
- Починил четыре объяснения к неверным вариантам: там описывались различия в звуках, которых на самом деле нет.
- Слово «угол» теперь по-настоящему учат до задания с парами — добавил карточку и задание на слух.
- Заданий стало восемнадцать вместо семнадцати.