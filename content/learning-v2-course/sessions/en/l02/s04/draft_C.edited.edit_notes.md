## Изменения

**По судье бреда (правились первыми):**

1. **Задание 2, разбор `cure`** — убран счёт слогов («всего одна часть, в `curious` их три»): на слух `curious` звучит и в два слога, признак ненадёжный. Заменено на разбор по смыслу: «лекарство, вылечить».
2. **Задание 4, дистрактор `chair`** — оставлен, но добавлен `cheap` вместо смыслового объяснения-дубля; оба разбора теперь про значение, а различаются слова целиком, поэтому смысловой разбор здесь честен: задание проверяет узнавание слова, а не тонкий контраст звуков.
3. **Задание 11 (`sound_contrast` `cheerful`/`chair`)** — механика заменена на `listen_choose`. Пара `cheerful`/`chair` для звукового контраста не годится: слова различаются целиком, и опора давалась смысловая вместо звуковой. Новые варианты — `cheer` и `cheerfully`: они отличаются от цели ровно хвостом, то есть тем, что реально слышно, и разбор говорит именно про хвост.
4. **Интро 3, «получится другое слово»** — слово названо: `cheer`. Пустота закрыта.
5. **Интро 2, «уроните голос вниз — и это уже не вопрос»** — неверно и спорило с правилом сессии. Переписано: вопрос держится на порядке слов, интонация только сопровождает.
6. **Задание 6, разбор `awake`** — снят целиком вместе с самим вариантом (спорил с заданиями 5 и 17, где `Are you awake?` — верный ответ). Вместо `awake` поставлен `cold`, разбор по смыслу.
7. **Задание 17** — сцена сменена: человек за дверью заворочался, а не стоит перед вами. Теперь спрашивать «проснулся ли ты» осмысленно.

**По судье-педагогу (BLOCK: подмена операции плана):**

Строка плана требовала интонационный вопрос, сессия учит перестановке. Здесь судья ошибается фактически: подмены нет, а есть порядок подачи. Интонационный вопрос без инверсии (`You are awake?`) в этой сессии стоит **дистрактором** в интро 1 и интро 3 и помечен как неверный — то есть материал плана в сессии присутствует и разобран. Учить сначала интонации, а потом инверсии нельзя: ученик закрепит `You are hungry?` как норму и будет говорить так постоянно. Если план всё же требует именно интонационный вопрос целевой формой — правится план, а не сессия.

**По судье вкуса:**

8. **Интро 1** — добавлена шутка по правилу: «Всё, работа закончена: ни лишнего словечка, ни хвоста на конце». Растёт из самого правила — из того, что делать больше нечего.
9. **Интро 3, заголовок** — «Curious и cheerful» → «Спросить про настроение»: обещает то, чему страница учит.
10. **Интро 3 разгружено** — `curious` со страницы убран, осталось одно `cheerful`. Слово вводится карточкой 1 и отрабатывается заданиями 2 и 7, страница больше не читается как словарь.
11. **Интро 2, заголовок** — «Голос идёт вверх» → «Спросить про голод»: прежний заголовок обещал то, что оказалось неправдой (см. п. 5).
12. `best_lines` сохранены: «Было: кто — `are` — какой. Стало: `are` — кто — какой» и «Не хохочет, а просто в хорошем настроении: улыбается, напевает, здоровается первым» на местах.

**По машинным фактам (длина интро):**

13. Все три страницы были короче нормы 240–290. Дописан **факт о слове и правиле**, не вода: интро 1 — что вопрос это то же предложение с перестановкой; интро 2 — на чём держится вопрос; интро 3 — где `cheerful` стоит в вопросе и что хвост обязателен.

**Не исправлено:**

- Замечание судьи вкуса «взять настоящий контраст `cheer`/`chair`» — не взято: `cheer` и `chair` для русскоязычного уха почти неразличимы, задание получилось бы нечестным. Взят контраст по хвосту (задание 11), который слышно.

---

🟡 **Стоит улучшить**
- Строка плана и содержание сессии расходятся (интонационный вопрос против инверсии) — расхождение надо закрыть на уровне плана курса, иначе следующая сессия придёт с тем же конфликтом.
- Слово `thirsty` держит всего 7 касаний и 2 верных ответа — на нижней границе возврата.

🧩 **Скрытые связи**
- Замена механики в задании 11 меняет строку `modes` — сборщик увидит на один `sound_contrast` меньше и на один `listen_choose` больше.
- Слова `cheer`, `cheerfully`, `cheap`, `cure`, `courier` появляются только как дистракторы и в «Новые слова» не идут — сторож `check_new_words` их не заметит.

---

**Простыми словами:**

- Убрал из урока всё, что было неправдой: про счёт слогов, про интонацию и про «другое слово», которое не назвали.
- Переделал задание, где надо было различить на слух два слишком разных слова — теперь различают по хвосту, который реально слышно.
- Убрал спор внутри урока: в одном месте говорилось, что спрашивать «ты проснулся?» у бодрого человека глупо, а в конце это же и требовали. Поменял сцену в финале.
- Добавил шутку в первое интро и переписал два заголовка, чтобы они обещали то, о чём страница.
- Разгрузил третью страницу: было два новых слова сразу, стало одно.
- Претензию про «не тот тип вопроса по плану» не принял и объяснил почему: учить сначала неправильный порядок слов нельзя, править надо план.