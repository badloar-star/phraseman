## Изменения

**По судье бреда (первым, без обсуждения):**
1. Убрано «английский пересчитывает вслух то, что мы держим в уме» — ложное противопоставление: по-русски «ночи тихие» тоже помечено окончанием. На месте — факт об английском: `-s` и `are` ходят парой.
2. Убрано «Считать придётся именно ночи» — пустая фраза.
3. Убрано «Неделя одна, скрепка тоже одна — запомнить проще, чем выговорить» — придуманная связь, которая ломается на следующей странице (при `The nights are quiet` скрепка тоже одна).
4. Задание 2: разбор `wake` получил звуковой признак через знакомое слово — «гласная там другая, как в `late`».

**По редактору вкуса:**
5. Убрано `hard` из интро 1 — слово вне границ сессии; заменено на `quiet`, которое отрабатывается ниже.
6. `kind` в задании 14 заменено на `busy` — `kind` в сессии не вводится.
7. Заголовок интро 3 переписан на то, чему учит страница: «Morning — про время, а не про себя» (ловушка `I am morning`). Убрано «а не „доброе утро“» и «Не только в `good morning`» — спор с тем, чего никто не думал, плюс `good morning` в курсе не проходили.
8. Из карточки 8 снята та же оговорка про «половинку приветствия».
9. Убрано «Хоть три слова после, хоть пять» — довесок к законченной мысли.
10. Задание 12 переделано на «дни загруженные» — теперь оно не спорит с финалом 18, где дни busy.

**По педагогу (BLOCK):**
11. Интро 1 сокращено с 317 до ~250 знаков, интро 2 — с 353 до ~270.
12. `everyone` теперь стоит правильным ответом: задание 17 получило плитку-ловушку `everyone` (реалистичная — ученик мог перепутать чужую скрепку), а в задании 14 `everyone` работает целью с разбором согласования.

**Не исправлено:**
— Ничего. Все `must_fix` закрыты.

---

🟡 **Стоит улучшить**
- Интро 2 после чистки стало коротким (~270 знаков) — на нижней границе нормы, но второй рабочий пример (`The days are busy`) уже отрабатывается заданием 12, дублировать в тексте не стал.

🧩 **Скрытые связи**
- Правка интро 1 (`hard` → `quiet`) и задания 12 (`quiet` → `busy`) связаны: `quiet` теперь объяснено в интро 1 и отработано в 11, `busy` — в интро 1, 7, 12, 14. Оба слова закрыты.
- Замена `kind` на `busy` в 14 убрала последнее слово вне границ сессии.

---

**Итог простыми словами:**
- Убрал из текста неправду про русский язык — будто мы «в уме» считаем количество ночей. По-русски мы тоже говорим «ночи», а не «ночь».
- Убрал правило с первой страницы, которое ломалось на второй: «неделя одна — значит и связка одна».
- Выбросил три шутки-приписки в конце абзацев, которые ничего не объясняли.
- Убрал два английских слова, которых в этой сессии нет и негде выучить, — заменил на те, что тут разбираются.
- Сделал так, чтобы задание про дни не противоречило финальному рассказу маме.
- Сократил первые две страницы до нужной длины.
- Добавил слово «все» в задание правильным ответом — раньше оно только мелькало.