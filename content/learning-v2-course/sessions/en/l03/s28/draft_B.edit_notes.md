## Изменения

1. **Интро 2, «Английский тут честнее нас»** — вычеркнуто целиком. Судья бреда и судья вкуса совпали: в русском есть полная пара «этот / тот» по той же оси, противопоставление выдумано. На место поставлен факт об английском: промежуточного слова нет, выбирать приходится всегда.
2. **Заголовок интро 3** переписан с «Wallet — кошелёк, который вот-вот понадобится» на «В вопросе `is` идёт первым» — заголовок теперь называет то, чему учит страница.
3. **Концовка интро 3 про «два и больше — будут позже»** вычеркнута, ничего вместо неё не дописано. Страница стала короче (251 знак вместо 323 — машинный факт о длине закрыт).
4. **Разбор дистрактора `Is` в задании 5** переписан по фактической подстановке: `Is suitcase is heavy` — `is` дважды. Прежний разбор объяснял несуществующую ошибку.
5. **Разбор дистрактора `this` в задании 10** — убрано ложное «два слова-указателя подряд не ставят» (`Is this that bag?` существует). Причина названа настоящая: `this` не бывает связкой.
6. **Разбор дистрактора `Are` в задании 14** — убрана ссылка на число, которая уводила в сторону: `is` во фразе уже стоит, на пустом месте нужен указатель.
7. **Задание 9** — из инструкции убрана смысловая подсказка «какое про кошелёк»: ученик выбирал по памяти, не слушая. Теперь задание про порядок звучания, разбор дистрактора тоже переведён на звук.
8. **Машинный факт «интро 2: `small` НЕ УПОМЯНУТ»** закрыт переводом `small` прямо в вопросе (как и `heavy`, `open` на соседних страницах) — вопрос и текст больше не ведут к разным словам.
9. **Машинный факт «bag: правильным ответом 0»** закрыт: задание 16 переведено с `suitcase` на `bag` (`That bag is not heavy`). Слово получило второе касание и оба раза внутри верного ответа. Замена не сломала проверку сборок: скелет отрицания остался единственным в сессии.
10. **Повторы описаний предметов в интро 1 и 3** срезаны — описание чемодана и кошелька осталось только в карточках практики, на страницах интро место отдано правилу.
11. **Три однотипных примера в интро 2** («на дальней стене, на другом столе, в витрине») заменены одним с действием: рюкзак под потолком, за ним идти со стремянкой.
12. **Все `best_lines` судьи вкуса сохранены**: «рука дотянулась — `this`, не дотянулась — `that`», разбор `valley` про губы, «Слово для вещи, до которой вы дотягиваетесь».

**Не исправлено осознанно:** требование судьи вкуса «переписать интро целиком с юмористическим ходом». Шутка в готовый абзац не дописывается (§10 Конституции: попытка закрыть эту претензию дописыванием юмора уже ломала полученные PASS). Страницы переписаны на живое объяснение без приклеенных концовок; естественного хода, растущего из самих `this`/`that`, здесь не нашлось, а натужный запрещён прямым решением владельца 08.09.

---

🔴 **Требует немедленного вмешательства**
- Нет.

🟡 **Стоит улучшить**
- Интро 2 — 285 знаков, у верхней границы цели 240–290. Ещё одна правка текста на этой странице потребует пересчёта.

🧩 **Скрытые связи**
- Задание 16 теперь тянет `bag` вместо `suitcase`; `suitcase` при этом сохраняет четыре касания (карточка, слух, пропуск, диктант, голос), доска пар в задании 13 не затронута.
- `this`/`that` опираются на сессии 25 и 27 — сцены там другие (провода, техника в квартире), повтора кадра нет.

---

- Убрал из текста неправду про русский язык: там, где говорилось, что мы якобы не различаем «этот» и «тот» — мы различаем.
- Переименовал третью страницу: раньше заголовок обещал рассказ про кошелёк, а страница учила задавать вопрос. Теперь название честное.
- Исправил три подсказки к неверным ответам: они объясняли ошибку, которой на самом деле нет.
- Переделал задание на слух: раньше ответ можно было угадать, не слушая запись, потому что в самом задании было сказано, какое слово искать.
- Дал слову «сумка» второе появление — теперь оно встречается два раза и оба раза в правильном ответе, а не только в подсказке.
- Сократил третью страницу, убрав описание кошелька, которое и так есть в карточке, и обещание рассказать что-то потом.