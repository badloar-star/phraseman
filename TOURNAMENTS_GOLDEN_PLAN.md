# ЗОЛОТОЙ РЕЖИМ — план постройки серверной игры «Турниры»

Дата начала: 2026-07-27. Основание: решение владельца переписать логику турниров
целиком — сервер, таймеры, задания, подбор, экраны, анимации.

Опора: аудит `TOURNAMENTS_AUDIT_2026-07-27.md` (101 найденная проблема) +
исследование эталонов (Kahoot, HQ Trivia, QuizUp, Duolingo, Quizizz, Trivia Crack).

**Порядок работы:** по каждой подкатегории задаю вопросы пачками по 4, даю
3 лучшие практики, ставлю рекомендацию и объясняю почему. Ответы владельца
фиксируются прямо в этом файле — он становится единственным источником правды
для реализации.

---

## СТРУКТУРА ПЛАНА

- **A. Фундамент времени и синхронизации** (A1–A5) — вопросы 1–20
- **B. Жизненный цикл матча** (B1–B6) — вопросы 21–44
- **C. Подбор игроков и боты** (C1–C5) — вопросы 45–64
- **D. Экономика и награды** (D1–D4) — вопросы 65–80
- **E. Очки, серии, рейтинг** (E1–E5) — вопросы 81–100
- **F. Режимы заданий** (F1–F8) — вопросы 101–124
- **G. Экраны и анимации** (G1–G6) — вопросы 125–144
- **H. Отказы, честность, эксплуатация** (H1–H2) — вопросы 145–150

---

## A. ФУНДАМЕНТ ВРЕМЕНИ И СИНХРОНИЗАЦИИ

Самый нижний слой. Всё остальное стоит на нём: если время врёт, врут очки,
рейтинг и ощущение честности. Главная жалоба игроков во всех подобных играх —
«у меня было меньше времени, чем у других».

### A1. Источник истины по времени
Кто решает, сколько осталось: сервер, клиент или оба. Как клиент узнаёт
серверное время. Что происходит при сбитых часах устройства.
*Сейчас:* клиент считает сам от константы, серверное значение получено и не
используется — рассинхрон без коррекции.

### A2. Формат дедлайна
Сервер шлёт длительность («10 секунд») или абсолютную метку («истекает в
20:34:07.412»). Как это переживает фоновый режим и блокировку экрана.
*Сейчас:* длительность, отсчёт локальный.

### A3. Приём ответа и окно допуска
Верить ли клиентскому времени ответа. Насколько зажимать. Сколько давать на
сетевую задержку. Что делать с ответом, пришедшим после дедлайна.
*Сейчас:* время ответа на вопрос сервером не измеряется вообще — считается одно
на весь раунд.

### A4. Продвижение фаз
Кто двигает матч вперёд: крон, клиент, оба. Что при свёрнутом приложении.
*Сейчас:* крон раз в минуту против фаз в 5–12 секунд; свёрнутое приложение
растягивает турнир с 2 до 10 минут.

### A5. Транспорт
Как клиент узнаёт об изменениях: подписка на документ, поллинг, постоянное
соединение. Цена каждого варианта в Firebase.
*Сейчас:* подписка на документ комнаты, живёт даже под шторками.

---

## B. ЖИЗНЕННЫЙ ЦИКЛ МАТЧА

### B1. Расписание против «играть сейчас»
Турниры по слотам, по требованию или обе схемы. Как они уживаются.

### B2. Состояния и переходы
Полный список фаз, что валидно, что запрещено, как чинить застрявшую комнату.

### B3. Лобби
Сколько ждём, что показываем, когда стартуем, можно ли выйти.

### B4. Раунды и структура
Сколько раундов, сколько заданий, одинаковые ли для всех, порядок.

### B5. Межраундовые экраны
Таблица лидеров, показ правильного ответа, длительность.

### B6. Финал и закрытие
Результаты, награды, разбор ошибок, когда комната умирает.

---

## C. ПОДБОР ИГРОКОВ И БОТЫ

### C1. Размер комнаты и наполнение
Сколько мест, сколько живых нужно для старта, что при недоборе.

### C2. Природа соперников
Боты, «призраки» реальных игроков, только живые. Честность перед игроком.

### C3. Поведение ботов
Скорость ответа, точность, распределение, зависимость от сложности.

### C4. Появление в лобби
Постепенно или сразу, за какое время, в каком порядке.

### C5. Игра с друзьями
Возможность попасть в одну комнату, приглашения, приватные турниры.

---

## D. ЭКОНОМИКА И НАГРАДЫ

### D1. Цена входа
Сколько, в чём, бесплатные попытки, что при отмене.

### D2. Призовой фонд
Как формируется, как делится, участие «взносов» ботов.

### D3. Недельный банк и сезон
Накопление, выплата, кому и когда.

### D4. Выдача наград
Мгновенно или по клику, что при отвале, срок хранения.

---

## E. ОЧКИ, СЕРИИ, РЕЙТИНГ

### E1. Формула очков
За правильность, за скорость, пол и потолок, штрафы.

### E2. Серии
Порог, размер бонуса, сброс, визуализация.

### E3. Ранг ответа
«Первый ответивший» — как считается и считается ли вообще.

### E4. Итоговые места
Как разрешаются равные счета, что видит игрок.

### E5. Сезонный рейтинг
Что копится, как обнуляется, лиги и дивизионы.

---

## F. РЕЖИМЫ ЗАДАНИЙ

### F1. Выбор варианта (guess_phrase)
### F2. Пропуск в предложении (fill_gap)
### F3. Лишнее слово (find_oddity)
### F4. Сборка фразы (translate_build)
### F5. На слух — выбор (listen_choose, sound_contrast)
### F6. На слух — диктант (listen_build)
### F7. Поле пар (speed_match, макет 07)
### F8. Микс и распределение режимов по раундам

По каждому: вёрстка, время, подсчёт, что при таймауте, анимации,
доступность, крайние случаи (длинный текст, 2 варианта, 6 вариантов).

---

## G. ЭКРАНЫ И АНИМАЦИИ

### G1. Хаб турниров
### G2. Лобби
### G3. Отсчёт и вход в раунд
### G4. Экран вопроса
### G5. Таблица и финал
### G6. Разбор ошибок

По каждому: первый кадр, состояния загрузки, пустые состояния, ошибки сети,
переходы, хаптика, звук, доступность.

---

## H. ОТКАЗЫ, ЧЕСТНОСТЬ, ЭКСПЛУАТАЦИЯ

### H1. Отвал и переподключение
### H2. Античит и защита экономики

---

## ЖУРНАЛ РЕШЕНИЙ ВЛАДЕЛЬЦА

Заполняется по мере ответов. Формат: `[номер] вопрос → выбранный вариант`.

_(пусто — опрос ещё не начат; вопросы 1–4 были заданы текстом, ответы НЕ получены,
владелец попросил задавать их формой выбора и начать заново с вопроса 1)_

---

# ПЕРЕДАЧА В НОВУЮ СЕССИЮ — ЧИТАТЬ ПЕРВЫМ

Этот раздел написан для ИИ-ассистента, который продолжит работу в новой сессии.
Здесь всё, что нужно знать, чтобы не начинать с нуля и не переспрашивать владельца.

## 1. Что от тебя требуется прямо сейчас

Задать владельцу **150 вопросов пачками по 4**, строго по порядку разделов
A → B → C → D → E → F → G → H (структура выше). Начать заново с вопроса 1.

**ЖЁСТКИЕ ТРЕБОВАНИЯ ВЛАДЕЛЬЦА К ФОРМАТУ ВОПРОСОВ** (нарушение = переделка):

1. **Задавать ТОЛЬКО инструментом формы выбора (AskUserQuestion), не текстом.**
   Владелец прямо потребовал: «вопросы задавать формой тут! сразу все объясняя
   в форме выбора». Текстом задавать нельзя — он это уже отклонил.
2. **По 4 вопроса за раз.** Дословно: «И ТЫ БУДЕШЬ ЗАДАВАТЬ ИХ ПАЧКАМИ ПО 4».
3. **Ровно 3 варианта ответа** на каждый вопрос — три лучшие мировые практики.
4. **ВСЕГДА ставить рекомендацию** — рекомендованный вариант идёт ПЕРВЫМ и
   помечается «(Рекомендую)» в названии.
5. **ВСЕГДА объяснять ПОЧЕМУ** рекомендуешь именно это.
6. **Объяснять как ребёнку.** Дословно: «я не айтишник и я много не понимаю ты
   должен объяснять как ребенку». Никакого жаргона. Бытовые аналогии
   (школьная контрольная, настенные часы, микроволновка, расписание поездов).
   Максимально подробно — и сам вопрос, и каждый вариант.
7. Описание каждого варианта должно честно называть его минусы, а не только плюсы.

## 2. Кто владелец и как с ним работать

- Отвечать **всегда по-русски**.
- Он пишет в резком тоне, с матом, часто КАПСОМ и с опечатками. Это не про тебя
  лично — он вымотан длинной серией поломок турнира. Не оправдываться,
  не извиняться многословно, не морализировать. Просто делать работу.
- Он **не программист**. Любое техническое слово надо разворачивать в аналогию.
- Он ценит: конкретику, скорость, честность про минусы, готовые команды для
  копирования.
- Он ненавидит: воду, повторные объяснения одного и того же, вопросы по одному
  вместо пачки, отчёты о готовности без реальной проверки.
- В конце КАЖДОГО ответа обязателен цветной итог рисков (🔴 🟡 💸 ⚡ 🧩 — только
  непустые категории), а после него — простой итог списком без технических слов.
  Это правило из его глобального CLAUDE.md, оно действует всегда.

## 3. Что уже сломано и починено в этой сессии (не переделывать заново)

Владелец не мог войти в турнир. Причин оказалось несколько, все найдены и
исправлены, всё задеплоено на боевой Firebase (проект `phraseman-ea0b3`):

1. **`tournamentStartNow` не была задеплоена** — существовала только в исходниках.
   Клиент звал несуществующую функцию → NOT_FOUND → «Не удалось войти».
   Задеплоена в `europe-west1`. Таймаут поднят 15с → 60с, инстансы 1 → 10.
2. **Вход зависел от тиков кронов.** Комнату шарда 0 создавал только крон раз в
   5 минут (`room_not_found`); комната жила в состоянии `scheduled`, а вход
   требовал строго `lobby` (`room_not_joinable`); комната от крона рождалась
   пустой. Исправлено: вход сам создаёт и наполняет комнату
   (`ensureRoomPlayable` в `functions/src/tournaments.ts`), пускает и в
   `scheduled`. Коммит `eef10376f`.
3. **Дев-начисление жемчужин не доходило до сервера.** Дев-кнопка магазина
   писала баланс только в телефон; серверная запись шла через `shardsApplyDelta`,
   который сверяет причину с каталогом `shard_reward_catalog` — каталог намеренно
   обнулён, причины `shards_store_purchase` в нём нет. Турнир читает СЕРВЕРНЫЙ
   `users/{uid}.shards` и отвечал `not_enough_gems` при «+500» на экране.
4. **Комнаты отменялись с `not_enough_players`.** Отмена в коде уже была убрана,
   но кроны `tournamentAdvanceRooms` / `tournamentFillBots` /
   `tournamentCreateRooms` работали в СТАРОЙ версии — их не задеплоили.
   Задеплоены. **Урок: турнирные функции надо деплоить пачкой, а не точечно.**
5. **Вход стоил жемчужины.** По требованию владельца сделан бесплатным:
   в `normalizeTournamentEconomy` нижняя граница `entryGems` была 1, поэтому 0
   молча откатывался на 3. Граница снижена до 0. Коммит `a402a79ee`.
   На боевом сервере выставлено `tournamentSchedule/economy.entryGems = 0`.

**Проверено после всех правок:** комната создаётся, входит в `lobby`, не
отменяется, проходит `round1 → table1 → round2 → table2 → round3`. Владелец
реально вошёл в турнир.

## 4. ЖЕЛЕЗНОЕ ПРАВИЛО ВЛАДЕЛЬЦА (нельзя откатывать)

Дословно (2026-07-27): «когда я в дев режиме — начисление на ЛЮБОЙ аккаунт через
дев ВСЕГДА админское и ВСЕГДА идёт на сервер. Сделай это железным правилом и
зафиксируй тестами, чтобы никто не изменил — это моё решение».

Реализовано: функция `devShardsGrant` (`functions/src/dev_shards_grant.ts`) —
без проверки админ-роли, пишет серверный баланс транзакцией, идемпотентна по
`opId`. Прод защищён серверным рубильником
`remote_config/app.numbers.dev_shards_grant_enabled` (fail-closed).
Управление: `node scripts/set_dev_shards_grant.mjs on|off|status`.
Закреплено 14 тестами: `tests/dev_shards_grant_contract.test.ts` (6) и
`functions/src/dev_shards_grant.test.ts` (8). **Эти тесты трогать нельзя.**

## 5. ЧТО НУЖНО ВЕРНУТЬ ПЕРЕД РЕЛИЗОМ В СТОР (критично!)

Сейчас проект в дев-состоянии. Перед публикацией обязательно:

- `node scripts/set_dev_shards_grant.mjs off` — иначе любой пользователь,
  зная имя функции, начислит себе жемчужины.
- Вернуть `tournamentSchedule/economy.entryGems` с 0 обратно на 3 — сейчас
  вход в турнир бесплатный для всех.

## 6. Живые баги игрового экрана (владелец прислал скриншоты)

Найдены прямо перед началом опроса, НЕ исправлены — их надо решить по ходу
плана (разделы F7 и G4), либо починить раньше, если владелец попросит:

1. **«Вопрос 9 из 24»** — идут 24 вопроса подряд вместо 4 раундов по 4 задания.
   Константы: `DEFAULT_TASKS_PER_ROUND = 4` (`functions/src/tournaments.ts:84`),
   `TASKS_PER_ROUND = 4` (`tournament_ai_blueprint.ts:55`),
   `QUESTIONS_PER_ROUND = 4` (`app/tournament_round.tsx`).
2. **Режим «Соедини пары» сделан не по макету.** Владелец: «СОЕДИНИ ПАРЫ ЭТО
   СРАЗУ НА ОДНОМ ЭКРАНЕ СЛЕВА И СПРАВА СЛОВА НА ДВУХ ЯЗЫКАХ И ТАК БЫЛО ПО
   МАКЕТУ». Сейчас показывается одно слово сверху и список снизу — неверно.
   Эталон — макет 07 (поле пар / speed_match).
3. **Индикаторы и звёзды уезжают за экран** — вёрстка шапки раунда ломается.
4. **Красная ошибка в рантайме:** `Cannot update a component ('ForwardRef...')`
   — обновление состояния во время рендера чужого компонента.
5. **Лобби появляется мгновенно всей стеной** вместо постепенного появления
   игроков; отсчёт «16 секунд» доходит до нуля и **висит ещё полминуты**, потом
   начинается раунд. Причина висения известна: крон двигает фазы раз в минуту,
   а фазы длятся 5–12 секунд (это и решает вопрос 4 опроса).

## 7. Ключевые файлы

| Что | Где |
|---|---|
| Серверная логика турниров | `functions/src/tournaments.ts` |
| Константы и чистые функции | `functions/src/tournament_core.ts` |
| Экономика турнира | `functions/src/tournament_economy.ts` |
| Клиент турнира (вызовы функций) | `app/tournament_client.ts` |
| Экран хаба | `app/(tabs)/tournaments.tsx` |
| Экран раунда | `app/tournament_round.tsx` |
| Экран лобби | `app/tournament_lobby.tsx` |
| Дев-начисление (сервер / клиент) | `functions/src/dev_shards_grant.ts` · `app/dev_shards_grant.ts` |
| Аудит на 101 проблему | `TOURNAMENTS_AUDIT_2026-07-27.md` |

## 8. Полезное про окружение

- Доступ к боевой базе есть: `service-account.json` в корне проекта. Скрипты
  для чтения/записи Firestore запускать **из папки `functions`** (там лежит
  `firebase-admin`), либо из корня, если модуль доступен.
- `firebase functions:list` и `firebase deploy --only functions:ИМЯ` работают.
- `gcloud` НЕ авторизован — не использовать.
- Сборка функций: `cd functions && npm run build`. Если появятся ошибки TS5033
  («не могу записать файл») — папку `lib` держит другой процесс, надо
  пересобрать и проверить, что в `lib` попала свежая версия.
- Тесты: `npx jest <путь>` из корня (клиент) или из `functions` (сервер).
- Владелец часто запускает **несколько сессий Claude одновременно** на одном
  репозитории. Работать только в текущей ветке, не создавать worktree и веток,
  коммитить часто. Если в файле чужие незакоммиченные правки — не лезть в него,
  искать обходной путь и сразу сказать владельцу.
- В `app/tournament_round.tsx` на момент передачи были ЧУЖИЕ незакоммиченные
  правки (другая сессия) — проверить перед редактированием.

## 9. Первая пачка вопросов (уже подготовлена — задать формой)

Ниже готовый текст. Задать через AskUserQuestion: 4 вопроса, по 3 варианта,
рекомендованный первым с пометкой «(Рекомендую)».

**Вопрос 1 — Кто главный по времени.**
Аналогия: школьная контрольная. Учитель с настенными часами (сервер) и ученики
с наручными (телефоны). По чьим часам считается «время вышло»?
Сейчас: телефон считает сам от числа «15 секунд», сервер считает отдельно, никто
их не сверяет. Часы телефона сбиты → всё ломается.
- А (Рекомендую) **Сервер задаёт, телефон рисует.** Сервер один раз говорит,
  когда вопрос кончится; телефон рисует плавный отсчёт сам; решение «успел или
  нет» принимает только сервер; при входе телефон один раз сверяет часы и
  запоминает поправку. Так сделаны Kahoot и HQ Trivia: красиво, честно, дёшево.
- Б **Только сервер.** Телефон каждую секунду спрашивает «сколько осталось».
  Максимально честно, но куча запросов = деньги, и цифра дёргается при плохой связи.
- В **Только телефон.** Дёшево и плавно, но любой переведёт часы и получит фору.
  Так сейчас.

**Вопрос 2 — Что именно присылает сервер про время.**
Аналогия: микроволновка («у тебя 15 секунд») против расписания поездов
(«отправление в 20:34:07»). Если сообщение шло 2 секунды, микроволновка даст
17 секунд вместо 15 — фора. Расписание даст честные 13.
Сейчас: способ «микроволновки». Отсюда фора при плохой связи и странности после
сворачивания приложения.
- А (Рекомендую) **Точный момент окончания.** Чинит и задержку сети, и
  сворачивание приложения. Стандарт соревновательных игр.
- Б **И момент, и длительность сразу.** Момент — для честности, длительность —
  чтобы красиво рисовать полоску прогресса. Чуть больше данных; длительность
  легко посчитать самому.
- В **Только длительность («15 секунд»).** Просто, но даёт фору за плохую связь
  и ломается при сворачивании. Так сейчас.

**Вопрос 3 — Верить ли телефону, когда он говорит «я ответил быстро».**
За скорость даются бонусные очки. Если секунды меряет телефон и просто сообщает
серверу — жулик подделает сообщение и всегда будет писать «0.1 секунды», выигрывая
призы без знания языка. Если меряет сервер — подделать нельзя, но в результат
попадает время на дорогу по сети, и человек с медленным интернетом страдает.
Сейчас: сервер вообще не меряет время каждого ответа, считает одно на весь раунд.
- А (Рекомендую) **Сервер меряет, но делает скидку на связь.** Вычитает поправку
  на дорогу сообщения, ставит границы (быстрее 0.3с — это бот). Жульничать
  нельзя, человек с плохой связью не страдает. Так делала HQ Trivia с реальными
  денежными призами.
- Б **Только сервер, без скидок.** Честно и неподделываемо, но человек в метро
  систематически проигрывает.
- В **Верим телефону.** Точно меряет «время думания», но полностью открыто для
  жульничества. При реальных призах недопустимо.

**Вопрос 4 — Кто двигает игру вперёд.**
Кто-то должен переключать этапы (вопрос → ответ → таблица → следующий), как
ведущий в телевикторине. Кандидаты: робот-будильник на сервере (надёжен, но
просыпается по расписанию), сам телефон (мгновенно, но при свёрнутом приложении
комната зависнет), оба вместе.
Сейчас: робот просыпается РАЗ В МИНУТУ, а этапы длятся 5–12 секунд. **Это и есть
причина «дошло до нуля и висит ещё полминуты».**
- А (Рекомендую) **Телефон толкает, робот страхует.** Таймер дошёл до нуля —
  телефон говорит серверу «двигай», сервер перепроверяет время и двигает. Робот
  просыпается редко и подбирает только брошенные комнаты. Мгновенно для игрока,
  копейки для бюджета, жульничество исключено перепроверкой.
- Б **Только робот, но будить чаще.** Просто, но робот постоянно перебирает все
  комнаты — расходы идут, даже когда никто не играет.
- В **Постоянное соединение.** Идеально быстро, но Firebase так не умеет: нужен
  отдельный сервер и отдельные деньги. Для викторины перебор.

## 10. Что делать после ответов

1. Записать ответы в «ЖУРНАЛ РЕШЕНИЙ ВЛАДЕЛЬЦА» выше (формат:
   `[1] Кто главный по времени → вариант А (сервер задаёт, телефон рисует)`).
2. Сразу задать следующую пачку (вопросы 5–8): время при сворачивании
   приложения; ответ, пришедший впритык к дедлайну; починка зависшей комнаты;
   как телефон узнаёт об изменениях (транспорт).
3. Реализацию НЕ начинать, пока не пройдены все 150 вопросов — если владелец
   явно не попросит починить что-то раньше.
